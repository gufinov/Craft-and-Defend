# Local evidence and builds

Large screenshots, logs, save fixtures and exports may live locally here; they are ignored. Commit concise evidence under `docs/evidence/` with commit, configuration, artifact SHA-256 and reproducible steps. Do not commit real user saves, credentials or engine binaries.
