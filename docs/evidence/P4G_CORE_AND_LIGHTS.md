# Evidence — P4G Core of Power and light sources — 2026-09-19

STATUS: CANDIDATE — editor gate (T148, T149) and windowed visual (T150) PASS; five regression gates PASS in the editor; `TEST_P4_ASSETS.cmd` on the export NOT RUN (no export built on this branch); owner playtest NOT RUN.

DONE: Implemented [the P4G contract](../P4G_CORE_AND_LIGHTS.md): eight entities, items and seven Workbench recipes (orders 197–203) in `contracts/content.json` / `game/data/content.json`, each entity with `visual` parts, `navigation` tags + integrity, `defense` and the new **`attributes`** block (value, role, light, mount, space, notes); `tools/validate_foundation.py` requires and type-checks `attributes` on the eight entities, checks `space` against the occupied extent, cores ground-only with a light, defense/navigation integrity agreement, and `hidden` items (the enemy core) never being recipe outputs; `ContentRegistry.is_hidden_item` / `entity_attributes`, `recipes_for` drops hidden outputs; owner renders as card icons (`tools/generate_derived_icons.py` `OWNER_ICONS` + `key_black_backdrop` for the six opaque-black renders; `tools/measure_item_atlas.py` `DERIVED_ORDER`; atlas regenerated, regions re-measured); `GameSession` builders `_build_core_of_power_visual`, `_build_enemy_core_visual`, `_build_torch_visual`, `_build_wall_lantern_visual`, `_build_post_lantern_visual`, `_build_campfire_visual`, `_build_light_block_visual` with shared `_add_core_slab`, `_add_lantern_body`, `_add_flames`, `_add_mesh_sphere`, `_rune_material`, and `_add_entity_light` (one `EntityLight` OmniLight3D per entity from `attributes.light`; `_start_light_flicker` Tween ±15 % for torch and campfire); `P4AssetsAutomation` T148–T150 registered in `app.gd` (`--p4-assets-automation=gate|visual`); `TEST_P4_ASSETS.cmd`; P3F `EXPECTED_WORKBENCH_ORDER` extended; docs (contract with the asset attribute sheet, this evidence, INDEX, BACKLOG, TEST_PLAN, design direction §9 note).

EXPECT: At the Workbench, page 3 ends with Torch, Wall Lantern, Post Lantern, Campfire, Light Block Blue, Light Block Red and Core Of Power, each showing the owner's render as its card. Place a Core of Power on nine flat cells: a stepped stone slab with gold corner posts and blue gems carrying a rune monolith that glows blue at night. Torches and campfires flicker; lanterns and light blocks hold steady; nothing ever goes out. A torch needs a solid cell under it (ground, wall top, a block or another placed entity); a post lantern needs three free cells above its support; the campfire needs 3×3. The enemy core cannot be crafted and is not in the book.

TEST:

| Test ID | Expected | Actual | Result | Evidence |
|---|---|---|---|---|
| Static | `python tools/validate_foundation.py`; `python -m unittest discover -s tests -q` | PASS (184 links, 45 items, 38 recipes); 60/60 | PASS | this worktree |
| Parse | `--check-only` on `game_session.gd`, `content_registry.gd`, `app.gd`, `p4_assets_automation.gd`, `p3f_presentation_automation.gd` | clean (no inference warnings) | PASS | — |
| T148 | asset attributes | no attribute or recipe problems; enemy core hidden and absent from the 35-entry book; no missing icons; core role/value/mount/space/colour/12 cells OK; enemy red/0/400; torch and campfire flicker, lantern steady | PASS | scratch `agent-lights/gate2/run.log` |
| T149 | placement and light | all eight placed OK; one `EntityLight` each with the attribute colour and range; mesh parts core 80, enemy 84, campfire 39, post lantern 55, wall lantern 40, torch 27, light blocks 55; core owns its 12 cells and not the corner above; integrities 240 / 400 / 4; floating torch `UNSUPPORTED`, torch on the light block OK; torch energy 1.60 → 1.84 over 0.5 s, lantern steady | PASS | same |
| T150 | rendered assets (windowed) | `p4-assets.png` 1280×720 at Sunset 20:30, 8 lights, 435 mesh parts; both cores, campfire, both lanterns, torch and both light blocks in view with their light pools | PASS | scratch `agent-lights/visual2/p4-assets.png` |
| Regression | `--p3f-presentation-automation=gate`, `--p3c-player-defense-automation=phase1`, `--p4-siege-units-automation=gate`, `--p3g-furnace-usability-automation=gate`, `--p3e-container-automation=gate` | all `*_AUTOMATION_PASS` (3, 9, 9, 7, 5 records), no script errors | PASS | scratch `agent-lights/reg-*` |
| Exported | `TEST_P4_ASSETS.cmd` | NOT RUN (no export on this branch) | NOT RUN | — |
| Owner playtest | crafting and placing the cores and lights | NOT RUN | NOT RUN | — |

LIMITATIONS/FAILURES: No wall or ceiling side-face mounting — the torch and the wall lantern stand on a solid top (the wall lantern's bracket post reads as a bracket when set on a wall top or beside a wall). The new-game inventory grants nothing, so the Core of Power is crafted (8 castle stone + 2 gold ingot) rather than owned at the start. The wave drill still uses `CoreDefenseService`'s abstract core cell; `core_defense_service.gd` and `basic_raider.gd` were not touched (another engineer wires the drill to the placed core). No enemy camp places the red core. Lights are presentation only: no effect on enemies, no cooking at the campfire. Models are box/cylinder/cone approximations of the owner's renders — expect proportion notes. Light blocks are entities, not voxels (the pick does not mine them; dismantle returns the item).

NEXT: owner playtest; export and run `TEST_P4_ASSETS.cmd`; retarget the wave drill to the placed core; enemy camps with a red core; wall brackets; merge decision with the P4 branch.

GIT/REPRODUCIBILITY: worktree `D:\CODEX\Craft_and_Defend\worktrees\p4-core-and-lights`, branch `feature/p4-core-and-lights` on top of `508f066` (`feature/p3d-tools-world-feedback`); not pushed, not merged. Engine `D:\CODEX\_tools\GodotVoxel\4.6-1.6\editor\godot.windows.editor.x86_64.exe` (4.6.stable.custom_build.89cea1439, Voxel Tools 1.6). Commands: `godot --headless --path game --import`; `godot --headless --path game --log-file <dir>\run.log -- --f0-data-root=<dir> --p4-assets-automation=gate`; the same without `--headless` and `=visual`; `python tools/generate_derived_icons.py && python tools/measure_item_atlas.py` after changing owner art.
