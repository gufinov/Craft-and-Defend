# Evidence — <gate> — <date>

STATUS: NOT RUN / PASS / PARTIAL / FAIL / BLOCKED

DONE:

EXPECT:

TEST:

| Test ID | Expected | Actual | Result | Evidence |
|---|---|---|---|---|
| <ID> | <expected> | <observed> | NOT RUN | <log/screenshot/repro> |

LIMITATIONS/FAILURES:

NEXT:

GIT/REPRODUCIBILITY:
- Repo, branch, commit, worktree:
- OS / CPU / GPU / RAM / resolution / renderer:
- Engine executable version and SHA-256:
- Export template version and SHA-256:
- Build artifact SHA-256:
- Commands, seed, world bounds, content version, save root:
- Clean-process restart and editor-closed export tested:

Do not replace NOT RUN with PASS without execution. Record failed tests alongside successful ones.
