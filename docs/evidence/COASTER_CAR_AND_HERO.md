# Evidence — Coaster car and hero (ride the coaster, 1–9 speed, the hero character) — 2026-09-19

STATUS: CANDIDATE — editor gate (T164–T166) and windowed visual (T167) PASS on branch `feature/coaster-car-and-hero`; regressions PASS; `TEST_COASTER_CAR.cmd` NOT RUN against an export (no export built on this branch); owner playtest NOT RUN.

DONE: Implemented [the coaster car and hero contract](../COASTER_CAR_AND_HERO.md): content (`coaster_car` item / entity / recipe 207, mirror formatted, validator PASS: 49 items, 42 recipes; owner-art icon through `generate_derived_icons.py` / `measure_item_atlas.py`, 50 regions); `CoasterRails.CAR`; `CoasterCartService` parked carts, per-cart `set_speed` / `speed_of`, `cart_rig`, `travel_direction`; `CoasterRide` (board / leave / speed levels / dismount position / three-quarter chase camera / player body follow); `HeroModel` (unarmoured and armoured builds, walk cycle, seated pose); `PlayerController` hero child, `set_third_person`, `view_origin`, camera placement; `GameSession` car visual, parked registration, boarding through `_defense_interact`, ride keys and V in `_unhandled_input`, riding HUD, pause / save / death / dismantle guards, `set_hero_armored`; `SettingsStore.hero_armored` (`[hero] armored`); app pause-menu **Hero: Armour on/off**, Escape leaves the car before pausing, `--coaster-car-automation=` registration; sandbox stock + parked car on the loop's lead-in; `CoasterCarAutomation` T164–T167; `TEST_COASTER_CAR.cmd`; P3F expected book order; docs (contract, this evidence, INDEX, TEST_PLAN, KEYBINDS, STATUS pointer, BACKLOG card). Fixed in passing: `fire_service.advance` had been nested under `if coaster_carts != null:` by the coaster merge (fire only spread while a cart existed); it runs every frame again.

EXPECT: Start `START_COASTER_SANDBOX.cmd`. A grey-and-oak car with a blue banner nose stands at the start of the loop's lead-in ahead. Walk up, aim at it, press **Shift**: the view jumps behind and beside the car, the blue-tunic hero sits in it with his arms on the gold bar, the HUD reads `RIDING · speed 3/9 · 1-9 speed · Shift leave`, and the car rolls along the lead-in, climbs into the loop, hangs over the top, leaves by the exit, turns at the end and comes back. Press **7**: it goes faster ("Coaster speed 7 / 9"); **1**: a crawl. **Shift** (or Escape): you stand beside the parked car. On foot, press **V**: the camera pulls back over your shoulder and you see the hero walking; **V** again returns to first person. Pause → **Hero: Armour on**: the hero wears plate (also when seated); the choice survives a restart.

TEST:

| Test ID | Expected | Actual | Result | Evidence |
|---|---|---|---|---|
| Static | `python tools/format_content.py`; `python tools/validate_foundation.py`; `python -m unittest discover -s tests -q`; `python tools/generate_derived_icons.py`; `python tools/measure_item_atlas.py` | PASS (13 blocks, 49 items, 42 recipes, 202 links); 60/60; atlas 1536×1024, 50 regions | PASS | this worktree |
| Parse | `godot --headless --path game --check-only --script` on `player/hero_model.gd`, `rails/coaster_ride.gd`, `rails/coaster_cart_service.gd`, `rails/coaster_rails.gd`, `player/player_controller.gd`, `app/game_session.gd`, `app/app.gd`, `settings/settings_store.gd`, `diagnostics/coaster_car_automation.gd`, `diagnostics/coaster_sandbox.gd`, `diagnostics/p3f_presentation_automation.gd` | clean (no errors, no warnings) | PASS | — |
| T164 | content, icon, recipe | no missing icons; order 207, inputs planks 4 / iron ingot 2 / castle stone 1 → 1; rail speed 3 on `rail_mount`, role `rail`; stack 2; `CoasterRails.CAR` not a track id | PASS | scratch `agent-car/gate3/run.log` |
| T165 | parked car, Shift boards, speeds, keys, leave, snapshot | parked before boarding and still after 60 frames; 39 car parts; `Seat` present; `interact_from_view` → `COASTER_BOARDED`; riding (player inactive, ride camera current); seated hero (54 parts) under `Seat`; speed 3/9 HUD; measured **2.00** cells/s at speed 2 and **6.00** at speed 6; `hotbar_5` → speed 5, slot unchanged; `interact` → left, player active, 1.00 m beside the car at the rail floor; car parked after 30 more frames; mid-ride snapshot stores the dismount position and the car | PASS | same |
| T166 | hero parts, armour swap, walk, seated, V, setting | 60 named parts unarmoured (Hair, Face, Tunic, LionCrest, Scarf, Belt, Bracer, Boot, Sword, Blade…); armoured has Breastplate, Pauldron, ShoulderDiamond, Tabard, Greave, Vambrace and no Tunic; legs / arms swing after 12 walk steps; seated hides legs, arms at −1.25 rad, sword hidden; first person at (0, 1.6, 0); V → camera z > 3, y > 2, hero visible, aim origin at the eye; back to first person; `set_hero_armored(true)` saved and read back by a fresh `SettingsStore`; applied to the player hero and the ride; button text on / off | PASS | same |
| T167 | rendered ride (windowed) | `coaster-car.png` 1280×720; car 93 parts, hero 54 parts, rider cell `(0, 3, 36)` on the loop's riser, ride camera current; bonus `hero.png` (both looks facing the camera, the loop with the parked car behind) | PASS | scratch `agent-car/visual7/coaster-car.png`, `hero.png` |
| Sandbox | `--coaster-sandbox --coaster-sandbox-shot` headless | `COASTER_SANDBOX loop COASTER_PLACED car OK`, slope run 10 pieces cart OK, `COASTER_SANDBOX_READY` | PASS | scratch `agent-car/sandbox/run.log` |
| Regression | `--coaster-rails-automation=gate` (T160–T162), `--p4-siege-units-automation=gate`, `--p3f-presentation-automation=gate` and `=visual` (T103 "Page 2 / 4"), `--p3c-player-defense-automation=phase1` (T73), `--p3d-usability-automation=phase1` | all `*_AUTOMATION_PASS`, no script errors | PASS | scratch `agent-car/reg-*`, `regvis-p3f` |
| Pre-existing | `--f0-automation=phase1` | T10 `UNLOAD_RELOAD` FAIL (`unloaded: false`) **identically on the untouched base `ab50397`** (temporary detached worktree): the default 128-block view distance keeps the chunk 78 cells away loaded; `--f0-automation=gate` is not a mode (phase1 / phase2) | NOT A REGRESSION | scratch `agent-car/reg-f0/run1.log`, `base-f0/run.log` |
| Exported | `TEST_COASTER_CAR.cmd` | NOT RUN (no export on this branch) | NOT RUN | — |
| Owner playtest | ride feel, camera through the loop, hero look | NOT RUN | NOT RUN | — |

LIMITATIONS/FAILURES: See the contract's "Limits": no momentum / gravity (speed = key); one rider; no camera collision (ride or third person) and the ride camera swings around the car as the heading reverses over a loop's top; no first-person seat view; V is a raw key; Shift on a car boards it, so a car cannot be repaired with Shift; armour is a cosmetic pause-menu toggle; the ride is not saved (a mid-ride save reloads on foot beside the car). The sandbox's loop now carries the coaster car instead of a mine cart (the slope run keeps its cart). Third-person aim rays start from the eye point, so the crosshair can disagree slightly with what the ray hits when the camera is offset behind a wall.

NEXT: owner playtest of the ride (speeds 1–9 through the loop, camera comfort), the car look and the hero; decide on momentum / gravity, a seat first-person view and camera collision; earn the armour; then the merge decision after `feature/p3d-tools-world-feedback` lands.

GIT/REPRODUCIBILITY: worktree `D:\CODEX\Craft_and_Defend\worktrees\coaster-car-and-hero`, branch `feature/coaster-car-and-hero` on top of `ab50397` (`feature/p3d-tools-world-feedback`); not pushed, not merged. Engine `D:\CODEX\_tools\GodotVoxel\4.6-1.6\editor\godot.windows.editor.x86_64.exe` (4.6.stable.custom_build, Voxel Tools 1.6), Windows 11. Commands: `godot --headless --path game --import`; `godot --headless --path game --log-file <dir>\run.log -- --f0-data-root=<dir> --coaster-car-automation=gate`; the same without `--headless` and `=visual`; `--coaster-sandbox --coaster-sandbox-shot`; regression suites as listed above.
