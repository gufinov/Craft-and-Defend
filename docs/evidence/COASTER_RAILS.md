# Evidence — Coaster rails side project (slopes, loop drag tool, mine cart) — 2026-09-19

STATUS: CANDIDATE — MERGED into `feature/p3d-tools-world-feedback` as `0e39dcb`; editor gate (T160–T162) and windowed visual (T163) PASS; `TEST_COASTER_RAILS.cmd` PASS on the integration export (PCK `91aa8669…`) together with the other twelve runners; owner playtest NOT RUN.

DONE: Implemented [the coaster rails contract](../COASTER_RAILS.md): content (`rail_slope`, `rail_loop`, `mine_cart` items, entities with `attributes` role `rail`, recipes 204–206; validator rules for `slope`, `coaster_tool`, `cart`; drawn icons in the derived atlas, regions re-measured); `CoasterRails` (mutual joint rule, `connections`, `chain`, `connected_cells`, `ride_point`, `loop_offsets`); `SiegeDefenseService._rail_chain` → `CoasterRails.chain`, chain-driven `_rail_step` / `_chain_degree` / `_chain_end_farthest`, slope-aware `_rail_point`; `InteractionService` mode `coaster_loop` (`begin_coaster_loop_at`, `set_coaster_loop`, `resize_coaster_loop`, `coaster_loop_keys`, `_replan_coaster_loop`, `_commit_coaster_loop` writing `coaster_joints` through the new optional `extra` argument of `WorkstationService.try_place`); `GameSession` slope / loop / cart visuals, joint-driven rail arms, 3×3×3 neighbour refresh, X / C key reads, `CoasterCartService` lifecycle; `CoasterCartService` ride; `CoasterRailsAutomation` T160–T163 registered in `app.gd`; `TEST_COASTER_RAILS.cmd`; P3C / P3F expectations for the four-page book; docs (contract, evidence, INDEX, BACKLOG, TEST_PLAN, KEYBINDS note, STATUS pointer).

EXPECT: Workbench page 4: **Rail Slope**, **Rail Loop**, **Mine Cart**. Place Rails, then a Rail Slope at the end (turn it with W / R until it climbs toward the block ahead) and a Rail on top of that block: one track. Hold Rail Loop, right-press on the ground and drag sideways: a green line of ghost cubes; hold **Shift**: a ring of ghost cubes stands up at the end of the line with two more cells after it; **X** shrinks it, **C** grows it (2–6); release: the pieces appear — flat rail blocks along the line, an octagonal loop of pitched rail pairs. Place a Mine Cart on the first rail: it rolls along, up the slope or into the loop, hangs upside down over the top, leaves by the exit, turns around at the end and comes back, forever.

TEST:

| Test ID | Expected | Actual | Result | Evidence |
|---|---|---|---|---|
| Static | `python tools/format_content.py`; `python tools/validate_foundation.py`; `python -m unittest discover -s tests -q`; `python tools/generate_derived_icons.py`; `python tools/measure_item_atlas.py` | PASS (13 blocks, 48 items, 41 recipes, 194 links); 60/60; atlas 1536×1024, 49 regions | PASS | this worktree |
| Parse | `godot --headless --path game --check-only --script` on `rails/coaster_rails.gd`, `rails/coaster_cart_service.gd`, `interaction/interaction_service.gd`, `defense/siege_defense_service.gd`, `crafting/workstation_service.gd`, `app/game_session.gd`, `app/app.gd`, `diagnostics/coaster_rails_automation.gd` | clean (no errors, no warnings) | PASS | — |
| T160 | content, icons, recipes | no missing icons; orders 204 / 205 / 206; outputs 4 / 2 / 1; slope 1 and not linear; loop tool with empty support; cart speed 3 on `rail_mount` | PASS | scratch `agent-coaster/gate3/run.log` |
| T161 | slope chain, kettle router, cart | six-cell chain across two levels, slope degree 2, control pair isolated, `_rail_step` from the slope → `(3, 1, 0)`, 15 slope mesh parts, cart mount `rail_mount`, top rail reached at frame 52, back home at 105, rig y 0.55 | PASS | same |
| T162 | loop drag ghost, resize, commit, ride | lead-in 4; Shift → radius 3, 16 loop cells, 22 cells, end frozen; X → 2 / 18; C ×8 → 6 / 38; held X thrice → 5; back to 3; all 22 ghosts ok; `COASTER_PLACED` count 22, 22 pieces, chain 22, top joins 2, side piece `LoopArms`, lead-in `RailArms`; cart over the top at frame 145, exit end at 271, home at 544; snapshot → JSON → `restore` keeps the 22-cell chain with its joints | PASS | scratch `agent-coaster/gate5/run.log` |
| T163 | rendered loop with cart (windowed) | `coaster-rails.png` 1280×720; 246 loop mesh parts, cart 19 parts hanging at `(-1, 5, 36)` over the loop; slope run with its own cart | PASS | scratch `agent-coaster/visual2/coaster-rails.png` |
| Regression | `--p4-siege-units-automation=gate` (T133 / T154 / T155 kettles), `--p3d-usability-automation=phase1`, `--p3k-blueprint-automation=gate`, `--p3f-presentation-automation=gate` and `=visual` (T103), `--p3c-player-defense-automation=phase1` (T73) and `=visual` (T78) | all `*_AUTOMATION_PASS`, no script errors | PASS | scratch `agent-coaster/reg-*`, `regvis-*` |
| Pre-existing | `--p4-assets-automation=gate` | T149 FAIL (`wall_lantern` on a wall face → `INVALID_MOUNT`) **identically on the untouched base `5ce8c5a`** (verified with the changes stashed) | NOT A REGRESSION | scratch `agent-coaster/reg-p4-assets`, `reg-p4-assets-base` |
| Exported | `TEST_COASTER_RAILS.cmd` | PASS on PCK `91aa8669…` after the merge (`0e39dcb`) | PASS | `artifacts/manual-coaster-rails-*` (2026-09-19 sweep) |
| Owner playtest | slope, loop drag, cart | NOT RUN | NOT RUN | — |

LIMITATIONS/FAILURES: See the contract's "Limits": no corkscrew / DNA / helix; the loop shares its bottom row with entry and exit; flat-jointed top-row pieces draw as rail blocks; lead-in and exit are `rail_loop` pieces; slopes place one at a time; kettles do not route through loop joints; no gravity, hauling or cart panel; placeholder icons and models; X / C are raw keys (not rebindable). The Workbench book became four pages (38 recipes): T73 / T78 / T103 expectations now read "Page 1 / 4" and "Page 2 / 4". `rail_slope` default rotation on a fresh hotbar rises toward −z; the player turns it with W / R (no auto-alignment).

NEXT: owner playtest of the drag feel (Shift latch, X / C) and the loop's look; decide whether lead-in / exit should consume ordinary rails; corkscrew (`rail_corkscrew`) and a lateral loop offset; ore hauling; then the merge decision after `feature/p3d-tools-world-feedback` lands.

GIT/REPRODUCIBILITY: worktree `D:\CODEX\Craft_and_Defend\worktrees\coaster-rails`, branch `feature/coaster-rails` on top of `5ce8c5a` (`feature/p3d-tools-world-feedback`); pushed; merged into the integration branch as `0e39dcb` (docs conflicts resolved keep-both). Engine `D:\CODEX\_tools\GodotVoxel\4.6-1.6\editor\godot.windows.editor.x86_64.exe` (4.6.stable.custom_build.89cea1439, Voxel Tools 1.6), Windows 11. Commands: `godot --headless --path game --import`; `godot --headless --path game --log-file <dir>\run.log -- --f0-data-root=<dir> --coaster-rails-automation=gate`; the same without `--headless` and `=visual`; regression suites as listed above.

## Smooth Switch and Crossing (CoasterCraft cards 2-3, 2026-09-20, `feature/coaster-smooth-switcher`)

| Test | What | Evidence | Result | Where |
|---|---|---|---|---|
| T170 | Smooth Switch | press → `smooth_bend`; ghost 8 `rail_loop` curve cells, entry `(8,0,30)` → exit `(14,0,31)`; Shift-aim → length 10 / lanes −3, exit `(18,0,27)`; C 11, X 10; 40 capped to 21 for 24 items; stone on the path: 1 red, `BEND_BLOCKED`, nothing laid; `BEND_PLACED` 8, 8 items paid; chain 14 with rails at both ends, lane-1 rail beside the entry stays out; entry / exit ride at `y 0.55`, middle at `z 30.97`; `LoopTrack` drawn; cart far at frame 122, home 37 later; restore keeps 14 cells with `curve` | PASS | scratch `feature-coaster-smooth-switcher/gate/run.log` |
| T171 | Crossing | press → `rail_cross`; ghost 19 cells, 3 shared (`(11..13, 0, 43)`) with `joints_b`; `CROSS_PLACED`, 19 paid; shared record: two curves, two joint pairs, ≥ 3 connections; `pair_for` a / b differ; kettle point = A's ride point + 0.95; chain 31; cart on A: shared at frame 62, A's far rail at 145, never B's entry / exit, home on A; cart on B: B's far rail at 145, never A's; `LoopTrack` + `LoopTrackB`, one collision box; restore keeps the two-curve cell | PASS | same |
| T172 | rendered (windowed) | `coaster-smooth.png` 1280×720; 888 track mesh parts, 3 two-track cells; carts at `(21,0,34)` / `(25,0,38)` mid-piece | PASS | scratch `feature-coaster-smooth-switcher/visual/coaster-smooth.png` |
| Regression | `--coaster-rails-automation=gate` (T160-T162, T168), `--coaster-car-automation=gate`, `--p3f-presentation-automation=gate` (T90 order + `rail_bend`, `rail_cross`), `--p3c-player-defense-automation=phase1`, `--p3d-usability-automation=phase1`, `--p4-siege-units-automation=gate`, `python tools/validate_foundation.py` | all `*_AUTOMATION_PASS` / PASS | PASS | scratch `feature-coaster-smooth-switcher/<suite>/run.log` |
| Sandbox | `--coaster-sandbox --coaster-sandbox-facing-check` | loop 26 pieces, circuit 28 / 28, smooth switch 8 pieces + cart, crossing 19 pieces (3 shared) + cart; `reversals=0` in 3 of 5 runs — the count is wall-clock sampled and catches the circuit's own U-turn corner when a sample lands on it (the base cart service in the same sandbox gave 0 and 2); the hero faces travel (`dot=1.00`) in every sample | PASS (timing-sensitive check) | scratch `feature-coaster-smooth-switcher/sandbox/run*.log` |
| Owner playtest | Rail Bend / Rail Cross drag feel, Shift sizing | NOT RUN | NOT RUN | — |
