# Current checkpoint — 2026-09-11

**STATUS:** **F4 IMPLEMENTATION PASS on an isolated worktree; owner graphical acceptance pending.** F3 is preserved at owner-accepted commit `6e8838f`. T27–T30 and all F0–F3 regressions pass in the provenance-matched Windows export with the editor closed. Canonical `main` remains clean and unchanged at its accepted F1 checkpoint.

**DONE:** Preserved all accepted F0–F3 behavior. Added the runtime world-clock configuration, day/night lighting with readable ambient floor, phase/day persistence without wall-clock catch-up, visible HUD time, F2 game-viewport PNG capture under the global data root, complete keybind integration, and portable launcher/instructions copied by the Windows builder.

**EXPECT:** Time advances only while gameplay simulation runs, freezes during pause/inventory/save, restores its saved phase and never catches up from wall time. F2 captures the game viewport without stopping play and persists/rebinds globally across slots. The portable folder launches without Git or Godot Editor while preserving the complete Foundation loop.

**TEST:** PASS on Windows 11 `10.0.26200`, Godot `4.6.stable.custom_build.89cea1439`, Voxel Tools `1.6.0 Module`, OpenGL Compatibility on NVIDIA GeForce RTX 5090. Capture produced exact 1280×720 PNGs while physics, player and simulation stayed active; a fresh process retained F8 and Reset restored F2. Day phase froze in overlay/pause, night remained lit, and Continue restored the exact phase without offline catch-up. The 3440×1440 fullscreen and centered window regressions pass. The copied five-file folder and its exact launcher pass with no Git/tools and editor count zero; the repository `START_GAME.cmd` also launched one visible provenance-matched process. F0–F3 exported regressions pass. Validator PASS; 20/20 Python tests PASS. See [F4 evidence](evidence/F4_FOUNDATION_ACCEPTANCE.md).

**LIMITATIONS/FAILURES:** Screenshot capture requires a graphical renderer; the headless negative attempt has no viewport image, while editor/export/copy GPU paths pass. Release allocator counters are unavailable, so actual Windows process working set/private bytes were measured externally. Portable isolation is verified on this PC, not a second physical machine. Owner graphical F4 acceptance remains pending. No engine/content substitution occurred.

**NEXT:** Tony launches the F4 worktree, confirms F2 capture while moving, checks the PNG in `%APPDATA%\CraftAndDefend\screenshots`, optionally rebinds it, and observes the clock through play/pause. Fix any owner-found F4 defect before promotion. Do not merge, push or publish without separate authority.

**GIT/REPRODUCIBILITY:** Main path `D:\CODEX\Craft_and_Defend\main`, clean `main` and `origin/main` at `c085c013d39b2b64321ea6c5a696b96fe2efd45c`. F4 worktree `D:\CODEX\Craft_and_Defend\worktrees\f4-foundation`, branch `feature/f4-foundation-acceptance`, based on clean F3 acceptance commit `6e8838fc3112589f2fcf59291b02a0a040245729`. No merge, push or release performed.
