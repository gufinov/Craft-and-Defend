# Current checkpoint — 2026-09-11

**STATUS:** **F5 REVISED CANDIDATE AND WINDOWS EXPORT PASS; OWNER MOTION/DRAG/3440×1440 ACCEPTANCE PENDING.** Tony confirmed the prior F5 interaction loop, then reported oscillating sunlight shadows, terrain/block edge jitter and an underspecified crafting modal. The isolated F5 worktree now contains a bounded rendering-stability repair and the requested three-panel crafting workflow without changing engine, renderer, Voxel Tools edition or content IDs.

**DONE:** Compatibility rendering now defaults to 4× MSAA and VSync, exposes Off/2×/4×/8× MSAA plus VSync in Settings, and persists those preferences. Physics interpolation is enabled and reset after player activation/restore. Directional shadows use four blended, near-field splits and the light transform updates once per displayed game minute rather than every rendered frame. Crafting now presents visible inventory, staged 2×2/3×3 input and a searchable vertically scrolling recipe book with square text thumbnails. Inventory items can be dragged into the grid or selected then placed; recipe selection and search+Enter auto-fill when materials exist. Workbenches include basic hand recipes alongside advanced recipes while remaining right-click-only. Staging never mutates inventory; Craft still uses the existing atomic service.

**EXPECT:** Sidestepping no longer exposes fixed-physics/render mismatch jitter, voxel silhouettes receive configurable multisample smoothing, and the sun no longer reprojects its shadow map every rendered frame. Tab remains inventory only; B remains hand build; right-click remains the only station entry. At 1280×720 and ultrawide, all three crafting panels remain reachable through scrolling. Dragging or auto-filling cannot consume, reserve or duplicate resources before Craft.

**TEST:** Matching Windows export PASS. With the Godot editor process count at zero, exported F5 phase1/phase2/visual, F2 gate/Continue, F4 phase1/phase2 and F1 phase1/phase2/display-runtime all exited 0. T35 verifies 4× defaults, 8×/VSync preference persistence, physics interpolation, bounded blended shadow splits and unchanged light rotation within a visual minute. T32 verifies all basic+advanced workbench recipes; F2 reconfirms exact-once crafting/furnace behavior. T18 reconfirms native 3440×1440 fill, active-monitor containment and 1920×1080/1280×720/1600×900 window placement. Static validator PASS and 20/20 Python tests PASS. The exported 1280×720 three-panel capture is visually unclipped. See [F5 evidence](evidence/F5_CRAFTING_INTERFACE.md).

**LIMITATIONS/FAILURES:** Automated checks can prove configuration and transform cadence, but cannot honestly certify that perceived motion is comfortable on Tony's monitor; owner sidestep/day-cycle review remains required. Drag staging is count-based rather than positional recipe discovery. Recipe thumbnails are intentional wireframe text placeholders. Compatibility logs the existing nonblocking Windows root-certificate and shader-cache warnings in isolated editor runs. No new castle items, weather, armor, magic or combat systems were added.

**NEXT:** Tony launches the worktree's `START_GAME.cmd` and checks sidestepping, sunlight/shadows, Settings > Graphics Quality, drag/drop, search+Enter, basic recipes at the workbench and ultrawide layout. Fix owner-found defects before promotion. Do not merge, push or publish without separate authority.

**GIT/REPRODUCIBILITY:** Canonical checkout `D:\CODEX\Craft_and_Defend\main` remains clean on `main` at `c085c013d39b2b64321ea6c5a696b96fe2efd45c`. Active worktree `D:\CODEX\Craft_and_Defend\worktrees\crafting-interface`, branch `feature/crafting-interface`; implementation/export source `27d8d39b1f9d00bd2b51b7d446b38c22b3656717`; game tree `8a44a257ddf98e868a1ed30c7e87ca2dc8365cd2`. Pinned engine is Godot `4.6.stable.custom_build.89cea1439` with Voxel Tools `1.6.0 Module`, Compatibility renderer. Windows EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `e0893bdf818ac8642a589de20309bb9f46d51730c875e1eff43221157c32c6c3`; built UTC `2026-09-11T15:04:59.9784769Z`. No merge, push or release performed.
