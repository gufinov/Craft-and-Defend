# Current checkpoint — 2026-09-11

**STATUS:** **F1 source/history safe; launcher recovery automated PASS; owner relaunch pending.** Tony's post-promotion screenshot correctly showed F0, despite F1 being present on local and remote `main`. The cause was an ignored F0 export retained in canonical `main`; `START_GAME.cmd` trusted any existing executable, while cleanup removed the separate worktree that contained the tested F1 export. The source was not rolled back or lost. The recovery build is proven F1 and the launcher now validates build provenance before use.

**DONE:** Preserved the accepted F1 history at commits `805c3bc` through `6c4083d`. Added a build manifest containing the tracked `game` tree identity, source commit, engine version and artifact hashes. `START_GAME.cmd` now delegates to a provenance guard that rebuilds missing, legacy, mismatched or dirty checkout packages, but preserves normal portable-folder launch behavior. Corrected stale root/game/tool documentation and added a regression contract. See [launcher recovery evidence](evidence/F1_LAUNCHER_RECOVERY.md).

**EXPECT:** `START_GAME.cmd` first verifies that `builds\CraftAndDefend` matches the current tracked game tree. On canonical `main`, the legacy unstamped F0 package is rebuilt once and the accepted F1 menu opens with Settings, the complete keybind editor and the F1 development subtitle. Later documentation-only commits do not force a rebuild because provenance follows the `game` tree rather than repository HEAD.

**TEST:** Windows 11 Pro build 26200; Godot `4.6.stable.custom_build.89cea1439`; Voxel Tools `1.6.0 Module`; RTX 5090 OpenGL Compatibility. Missing, matching, falsified and legacy/unversioned package states behaved as required. The rebuilt package passed the exported F1 display/multi-monitor suite, both F1 restart phases and both F0 save/Continue phases with the editor closed. Foundation validation and all 18 Python tests passed. The exact one-click command selected the provenance-matched executable; this automation host exposed that manually launched process without a closable window handle, so it was stopped by its verified PID after the selection check.

**LIMITATIONS/FAILURES:** The earlier promotion closeout was wrong to claim the canonical graphical launch path was ready without testing the ignored build already present there. Git successfully preserved and pushed F1, but Git cannot validate ignored local artifacts. Two initial recovery-helper runs also falsely reported failure after successful export because they relied on an unset `$LASTEXITCODE`; hash-format validation replaced that check and all subsequent recovery cases passed. No user save was deleted or migrated, and no engine/content substitution occurred.

**NEXT:** Promote the launcher recovery, rebuild canonical `main`, and have Tony relaunch with `START_GAME.cmd` to confirm the visible F1 menu. F2 remains unstarted.

**GIT/REPRODUCIBILITY:** Canonical local and remote `main` were both `6c4083df75a327756f5f0516638a4a89e34ae808` when the incident was reproduced; the seven accepted F1 commits and source files were present. Recovery worktree: `D:\CODEX\Craft_and_Defend\worktrees\launcher-recovery`; branch `fix/launcher-build-provenance`; implementation checkpoint `3a93a8a`. Build output, manifest, logs and isolated test saves are ignored. No release was published.
