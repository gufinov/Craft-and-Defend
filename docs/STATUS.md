# Current checkpoint — 2026-09-25 (wave 4 merged into `main`: gates, signs v3, the Expo Directory and traps)

**STATUS:** **ALL FOUR WAVE-4 CARDS MERGED AND GREEN ON `main` (`77178a5`).** `main` carried `b472d16` (Development Expo + flight + signs v2 + defence sets). On top of it, in this order: **`204b40c`** signs v3 (`feature/signs-v3` `1b6aed0`), **`ccb4429`** the gate family (`feature/gates-v2` `35cbdc2`), **`4cfa084`** the Expo Directory (`feature/expo-directory` `2ec0861`), **`7b95c98`** traps (`feature/traps` `1882b75`), then **`77178a5`**, the T224 flight-test repair. Engine `4.6.stable.custom_build.89cea1439`. Packaged build at this head: EXE SHA-256 `4ac12872…`, **PCK SHA-256 `d0fcbfc6…`**. Every gate below was run at this head after the conflict resolution; the exported 15-runner sweep is **15 / 15 PASS**.

**DONE (this checkpoint):**
- **Signs v3** (`1b6aed0`, [Signs](SIGNS.md)): the **District Board** (`sign_board_large`, Workbench order **225**, 6 Planks + 3 Sticks) — the same sign three cells across — and the stacked **`header_body`** mode, which stores and restores a header, a subheader and a body and renders them in that order with none of them below its readable floor (0.10 / 0.070 / 0.055 m of cap height). Posts are tall now: a ground board's centre sits **1.65 m** above the floor it stands on, on a post whose foot reaches that floor; a wall board centres at **1.55 m** with no post. Every Expo district's entrance board asks for the large board and carries the stacked block, with a `subtitle` in the manifest sign record. T232 / T233.
- **Gate family** (`35cbdc2`, [Defence sets](DEFENSE_SETS.md)): the owner's **"I could open the gate, but not close it"** is fixed — an open leaf's blockers were its only collision, so the aim ray flew past it and `_raycast_station` never found a station to toggle back; it now falls back to the cells along the aim line for an open gate. **T229_GATE_TOGGLE** drives the real interact path (`InteractionService.secondary_press_from_view`) four presses at one aim and gets open, shut, open, shut. The family is now **Gate 1×2**, **Double Gate 2×3** and **Great Gate 4×4**, each with the frame built for it, each refusing a frame whose opening is not wholly clear (`OPENING_BLOCKED`). The Construction Yard stands a signed Double Gate booth and a signed Great Gate booth with a catapult parked beyond the Great Gate. T229 / T230 / T231.
- **Expo Directory** (`2ec0861`, [Development Expo](DEVELOPMENT_EXPO.md)): **K** opens `AppState.DIRECTORY` — a searchable list of every exhibit in the campus, filtered by district, category and test status, with **Teleport** (via `ExpoBuilder.approach_for`) and **Notes** on every row and a Notes button in every station panel. `ExpoNotes` records a status and a remark per exhibit; `--expo-notes-export` writes `artifacts/expo_notes.md`. The manifest gained **`added_in`** and its mojibaked dashes were repaired. `SaveCoordinator.read_checkpoint()` is new. T234 / T235 / T236.
- **Traps** (`1882b75`, [Traps](TRAPS.md)): **`TrapService`** ticks at 10 Hz and is driven entirely by content attributes; the **Spike Trap** (Workbench order **229**) is its first trap. Two rules are tested rather than asserted: a trap is **never targeted while a route exists** (T238), and `prefer_weakest` ranks the frontier so a blocked raider chews the weakest thing in its way (T239). The same card fixed blocked raiders **stranding on `NO_PERMITTED_BREACH`** for entities carrying no `defense` sheet. The Expo gained a **Trap Range** district with its own control pedestal (`control_group`). T237 / T238 / T239.
- **T224_FLIGHT was dead code and is a live test again** (`77178a5`). The signs-v2 card extracted `_run_wide_board_phase1` out of `_run_sign_phase1` and left the T224 block stranded **after that helper's `return`**, so flight went untested from `b472d16` until now. The block is `_run_flight_phase1()` and `_run_phase1` awaits it. Confirmed by evidence, not by reading: `--p3d-usability-automation=phase1` now records **`T224_FLIGHT ok=true`** beside `T232_SIGN_STACKED`.
- **Merge reconciliation** — the decisions made here, not silently. **Recipe orders**: signs' District Board and the gate family both wanted **225**. The District Board keeps 225 (the allocated block) and the Great Gate's leaf moved to **226**, so the workbench book now reads `double_gate_frame` 222, `double_gate` 223, `great_gate_frame` 224, `sign_board_large` 225, `great_gate` 226, `spike_trap` 229. `EXPECTED_WORKBENCH_ORDER` in `p3f_presentation_automation.gd` is in that book order and `docs/DEFENSE_SETS.md` was corrected where it named 225. **Icons**: `DERIVED` and `DERIVED_ORDER` end in the same order — `sign_board_large` (atlas cell **35**), then the gate family (36–39), then `spike_trap` (40) — and `derived_atlas_p4.png`, `item_atlas_regions.json` and `game/data/content.json` were **regenerated with the tools**, never hand-edited or text-merged. **The Expo manifest was merged semantically, by district and exhibit id, not by text** (a scripted 3-way merge whose emitter round-trips the repo's record-per-line style byte for byte, verified against all three inputs before it was trusted): every change from every card survives — signs' plaza `subtitle`, the gates card's two Construction Yard booths, the Directory's `added_in`, the traps card's `trap_range` district and the mojibake repair — with `game/data/development_expo.json` byte-identical to the contract. The fourteen district parcels do not overlap and `validate_foundation.py` agrees. `app.gd`, `game_session.gd` and `workstation_service.gd` auto-merged cleanly and were parse-checked; `docs/TEST_PLAN.md` carries **T229–T239 once each, in order**, with no renumbering.

**EXPECT:** `START.cmd` → **Development** → **New**. Everything the previous checkpoint describes still stands, plus: every district's entrance board is a **three-cell District Board on tall posts**, read at head height, with a header, a subheader and a body stacked on it; the **Construction Yard** stands a **Double Gate** and a **Great Gate**, each in its own frame, with a **catapult parked beyond the Great Gate** inside its four-cell opening; **right-clicking any gate now closes it as well as opens it**; **K** anywhere in the Expo opens the **Expo Directory** — search, filter, Teleport, Notes, Export Notes; and a new **Trap Range** district east of the plaza runs a raider down a corridor of **Spike Traps** with a control pedestal at its mouth.

**TEST (`77178a5`, 2026-09-25):** Static PASS — `python tools/validate_foundation.py` **PASS** (68 items / 61 recipes) and `python -m unittest discover -s tests` **81/81 OK**. Godot reimport clean and **all fifteen touched scripts parse** (`--check-only`). Headless editor gates at this head, each log under `artifacts/merge_gates_w4/<slug>/run.log`: `--p3d-usability-automation=phase1` **P3D_USABILITY_AUTOMATION_PASS** (8 records incl. **`T224_FLIGHT`** and **`T232_SIGN_STACKED`**), `--p4-siege-units-automation=gate` **P4_SIEGE_UNITS_AUTOMATION_PASS** (24 records incl. **T229_GATE_TOGGLE**, **T230_GATE_SIZES**, **T237_SPIKE_TRAP**, **T238_TRAPS_UNTARGETED**, **T239_BLOCKED_RAIDER**), `--development-expo-automation=gate` **DEVELOPMENT_EXPO_AUTOMATION_PASS** (19 records incl. **T231_GATE_EXPO**, **T233_EXPO_BOARDS**, **T234_DIRECTORY_SEARCH**, **T235_DIRECTORY_TELEPORT**, **T236_EXPO_NOTES**, **T239_TRAP_RANGE**; log `artifacts/merge_gates_w4/retry1/` — see LIMITATIONS for the first attempt), `--p3c-player-defense-automation=phase1`, `--p4-enemy-units-automation=gate`, `--p2-navigation-automation=phase1`, `--p3k-blueprint-automation=gate`, `--castle-kit-automation=phase1`, `--p3f-presentation-automation=gate`, `--p3e-container-automation=gate`, `--p3g-furnace-usability-automation=gate`, `--p4-resources-automation=gate`, `--p4-assets-automation=gate`, `--coaster-rails-automation=gate`, `--coaster-car-automation=gate`, `--f1-automation=phase1` (`passed:true`), `--f3-automation=phase1` (**F3_AUTOMATION_PASS**), `--development-check` (`ok=true failures=[]`) and `--coastercraft-check` (`ok=true failures=[]`) all **PASS**, plus `--coaster-sandbox --coaster-sandbox-facing-check` **`reversals=0`**. Windowed, one at a time: `--development-expo-automation=visual` **PASS** (17 screenshots, all 17 records ok) and `--p3d-usability-automation=visual` **PASS** (7 screenshots). **Windows export PASS** (`pwsh -NoProfile -Command "./tools/start_game.ps1 -PrepareOnly"`): EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`, **PCK SHA-256 `d0fcbfc6e1f104944d4864dd93d16a227d85bccf1e258817a19289e3f8d67fdf`**. `tools\runners\RUN_ALL.cmd` on that export: **15 / 15 PASS in 6 min** (`artifacts/run_all-20260925-012012`):

| Runner | Result | Seconds | ExitCode |
| --- | --- | --- | --- |
| TEST_COASTER_CAR.cmd | PASS | 6 | 0 |
| TEST_COASTER_RAILS.cmd | PASS | 32 | 0 |
| TEST_DEVELOPMENT_EXPO.cmd | PASS | 247 | 0 |
| TEST_P3C_PLAYER_DEFENSE.cmd | PASS | 10 | 0 |
| TEST_P3D_USABILITY.cmd | PASS | 8 | 0 |
| TEST_P3E_FURNACE.cmd | PASS | 20 | 0 |
| TEST_P3F_PRESENTATION.cmd | PASS | 6 | 0 |
| TEST_P3G_FURNACE_USABILITY.cmd | PASS | 5 | 0 |
| TEST_P3H_BALANCE_CONTROLS.cmd | PASS | 4 | 0 |
| TEST_P3K_BLUEPRINTS.cmd | PASS | 4 | 0 |
| TEST_P4_ASSETS.cmd | PASS | 5 | 0 |
| TEST_P4_ENEMY_UNITS.cmd | PASS | 5 | 0 |
| TEST_P4_RESOURCES.cmd | PASS | 2 | 0 |
| TEST_P4_SIEGE_UNITS.cmd | PASS | 29 | 0 |
| TEST_P4_WEAPON_PANEL.cmd | PASS | 4 | 0 |

**Screenshot verdicts (read at this head, not just recorded):**
- `development-expo-district-board.png` — **good.** The Defense Range's board is unmistakably three cells wide on three posts, standing at head height at the district mouth. "DEFENSE RANGE" is large and legible from standing distance; the subheader under it reads cleanly. This is the fix the owner asked for.
- `p3d-sign-stacked-board.png` — **good, with a nit.** The full stack renders in order: header, subheader "Siege weapons and ammunition.", then a three-line body. At five metres in dawn light the body lines are at the very bottom of legibility — correct by the readable floor, but the body is for stepping closer, not for reading across the plaza.
- `development-expo-gate-family.png` — **good.** Shot from inside the Great Gate's opening: the two-cell jambs stand either side and a real catapult is parked in the span with clear room each side and headroom over it. It shows the clearance claim rather than the leaf, which `development-expo-double-gate.png` covers — that one shows a barred leaf hanging shut in its frame beside its signed booth.
- `development-expo-directory.png` — **good.** The Directory panel reads as a finished tool: search box with "rail" typed, district / category / status filter rows, "16 of 110 entries · newest stamp 2026-09-24", and rows carrying an icon, a summary, a status and Teleport / Notes buttons, with the note editor down the right. Nothing is clipped at 1280×720.
- `development-expo-trap-range.png` — **good.** A walled corridor with spike traps laid down its floor, a raider partway along it taking damage (the HUD shows "Raider hit your core for 6. Core integrity: 198/240") and the control pedestal at the corridor's mouth. The district reads at a glance.

**LIMITATIONS/FAILURES:** Nothing is red at this head, but these stand honestly.
- **The Expo gate is flaky on this machine, and was already flaky before the merge.** The first `--development-expo-automation=gate` run at this head failed **T214_EXPO_MOUNTAIN** with eight unloaded cells in the mountain tunnel (`floor 0 / feet 0 / head 0` — chunks not streamed in yet). A control run of the **same suite at `b472d16`, the pre-merge head, also failed** — on **T216_EXPO_INDUSTRY**, `rail_chained: false`, in the same tunnel. The re-run at this head is clean (`failures []`, 19/19). Both evidence sets are kept (`artifacts/merge_gates_w4/development_expo_automation_gate/` is the failing first run, `.../retry1/` the clean one). This is a **pre-existing chunk-streaming race in the mountain tunnel**, not a wave-4 regression, and it deserves a fix rather than a re-run habit.
- ~~The central plaza board still falls back to the two-cell width.~~ **Retired by card D2** (`feature/devtools-polish`): districts carry their own `sign_anchor` / `sign_facing`, a `full` district's board is queued after its pad is levelled rather than before, and the plaza's board is the three-cell District Board standing on its own pad at `(7, 0, 36)` reading back at the spawn. A **district** board that falls back is now a T233 failure, not a note; `T246_PLAZA_BOARD` is the check.
- ~~Exhibit notes are held in memory until a save~~ and ~~the Directory lists at most 60 rows at a time~~ are both **retired by card D2**: a note is written through to `<data root>/development/expo_notes.json` the moment it is saved and is merged back in at Development start whether or not the world was ever checkpointed (`T245_NOTES_DURABLE`), and the Directory is paged rather than capped — 60 rows a page, `showing 1-60 of 110`, every entry reachable (`T247_DIRECTORY_PAGING`).
- Each card's other gaps are unchanged: **no lever, plate or drawbridge** for any gate and **no gate wider than four cells**; the **`slow` effect is declared in content but awaits `apply_slow`**, so the Spike Trap damages without slowing; the **rail turret's rail position is still not saved** (it re-homes to its anchor on load); and the save/restore path still validates stored ammunition against **`starting_ammo` rather than `capacity`**.
- Everything the previous checkpoint listed — the Expo being an exhibition rather than a mode with objectives, the `TEST_COASTER_CAR.cmd` shutdown-race flake, the loop's harmless `Basis::set_axis_angle` warnings, the long windowed visual run — is unchanged. **Owner playtest of these four cards: NOT RUN.**

**NEXT:** Owner playtest of the gate family, the District Boards, the Expo Directory and the Trap Range in the exported build at this head (the walk-through is below). Then, in rough order of value: fix the mountain-tunnel chunk-streaming race that makes the Expo gate flaky; district-level `sign_anchor` so the plaza board can be three cells wide; `apply_slow` so the Spike Trap's declared effect is real; a lever or plate for the gates; saving the rail turret's rail position. (Card D2 has done the district `sign_anchor`, the durable notes and the Directory's cap on `feature/devtools-polish`.)

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\main`, branch **`main`**, head **`77178a5`** — `b472d16` plus merge commits `204b40c` (`feature/signs-v3` `1b6aed0`), `ccb4429` (`feature/gates-v2` `35cbdc2`), `4cfa084` (`feature/expo-directory` `2ec0861`) and `7b95c98` (`feature/traps` `1882b75`), then the T224 repair, with this checkpoint's documentation commit on top. The four card worktrees under `D:\CODEX\Craft_and_Defend\worktrees\` were never touched. Engine `4.6.stable.custom_build.89cea1439` at `D:\CODEX\_tools\GodotVoxel\4.6-1.6`. Packaged build: EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`, PCK SHA-256 `d0fcbfc6e1f104944d4864dd93d16a227d85bccf1e258817a19289e3f8d67fdf` (`builds/CraftAndDefend/build_manifest.json`). Evidence at this head: `artifacts/merge_gates_w4/` (every headless gate log and its results JSON, the two windowed visual runs and their 24 screenshots, plus the failing first Expo run and the clean re-run) and `artifacts/run_all-20260925-012012` (the 15 / 15 sweep). Reproduce: `python tools/format_content.py`, `python tools/generate_derived_icons.py` and `python tools/measure_item_atlas.py` (all three idempotent at this head), then `pwsh -NoProfile -Command "./tools/start_game.ps1 -PrepareOnly"` and `tools\runners\RUN_ALL.cmd`.

**OWNER WALK-THROUGH (one pass around the campus).** Run `START.cmd` from **`D:\CODEX\Craft_and_Defend\main\builds\CraftAndDefend\`** — the build prepared from the worktree `D:\CODEX\Craft_and_Defend\main`. Choose **Development**, then **New**.
1. **Stand in the plaza and read the boards.** The orientation board north-east of the Core is now the full three-cell **District Board**, reading back at you from where you spawn (card D2). Walk to any district mouth — Supply Depot to the north is closest — and read its **District Board** from about five metres without stepping closer: three cells across, on tall posts, header then subheader then body.
2. **Press K.** The **Expo Directory** opens and the world keeps running behind it. Type `rail` in the search box and watch the count fall; clear it, then filter by district (**CONSTRUCTION YARD**) and by category (**Siege & Defense**).
3. **Teleport from the Directory.** Pick the **Double Gate** row and press **Teleport**. You should arrive standing in front of that booth, facing it.
4. **Work the gate family.** Right-click the **Double Gate**'s leaves: they open. **Right-click again — they close.** That is the bug the owner reported. Do it twice more to be sure. Then walk to the **Great Gate**, open it, and walk through: the **catapult parked beyond it** sits inside the four-cell opening with room each side.
5. **Leave a note.** With a gate panel open, press its **Notes** button (or use the Notes button on the Directory row), set a status — **Working** or **Broken** — write a remark and **Save note**. Press **K** and confirm the row now carries that status.
6. **Walk the Trap Range.** It is east of the plaza, between the Defense Range and CoasterCraft. Read its District Board, then stand at the **control pedestal** at the corridor's mouth and use it to send a raider down the corridor of **Spike Traps**. Watch it take damage as it walks the line; note that it walks the corridor rather than attacking the traps while a route exists.
7. **Check the recipe book.** Open a Workbench and scroll to the end: `double_gate_frame`, `double_gate`, `great_gate_frame`, **District Board**, `great_gate`, then **Spike Trap**, in that order, each with its own icon.
8. **Export what you found.** Back in the Directory, press **Export Notes**. From an exported build it writes `<data root>\artifacts\expo_notes.md` (from source it writes `artifacts/expo_notes.md` beside the repository); the panel tells you the path it used. Open it and check your remark from step 5 is in it.
9. **Save and reload.** **Escape → Save and Exit to Menu**, then **Continue**. The gates come back in the state you left them, and your notes are still in the Directory.

---

## Previous checkpoint — 2026-09-24 (signs v2 + defence sets merged into main)

**STATUS:** **BOTH CARDS MERGED AND GREEN ON `main` (`97d0034`).** `main` now carries the Development Expo + flight baseline (`60166e9`), the signs v2 card (`8ea72e5`) and the defence sets card (`9ca28cf`), reconciled in merge commit **`97d0034`**. Engine `4.6.stable.custom_build.89cea1439`. Packaged build at this head: EXE SHA-256 `4ac12872…`, **PCK SHA-256 `2cefc3ee…`**. Every gate below was re-run at this head after the conflict resolution; the exported 15-runner sweep is **15 / 15 PASS**.

**DONE (this checkpoint):**
- **Signs v2** (`8ea72e5`, [Signs](SIGNS.md)): the **Wide Board** (`sign_board`, Workbench order **219**, 4 Planks + 2 Sticks) — the same sign two cells across with a post at each end, its own item and entity so the placement stack's one entity = one footprint rule is untouched; it reserves both cells, refuses a pair whose second cell is taken, mounts on a wall with no posts and saves like any other sign. `WorkstationService.is_sign()` is now the single sign predicate. Board typography lays out before it shrinks (`GameSession.fitted_sign_text` / `wrap_sign_text`): lines wrap on word boundaries, never mid-word, a heading is sized for the board's own width, nothing drops below a readable floor, and content that still will not fit loses whole lines rather than glyph size. The Expo manifest gained **`sign_anchor`** and a sign block's **`"board": "wide"`**, the Supply Depot plinths were widened to match, and T212 was extended alongside the new T225.
- **Defence sets** (`9ca28cf`, [Defence sets](DEFENSE_SETS.md)): the **Gate** (`gate`, 1×2, Workbench order **220**, 6 Castle Stone + 2 Iron Ingot) hangs in a `gate_frame`'s `gate_mount` socket and starts shut — shut, `WorkstationService.navigation_cell_data` reports its cells solid and tagged `fortification`, so a raider with no other way picks it as an obstruction instead of routing through; right-click opens it (`gate_open` on the station record, saved), and a destroyed leaf leaves the frame standing. The **Wall Kit** (`wall_kit_8`) is the first blueprint to carry an `entities` list, and one bulk `inventory.try_transaction` pays for the whole kit. The **Rail Turret** (`rail_turret`, Workbench order **221**) is the siege block plus `rail_speed` on a `rail_mount`. Expo booths **`cy_gate`**, **`cy_wall_kit`** and **`dr_rail_turret`** were added, the Battlefield fortification now hangs a real gate that `ExpoResetService` shuts on reset, and the Defense Range widened 48 → 55 cells with its corridor moved south. Three real bug fixes came with it: `_restored_station` no longer drops patrolling siege weapons, rail riders now shop at the containers beside the rail cell they are riding rather than only their anchor, and `undo_last` now pops `_stamps`. T226 / T227 / T228 cover them.
- **Merge reconciliation (`97d0034`)** — the decisions made here, not silently: both cards wanted Workbench order 219, so **signs keeps 219** (the older card) and the defence recipes were renumbered to **Gate 220** and **Rail Turret 221**, with `EXPECTED_WORKBENCH_ORDER` in `p3f_presentation_automation.gd` and both cards' docs updated to match. The derived-icon lists in `tools/generate_derived_icons.py` and `tools/measure_item_atlas.py` end in the same order — `sign_board` (atlas cell **32**) then `gate` (33) and `rail_turret` (34) — and `game/assets/ui/derived_atlas_p4.png`, `game/data/item_atlas_regions.json` and `game/data/content.json` were **regenerated with the tools**, never hand-edited. The defence-sets card had re-indented the whole of `contracts/development_expo.json` to expanded JSON; the merge restores the repo's record-per-line style and takes the **union** of the manifest — signs' `sign_anchor` and `"board": "wide"` plus the three new booths, the Battlefield gate and the widened Defense Range — with `game/data/development_expo.json` byte-identical to the contract, as the validator requires. `game/scripts/crafting/workstation_service.gd` is the union of both cards' constants, records and match arms, with no duplicated helper. `docs/TEST_PLAN.md` carries T212, T225, T226, T227 and T228 once each, in order, with no renumbering.

**EXPECT:** `START.cmd` → **Development** → **New**. Everything the previous checkpoint describes still stands, plus: the plaza's orientation board and the Supply Depot's category boards are now **two-cell wide boards** whose headings never break mid-word; the Battlefield's arena sign stands at the player-side gate rather than the field's corner; the **Construction Yard** has a Gate booth (a leaf hanging in its frame — right-click it) and a Wall Kit booth; the **Defense Range** is five cells wider and stands a **Rail Turret** on its rail; and the Battlefield's curtain wall now has a real portcullis that starts shut, that the wave must breach or route around, and that **RESET BATTLEFIELD** shuts again.

**TEST (`97d0034`, 2026-09-24):** Static PASS — `python tools/validate_foundation.py` **PASS** (62 items / 55 recipes) and `python -m unittest discover -s tests` **81/81 OK**. Godot reimport clean and all eleven touched scripts parse (`--check-only`). Headless editor gates at this head, each log under `artifacts/merge_gates/<slug>/run.log`: `--p3d-usability-automation=phase1` **P3D_USABILITY_AUTOMATION_PASS** (T212 incl. the wide board), `--p4-siege-units-automation=gate` **P4_SIEGE_UNITS_AUTOMATION_PASS** (19 records incl. **T226_GATE** and **T228_RAIL_TURRET**), `--p3k-blueprint-automation=gate` **P3K_BLUEPRINT_AUTOMATION_PASS** (incl. **T227_WALL_KIT**), `--development-expo-automation=gate` **DEVELOPMENT_EXPO_AUTOMATION_PASS** (13 records incl. **T225_SIGN_ANCHORS**), `--p3c-player-defense-automation=phase1`, `--p4-enemy-units-automation=gate`, `--p2-navigation-automation=phase1`, `--p3f-presentation-automation=gate`, `--p3e-container-automation=gate`, `--p3g-furnace-usability-automation=gate`, `--p4-resources-automation=gate`, `--p4-assets-automation=gate`, `--coaster-rails-automation=gate`, `--coaster-car-automation=gate`, `--f1-automation=phase1` (`passed:true`), `--f3-automation=phase1` (**F3_AUTOMATION_PASS**), `--development-check` (`ok=true failures=[]`) and `--coastercraft-check` (`ok=true failures=[]`) all **PASS**, plus `--coaster-sandbox --coaster-sandbox-facing-check` **`reversals=0`**. Windowed, run one at a time: `--development-expo-automation=visual` **PASS** (11 screenshots, `artifacts/merge_gates/visual_expo/`) and `--p3d-usability-automation=visual` **PASS** (6 screenshots, `artifacts/merge_gates/visual_p3d/`). **Windows export PASS** (`pwsh -NoProfile -Command "./tools/start_game.ps1 -PrepareOnly"`): EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`, **PCK SHA-256 `2cefc3eecb574f8b00ab204acfa179c6cfaf64179d65ef8f999fbd20a0188a6a`**. `tools\runners\RUN_ALL.cmd` on that export: **15 / 15 PASS in 10 min** (`artifacts/run_all-20260924-221049`):

| Runner | Result | Seconds | ExitCode |
| --- | --- | --- | --- |
| TEST_COASTER_CAR.cmd | PASS | 8 | 0 |
| TEST_COASTER_RAILS.cmd | PASS | 42 | 0 |
| TEST_DEVELOPMENT_EXPO.cmd | PASS | 410 | 0 |
| TEST_P3C_PLAYER_DEFENSE.cmd | PASS | 10 | 0 |
| TEST_P3D_USABILITY.cmd | PASS | 7 | 0 |
| TEST_P3E_FURNACE.cmd | PASS | 20 | 0 |
| TEST_P3F_PRESENTATION.cmd | PASS | 8 | 0 |
| TEST_P3G_FURNACE_USABILITY.cmd | PASS | 5 | 0 |
| TEST_P3H_BALANCE_CONTROLS.cmd | PASS | 12 | 0 |
| TEST_P3K_BLUEPRINTS.cmd | PASS | 5 | 0 |
| TEST_P4_ASSETS.cmd | PASS | 6 | 0 |
| TEST_P4_ENEMY_UNITS.cmd | PASS | 6 | 0 |
| TEST_P4_RESOURCES.cmd | PASS | 2 | 0 |
| TEST_P4_SIEGE_UNITS.cmd | PASS | 28 | 0 |
| TEST_P4_WEAPON_PANEL.cmd | PASS | 5 | 0 |

**Screenshot verdicts (read, not just recorded):** the plaza orientation board renders as a real **two-cell wide board on two posts**, heading unbroken, body wrapped on word boundaries. `p3d-sign-wide-board.png` shows "DEVELOPMENT EXPO SUPPLY DEPOT" across the wide board with its 4 × 2 icon-and-name grid, readable at about four metres. The Supply Depot boards are wide, and the Construction category now pages to **1/2 and 2/2** because the Gate joined it. The Battlefield shot shows the new gate best: a **portcullis leaf hanging shut in the curtain wall's frame**, with the wave crossing the open ground toward it. See LIMITATIONS for what the visual pass does **not** show.

**LIMITATIONS/FAILURES:** Nothing is red, but the honest gaps from both cards stand. **No dedicated visual record exists for the `cy_gate` and `cy_wall_kit` booths** — the T218 Construction Yard camera frames the assembled castle, not the new booths, so the gate was read visually in the Battlefield shot instead; the booths themselves are covered only by the headless T218 / T219 records. The **rail turret's rail position is still not saved** (it re-homes to its anchor on load, as the kettle always has). The save/restore path validates stored ammunition against **`starting_ammo` rather than `capacity`**, so a weapon loaded above its opening clip is dropped; the rail turret dodges this by opening at capacity and the underlying check was left alone as out of scope. The gate has **no lever, plate or drawbridge**, **no gate wider than one cell** and no timed portcullis; the wall kit is a single eight-cell length with no dragged runs and no player-authored kits. Raiders still do not seek out a gate they cannot see a route through — they breach it only when it is the cheapest obstruction in their way. A tree block intrudes into the Construction Yard castle's gate approach in the T218 screenshot (pre-existing, cosmetic). Everything the previous checkpoint listed — the Expo being an exhibition rather than a mode with objectives, the `TEST_COASTER_CAR.cmd` shutdown-race flake, the loop's harmless `Basis::set_axis_angle` warnings, the ~20-minute windowed visual run — is unchanged. **Owner playtest of these two cards: NOT RUN.**

**NEXT:** Owner playtest of the gate, the wall kit, the rail turret and the wide boards in the exported build at this head. Then: a lever or plate for the gate, a multi-cell gate, a dragged run of wall kits, saving the rail turret's rail position, and a visual record that frames the two new Construction Yard booths.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\main`, branch **`main`**, head **`97d0034`** — the merge of `8ea72e5` (`feature/signs-v2`) and `9ca28cf` (`feature/defense-sets`) onto `60166e9`, with this checkpoint's documentation commit on top. Pushed to `origin/main`. Engine `4.6.stable.custom_build.89cea1439` at `D:\CODEX\_tools\GodotVoxel\4.6-1.6`. Packaged build: EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`, PCK SHA-256 `2cefc3eecb574f8b00ab204acfa179c6cfaf64179d65ef8f999fbd20a0188a6a` (`builds/CraftAndDefend/build_manifest.json`). Evidence at this head: `artifacts/merge_gates/` (every headless gate log plus the two windowed visual runs and their 17 screenshots) and `artifacts/run_all-20260924-221049` (the 15 / 15 sweep). Reproduce: `python tools/format_content.py`, `python tools/generate_derived_icons.py` and `python tools/measure_item_atlas.py` (all three idempotent at this head), then `pwsh -NoProfile -Command "./tools/start_game.ps1 -PrepareOnly"` and `tools\runners\RUN_ALL.cmd`.

---

## Previous checkpoint — 2026-09-23 (Development Expo + flight — owner playtest passed)

**STATUS:** **DEVELOPMENT EXPO COMPLETE AND OWNER-PLAYTESTED ON `feature/development-expo` (cards A–G, waves 1 and 2 merged, plus flight and two streaming fixes; last code commit `b788a8c`, with this checkpoint's own documentation commits on top).** Owner playtest of the campus: **PASS** ("everything works"); flight verified by the owner in the exported build. Engine `4.6.stable.custom_build.89cea1439`; the packaged build at this head is EXE SHA-256 `4ac12872…`, **PCK SHA-256 `704ae6ee…`** (`builds/CraftAndDefend/build_manifest.json`, built 2026-09-23T02:18:44Z from `b788a8c`). **NOT merged to `main` (`main` is still `23aa7eb`), NOT pushed.**

**DONE (this milestone, newest first):**
- **Signs that read properly** (2026-09-24, `feature/signs-v2`): a second board
  and a typographer. The **Wide Board** (`sign_board`, 4 Planks + 2 Sticks) is
  the same sign two cells across with a post at each end — its own item and
  entity, so the placement stack's one entity = one footprint rule is untouched;
  it reserves both cells, refuses a pair whose second cell is taken, mounts on a
  wall with no posts and saves like any other sign. **Board typography** now
  lays out before it shrinks (`GameSession.fitted_sign_text`): lines wrap on
  word boundaries and never mid-word, a heading is sized for the board's own
  width, nothing goes below a readable floor (0.030 m of cap height, 0.032 m
  for grid captions), and content that still will not fit loses whole lines
  rather than glyph size; the item grid puts each icon at the head of its row
  with the name beside it, so it reads at about four metres. In the Expo, an
  exhibit may now carry a **`sign_anchor`** (a cell offset, `centre`,
  `entrance` or `near:<exhibit_id>`) and `ExpoBuilder` stands its board there
  instead of at the parcel's corner: the Battlefield's arena sign now stands at
  the player-side gate, the plaza's orientation board and every Supply Depot
  chest board are wide boards (the depot's plinths were widened to match), and
  a rebuild replaces a board of the wrong width. T212 (phase1 + two visual
  records) and the new T225_SIGN_ANCHORS cover it. See [Signs](SIGNS.md).
- **Far parcels wait for streaming instead of failing** (2026-09-23, `32dad5e`, `b788a8c`): the exported build gave up on the CoasterCraft pad while the player still stood in the plaza — an op whose region has not streamed in no longer spends a requeue (`ExpoBuilder._region_loaded`, cap raised to 120), and a district's build wait settles and reports only its own districts' failures (`_wait_built(label, owners)`, `_failure_owned`).
- **A district is not called built while its own ground is still streaming** (2026-09-23, `8c21abf`): the Expo gate settles the builder before declaring a district done — T216 had once counted a rail line that was still arriving.
- **Flight** (2026-09-23, `fc3818e`, owner request): a double tap of **Right Shift** (within 0.4 s) toggles flying — the movement keys steer along the camera's aim (look up and press forward to climb), Space rises, Z drops, A doubles the speed, the body still collides with the world, and flight is refused while riding a coaster car; the first tap of the pair still performs its ordinary Interact. T224 (`--p3d-usability-automation=phase1`). See [Keybinds](KEYBINDS.md).
- **Development Expo — wave 2** (2026-09-22, `feature/development-expo`, cards D + E + F + G merged as `fa2b62f`, `fc21f51`, `3cd08b4`, `335e756`): the campus the handoff commissioned is now built end to end from one manifest. **Industry / Logistics** (card D) — the authored chain the owner can walk west to east: an ore face with a Miner, an Ore Bin beside it and beside the track, one unbroken rail line from the Mining Mountain's first cell `(-120, 0, -34)` to the yard's last `(-14, 0, -34)` with a Mine Cart on it, a Warehouse at the dock and a Foundry touching it, plus four storage-network booths that each pool exactly as their signs claim; a `placements` seam in the manifest pins named fixtures at exact offsets, and a `pavilion` terrain kind roofs the **Lighting** gallery where all six light sources stand side by side under shade (T216 / T217). **Construction Yard, Defense Range and Battlefield** (card E) — every castle piece on its own signed booth and the same pieces assembled (a drag-built wall with its deck and merlons, the P3K blueprint stack FOUNDATION 4 / TOWER SEGMENT 4 / CAP 4 stamped into ordinary voxels and recorded as stamps, a small castle with gate frame, stair and tower platform); six weapon booths, each on the mount its content sheet allows, with its munition in a container the real `StorageNetwork` reports, a target down its own lane, and the unmodified siege reload topping a drained Cannon back up; a live **Battlefield** whose `battlefield_control` pedestal runs START ATTACK through the existing `CoreDefenseService` drill at its own Core of Power, and whose RESET BATTLEFIELD restores the arena — both cores, the batteries, their ammunition, the magazine — through the `battlefield` reset group while every other district is left exactly as it stands (`ExpoResetService`, composite terrain kinds, `CoreDefenseService.start_prototype(core_station_id)`; T218 / T219 / T220). **CoasterCraft** (card F) — a component gallery with one signed booth per track family, each specimen laid by the real lay tool so it carries that tool's own curve, and the **Grand Demonstration Coaster**: 208 pieces forming one connected chain through its station and nothing else, ridden end to end by the parked Coaster Car on the ordinary `CoasterRide` path, never inverting outside the loop; a `build:` routine seam lets an exhibit own its whole parcel (`ExpoCoaster`, `ExpoBuilder.place_track`; T221 / T222). **Supply Depot** (card G) — generated from the content registry, never hand-listed: 58 visible items, eight units of each, one category per chest, 22 chests plus the reserved Future Food / Future Armour boards, each chest on a plinth under its own Header + Item Grid sign; an item nobody has classified is a validation **failure**, not a bucket (T223). The manifest's `deferred_items` is now **empty** — every visible id is on an exhibit — and the campus places **126 signs** across 13 districts and 89 exhibits. See [Development Expo](DEVELOPMENT_EXPO.md).
- **Integration calls made while merging** (documented, not silent): one `ExpoBuilder.place_exhibit` that tries the `build:` routine first and otherwise runs terrain + `placements` + entities, skipping the per-entity geometry for card E's composite parcels; one `_run_stock` op serving both card G's exact-count supply chests (topped back up on a rebuild) and card E's munition chests; one validator terrain-kind set, with card G's growth rule (catalog OR exhibit OR deferral) as the final one; and the gate reads the whole fixture with the simulation paused, handing it back before the three live scenarios (T219's reload, T220's drill, T222's ride) that need the services running. Card G's widened Supply Depot parcel (x −48, 64 wide) clears card D's extended Industry district and the Resources district, so nothing had to be moved.
- **Builder robustness the exported build forced** (`cc362a0`, `cd02b56`, `37bfa11`): three faults that only showed once the suite walked the whole campus. A voxel write that did not take was counted as done, so `_run_column` (which checked only a column's two end cells for streaming) and `_run_cell_batch` could leave holes in authored ground — both now treat it as unfinished work, and an op deferred more than `MAX_REQUEUES` times is reported as a builder failure instead of retrying for ever. A mounted fixture attempted before the op that places its mount had run recorded `INVALID_MOUNT` immediately; it is now retried like `UNLOADED`. And the mountain body, queued before its tunnel, could be deferred for streaming and taken up again *after* the carve, filling the tunnel back in — a solid run of stone from `(-97, 0, -32)` west in the exported build; a carve op now records the cells it empties on purpose and a column fill skips them.
- **Wave 1** (2026-09-22, cards A + B + C, merged earlier on this branch): lost-Core Continue safety, Development Start with its own save namespace and Reset Expo, the sign item / editor / persistence, and the manifest, layout and builder with the western campus. Its entry is unchanged below.

**EXPECT:** `START.cmd` → main menu → **Development** → **New**. You spawn on a clear stone plaza with the Core of Power and an orientation board naming every direction. North is the **Supply Depot**: 22 chests on plinths, each board a category with its item grid, eight of everything, nothing to gather first. West are **Resources** and the **Mining Mountain** — walk the lit tunnel end to end beside its rail line. North-west the **Industry** chain runs: Miner → Ore Bin → Cart → Warehouse → Foundry. South the roofed **Lighting** walk shows all six light sources at once. North-east the **Construction Yard** has every castle piece and a small castle built from them. East the **Defense Range** stands six weapons, each with its ammunition and a target down its lane. Far east the **Battlefield**: right-click the control pedestal, **START ATTACK**, and watch a mixed wave cross the open ground at that Core while the batteries fire — then **RESET BATTLEFIELD** and the arena is back with the rest of the campus untouched. South-east **CoasterCraft**: the component gallery, then board the Coaster Car and ride the Grand Coaster's lift hill, drop and true loop. East of the plaza one district is reserved and deliberately empty.

**TEST (`b788a8c`, 2026-09-23):** Static PASS — `python tools/validate_foundation.py` PASS (59 items / 52 recipes) and `python -m unittest discover -s tests` **79/79 OK**, both re-run at this head on 2026-09-23. Editor gates at this head: windowed `--development-expo-automation=gate` **DEVELOPMENT_EXPO_AUTOMATION_PASS** (T213–T223, evidence `artifacts/manual-development-expo-193836499/gate/`, 2026-09-23 16:32) and `--development-expo-automation=visual` **PASS** with the 11 rendered records and 11 screenshots in `artifacts/manual-development-expo-193836499/visual/`. Exported-build runners on the packaged build of this game tree (EXE `4ac12872…`, PCK `704ae6ee…`): `tools\runners\RUN_ALL.cmd` **15 / 15 PASS in 6 min** (2026-09-23 19:14, table and logs in `artifacts/run_all-20260923-191432`), including `TEST_DEVELOPMENT_EXPO.cmd` — **`DEVELOPMENT EXPO TEST: PASS`**. For contrast, the 2026-09-23 07:27 sweep before the two streaming fixes was 14 / 15 with `DEVELOPMENT EXPO TEST: FAIL` — the failure those fixes answer. The previous checkpoint's evidence for `37bfa11` follows.

**TEST — prior evidence for the wave-2 head (`37bfa11`, 2026-09-22):** Static PASS — `python tools/validate_foundation.py` PASS (59 items / 52 recipes), `python -m unittest discover -s tests` **79/79 OK**. Headless editor gates, each log kept under `artifacts/expo-wave2/gates/<name>/run.log`: `--development-expo-automation=gate` **DEVELOPMENT_EXPO_AUTOMATION_PASS** (T213, T214 plaza + mountain, T215, T216, T217, T218, T219, T220, T221, T222, T223 — 12 records, 0 failures), `--development-check` `ok=true` (T211; reset groups `battlefield`, `coaster` and `district:<id>` for all 13 districts), `--f3-automation=phase1` PASS (T210), `--f1-automation=phase1` PASS, `--coastercraft-check` `ok=true`, and `--p3c-player-defense-automation=phase1`, `--p3d-usability-automation=phase1` (T212), `--p3e-container-automation=gate`, `--p3f-presentation-automation=gate`, `--p3g-furnace-usability-automation=gate`, `--p3k-blueprint-automation=gate`, `--p4-siege-units-automation=gate`, `--p4-enemy-units-automation=gate`, `--p4-resources-automation=gate`, `--p4-assets-automation=gate`, `--p2-navigation-automation=phase1`, `--coaster-rails-automation=gate`, `--coaster-car-automation=gate` all PASS, plus `--coaster-sandbox --coaster-sandbox-facing-check` `reversals=0`. Windowed `--development-expo-automation=visual` **PASS**: 11 rendered records and 11 screenshots collected in `artifacts/expo-wave2/` — plaza, sign close-up, supply chest, tunnel, industry, lighting, construction, defense range, battlefield mid-attack, coaster gallery, Grand Coaster. **Windows export PASS** (`pwsh -NoProfile -Command "./tools/start_game.ps1 -PrepareOnly"`): EXE SHA-256 `4ac12872…`, **PCK SHA-256 `4a6990a1…`**. `tools\runners\RUN_ALL.cmd` on that export: **15 / 15 PASS in 5 min**, including `TEST_DEVELOPMENT_EXPO.cmd` (199 s) — table and logs in `artifacts/run_all-20260922-220251`. Owner playtest **NOT RUN**.

**LIMITATIONS/FAILURES:** The Expo is an exhibition, not a game mode with objectives — the handoff forbids adding gameplay here (§17). The coaster-gallery screenshot frames the specimens from far enough back that the booth signs are not readable in the image (the gate reads them from the station records, not the picture). `TEST_COASTER_CAR.cmd` has a known shutdown-race flake — the run can report leaked ObjectDB instances and resources still in use at exit; re-running it clears it. Riding the Grand Coaster's loop logs harmless `Basis::set_axis_angle` normalisation warnings from `coaster_cart_service.gd:223`. The windowed visual run takes roughly twenty minutes on this machine because it streams the whole campus; the headless gate takes about four. Everything the previous checkpoint listed — walk-through water, trolls shooting through walls, the enemy base being only a core on a clearing, no corkscrew / winding-snake / splitter track — still stands; this milestone changed none of it.

**NEXT:** The owner playtest passed and the sweep is green, so the branch is ready to push; `main` still waits on the owner's merge authorisation. Sign typography and the Battlefield sign's position are done (`feature/signs-v2`, 2026-09-24). The reserved district east of the plaza is the seam for the next system: one exhibit record in `contracts/development_expo.json` claims it.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\worktrees\development-expo`, branch **`feature/development-expo`**, last code commit **`b788a8c`** (the documentation commits of this checkpoint are the only ones above it) — **not pushed, not merged to `main`** (`main` is still `23aa7eb`). On top of the wave-2 work below: `fc3818e` (flight, T224), `8c21abf` (settle before a district is called built), `32dad5e` and `b788a8c` (far parcels wait for streaming). The packaged build at this head: engine `4.6.stable.custom_build.89cea1439`, EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`, PCK SHA-256 `704ae6eed3e5bbc9ab483091c858cf4e2d793b8c9624977e206281d9b14b851b` (`builds/CraftAndDefend/build_manifest.json`; export log `artifacts/windows_export.log`). Evidence at this head: `artifacts/manual-development-expo-193836499/` (gate + the 11 visual screenshots) and the 15 / 15 runner sweep in `artifacts/run_all-20260923-191432`. Reproduce: `pwsh -NoProfile -Command "./tools/start_game.ps1 -PrepareOnly"` then `tools\runners\RUN_ALL.cmd`. Wave-2 history, unchanged — merge commits in order: `fa2b62f` (`feature/expo-industry` `f8b3a14`, card D), `fc21f51` (`feature/expo-battlefield` `99b5ac3`, card E), `3cd08b4` (`feature/expo-coaster` `e29190b`, card F), `335e756` (`feature/expo-supply` `bf9aa60`, card G); then `b464265` (what the merge broke: T216's world-wide rail count, T214's tunnel walk, card F's coaster shots folded into card E's `_shoot` helper), `cc362a0`, `e50bd4f`, `cd02b56` and `37bfa11` (the builder robustness above); that head's packaged build was PCK SHA-256 `4a6990a1ee51d47beceb9a05cb789b0f650f9fec05a451397a5348c3ad5ea39d`, with its gate logs and eleven screenshots in `artifacts/expo-wave2/` and its sweep in `artifacts/run_all-20260922-220251`. The pinned engine lives at `D:\CODEX\_tools\GodotVoxel\4.6-1.6`.

---

## Previous checkpoint — 2026-09-19 (round 2: big world, enemy base, Core of Power, lights, pathing)

**STATUS:** **ROUND-2 BUILD + ROUND-3/4/5/6 FIXES + COASTER RAILS + COASTER CAR / HERO + COASTERCRAFT SMOOTH TRACKS MERGED, HUD CORNER MINIMAP — CANDIDATE PASS ON THE EXPORT (PCK `e275e1ce…`, branch `feature/p3d-tools-world-feedback`, pushed); OWNER RE-PLAYTEST PENDING; NOT MERGED TO `main`.**

**Launchers (2026-09-22, `feature/launchers`):** one `START.cmd` at the repo root replaced `START_GAME` / `START_COASTER_SANDBOX` / `START_COASTERCRAFT` / `START_F0` / `STOP_GAME` / `BUILD_WINDOWS` / `BUILD_WINDOWS_F0` / `VIEW_NAVIGATION_SPIKE` (`START.cmd help`: `sandbox`, `coastercraft`, `build`, `stop`), and the `TEST_*.cmd` runners moved to `tools\runners\` with a repo-owned `RUN_ALL.cmd`. Older entries below still use the old names.

**DONE (newest first):**
- **Development Expo — wave 1** (2026-09-22, `feature/development-expo`, cards A + B + C merged; NOT exported, NOT playtested, NOT merged to `main`): **lost-Core Continue safety** — a destroyed Core of Power is Game Over, never a broken save: `WorkstationService.restore` drops a single unreadable station record (reported as `skipped` / `STATION_RESTORE_SKIPPED`) instead of refusing the whole snapshot, a saved station whose ground was dug or blasted away is restored where it stood (`EntityFootprintService.force_reserve`), integrity outside the sheet is clamped, a broken barricade drill is cleared like a broken core drill, and a snapshot that truly cannot be read leaves the loading screen with Back to Main Menu (`GameSession.load_failed`); the main menu reads the checkpoint (`GameSession.game_over_report`) and offers **Game Over — Start New** with Continue disabled, never re-creating a core. **Development Start** (`DevelopmentMode`): main-menu submenu (Continue / New / Back), `START.cmd dev` / `--development`, its own `<data root>/development` coordinator (Slot A, Slot B and CoasterCraft untouched), its own pause menu with **Reset Expo** and its confirmation, no ambient enemy pressure, hidden drill line and enemy-base compass, a fresh world at 09:00 with the day/night cycle running, no creative top-up, and `EXPO_FIXTURE_VERSION`. **Signs**: a real `sign` item and Workbench recipe (2 Planks + 1 Stick), ground (post) or wall (no post), the board rendered with in-world text and item icons; a right-click SIGN editor with the four display modes (Single Text, Split Text, Item Grid 4 x 2, Header + Item Grid), an icon-first item picker (category strip, no dropdown) and eight item slots; content is stable ids and text on the station record, saved and migrated by `WorkstationService`, with `GameSession.configure_sign` / `sign_data` as the API. **Expo manifest, layout and builder**: `contracts/development_expo.json` (+ `game/data` mirror, validated by `tools/validate_foundation.py`) describes districts and exhibits — kind, footprint, clearance, orientation, referenced ids, terrain, connections, sign, reset group, expansion priority — and a `deferred_items` rule that fails validation when new content is neither exhibited nor deferred to a named card; `ExpoLayout` packs parcels deterministically, keeps expansion corridors and avenues and computes the canonical world bounds from the layout plus its margin (`(-160, -16, -96)` size `(384, 48, 320)`, chunk aligned; `WorldAdapter.initialize` takes a `bounds_override`, a normal game still reads `world.json`); `ExpoBuilder` applies authored voxels in budgeted per-frame ops, places fixtures through `WorkstationService.try_place(..., {"_free": true})` and **places and writes the manifest's real signs** through `GameSession.configure_sign` (45 of them; a sign whose own corner is taken or buried takes the nearest free standing cell, and a board's text shrinks to fit its panel). Built on Development New: plaza with the Core and its avenues, the Day One chain, the Equipment booths with the empty signed armour parcel, the Resources district and the Mining Mountain (72x72 voxel mass, authored deep ore core, a 9x7 tunnel lit end to end with a 64-cell rail line, manual and automated mining chambers). T210 (`--f3-automation=phase1`), T211 (`--development-check`), T212 (`--p3d-usability-automation=phase1|visual`), T213/T214/T215 (`--development-expo-automation=gate|visual`). See [Development Expo](DEVELOPMENT_EXPO.md) and [Signs](SIGNS.md).
- **Development Expo — wave 2, card G (Supply Depot)** (2026-09-22, `feature/expo-supply`, NOT merged): the Supply Depot is **generated from the content registry**, never hand-listed — `SupplyDepot` (`game/scripts/expo/supply_depot.gd`) classifies every non-hidden item through `ItemCategories` and returns chests of **eight units of each type**, one category per chest, at most eight distinct types and at most eight of the chest's nine slots (so eight picks, which do not stack, fill a chest alone and the ninth slot always stays empty), in category order and then registry order; `ExpoBuilder` places each chest on a stand — a two-cell plinth carrying the chest's own **Header + Item Grid** board above it, facing the plaza — and stocks it through the ordinary container service, idempotently. 22 chests plus the reserved **Future Food** / **Future Armour** boards (no fake items); the Supply Depot district widened to 64 x 14 to hold them with room for 28 stands. An item nobody has classified is a **failure, not a bucket**: `ItemCategories.classify` is explicit-only, and `tools/validate_foundation.py` reads that one runtime map (rather than copying it) and names the item and the file to fix, alongside the depot's own rules (each item exactly once, count 8, the type and slot budgets, the parcel's capacity) and `SupplyDepotTests`. **T223** / **T223V** in `--development-expo-automation=gate|visual`, and the milestone's exported-game runner `tools\runners\TEST_DEVELOPMENT_EXPO.cmd`, registered in the `RUN_ALL` sweep (now 15 runners). Windows evidence: exported PCK `b0729844…` (EXE `4ac12872…`), `TEST_DEVELOPMENT_EXPO.cmd` PASS and `RUN_ALL.cmd` **15 / 15 PASS** (`artifacts/run_all-20260922-192937`). See [Development Expo](DEVELOPMENT_EXPO.md).
- **Live world under menus + storage network** (2026-09-22, `feature/live-modals` `fce84eb`, `feature/storage-network` `c418bc9` merged `e6ddd4f`): only the pause menu / saving pause the world — inventory, hand-build and every station panel leave furnaces, miners, foundry, carts, raiders and the clock running and refresh live (T199); the foundry has ore / fuel / output slots (64 each) auto-fed from adjacent storage, a progress bar and time, ingots pushed back (T200); `StorageNetwork` — containers touching each other pool (chests beside a warehouse, daisy-chained warehouses) and any machine touching a network uses it: siege weapons reload from an adjacent chest, miners output to adjacent storage without a bin, furnaces pull fuel/ore, carts unload into any adjacent storage (T201, T202). See [Industry](INDUSTRY.md).
- **Industry wave 1 + CoasterCraft mode + raiders unstuck merged** (2026-09-21, six card branches merged into `feature/p3d-tools-world-feedback` as `35800a4`, `9aed46f`, `dbf76b5`, `af18480`, `b56649f`, `ef8fd3c`): **CoasterCraft mode** (main menu CoasterCraft → New / Continue, a bare 60 × 100 plate on its own coordinator, own pause menu, no monsters; `START_COASTERCRAFT.cmd`, `--coastercraft-check`; T191); **CoasterCraft Shop** (the coaster recipes moved out of the Workbench into the shop's own book, the shop itself is Workbench order 213; T192); **Miner + Ore Bin** (drills ore within radius 3 into an adjacent bin, right-click for status; orders 214 / 215; T193); **Warehouse + Foundry** (27-slot store, the foundry beside it smelts the ingot you pick from the Warehouse's ore + Coal; orders 216 / 217; T194); **Hauling** (mine carts load from ore bins and unload into warehouses as they pass, cargo saved and shown on the cart; T195); **Raiders unstuck** (progress watchdog, step-ups, shuffle, stall recovery; T196–T198, `docs/RAIDER_PATHING.md`). Integration calls: one `container_room` / `container_put` in `WorkstationService` (`container_insert` is an alias, both emit `station_changed` so bin heaps / warehouse crates refresh); the hauling card's placeholder `ore_bin` / `warehouse` entities dropped; one validator container rule (`CONTAINER_STATION_TYPES`); the Workbench book is now 4 pages, so T73 / T78 / T103 derive the page count from the live book instead of a literal; `docs/INDUSTRY.md` is one file (Mining / Warehouse and foundry / Hauling). Not exported yet; owner playtest NOT RUN.
- **Owner round 2026-09-21 (2)**: vertical trusses only (no longitudinal stringers); curves 90 / 180 only; the held slot's key again empties the hands; **Y** drops an item (Shift+Y the stack), walk over it to pick up, saved with the game. T190.
- **Truss holes over other track** (2026-09-21): a piece over (or beside) another track gets no bent; the bents either side bridge the hole with their stringers. T189; `coaster-climb.png` crosses a rail line.
- **Smooth track, snap, auto-shape** (2026-09-21, `feature/coaster-sweep`, `feature/coaster-snap`, `feature/coaster-autoshape` merged; `feature/coaster-smooth-rail-switch` merged for real — the 2026-09-20 merge commit of that name had taken the trusses branch instead, so Rail Bend was still in the game until now): banking scales with curvature and eases in/out (no jump at a bend's start or an S-bend's inflection), rails and spine are one swept mesh per run (no facets, continuous twist), T186; ghosts snap to an open track end with a yellow highlight, T187; plain rails auto-shape into a 2×2 radius-1.5 elbow or a six-cell lane shift (undo restores them), T188; Rail Switch = the smooth switcher, Rail Bend gone.
- **One track style** (2026-09-21, `feature/coaster-track-style` merged): plain rails, Rail Slope and the loop's ground pieces draw with the curve pieces' renderer (two rails, oak ties, stone spine); plain-rail corners are quarter arcs; slopes run edge to edge on the 45° line with a short bent; every joint measured flush (T185). See [Coaster rails](COASTER_RAILS.md).
- **Owner playtest round (2026-09-20, coaster sandbox)** (`ceaefa5`…): spawn fell through the plate (the mountain demo sat on the spawn cell; moved to the plate's far end, spawn-clear print); **the heading follows the aim** — every coaster tool's press heads the way the player faces (the aimed cell is the entry), Shift-drag re-aims climbs / loops and flips a bend dragged behind its entry (T182); the sandbox's Rail Curve defaulted to a U-turn (reset after the demo); creative removal with a full pack discards instead of refusing; two-column pause menu; **Rail Switch is now the smooth lane switcher** (Rail Bend folded into it, `feature/coaster-smooth-rail-switch`); **real trestle trusses** (two legs, sway X-bracing, stringers between bents, `feature/coaster-trusses`, T183); **Undo** — U / Ctrl+Z takes back the newest placement, twelve deep, with refunds and auto-cleared terrain restored (T184). See [Coaster rails](COASTER_RAILS.md) (bottom sections) and [Keybinds](KEYBINDS.md).
- **CoasterCraft smooth tracks** (2026-09-20, `5346b72` core + four agent branches merged `be6a40b`…`98a3263`): `TrackCurve` engine; the **true loop** (helix touching the ground at its entry and exit one lane over; Shift-drag sizes it up to the sky; pack-priced per piece, creative sandbox free); **Rail Bend** (smooth lane switcher, ±lanes, Shift-drag), **Rail Cross** (X crossing with shared two-track cells), **Rail Curve** (45/90/135/180, radius by Shift-drag, banked, left mirror), **Rail Climb** (slope-in → any grade → slope-out, Shift-aim at a hillside), **Track auto-clear** setting (pause menu; dug blocks drop to the pack; water/bedrock/castle stone never), automatic **trestle supports**. T170–T181. Sandbox plate shows one of each. See [CoasterCraft tracks](COASTERCRAFT_TRACKS.md).
- **HUD** (`ab50397`): minimap in the top-right corner, re-laid on resize; HUD text wraps in a left column beside it. Coaster sandbox (`START_COASTER_SANDBOX.cmd`, `--coaster-sandbox`) merged from `feature/coaster-rails`.
- **Coaster car and hero** (branch `feature/coaster-car-and-hero` `a230867`, merged fast-forward): a rideable `coaster_car` (Shift boards, 1–9 speed, Shift / Escape leaves, chase camera), the hero character (`HeroModel`, armour toggle in the pause menu) seated in the car and shown in third person with **V**; T164–T167, `TEST_COASTER_CAR.cmd`. See [Coaster car and hero](COASTER_CAR_AND_HERO.md) and [its evidence](evidence/COASTER_CAR_AND_HERO.md). Not yet merged.
- **Coaster rails merged** (`0e39dcb` merge of `feature/coaster-rails` `7f2cdd9`; `68d73a0`): Rail Slope, Rail Loop drag tool (Shift + X / C) and Mine Cart are in the integration branch; Workbench book is four pages. The real-click visual helpers (T115 / T123) now push motion + press + release in one frame — the pointer leaving the window between press and release used to cancel the button, which is why those tests flaked.
- **Round-6 fixes** (`5ce8c5a`, `8d0c301`): wall lanterns hang on walls (torches on ground or wall); rails re-shape into corners / T / crossroads and kettles ride corners but keep straight at junctions; stuck raiders back up and sidestep before re-planning; recipes that overflowed the 3×3 grid (mk2, cannon, core) fixed with a validator rule. Side project **coaster rails** was built in worktree `worktrees\coaster-rails` (branch `feature/coaster-rails`) and merged (see above). T155.
- **Coaster rails side project** (branch `feature/coaster-rails`, on top of `5ce8c5a`, merged as `0e39dcb`): 45° `rail_slope`, the `rail_loop` drag tool (Shift loop, X / C radius) and a `mine_cart` that rides slopes and loops; kettles keep their flat-rail behaviour and also climb slopes; T160–T163, `TEST_COASTER_RAILS.cmd`. See [Coaster rails](COASTER_RAILS.md) and [its evidence](evidence/COASTER_RAILS.md); the Workbench book is four pages there.
- **Load fix + STOP_GAME.cmd** (`15fcf3f`): a save whose defended core was destroyed loads (drill restored as lost) instead of hanging on `CORE_STATION_MISSING`; any drill restore failure clears the drill; `STOP_GAME.cmd` (repo root and portable build) closes a stuck game.
- **Round-5 fixes** (`c187eba`): the view-distance setting now truly streams terrain (a `VoxelViewer` on the camera was missing); rails/slabs/merlons auto-align and lay in a line by right-drag; the kettle's menu opens where it rides and it faces across the rail; vertical drags stay vertical to release; raiders breach player stone when no other way exists (brutes fast), no longer stack in one cell, and spawn in kind rows; clicking a recipe stages it with red placeholders for missing ingredients.
- **Round-4 fixes** (`2a346d7`): the invisible fence is gone (clamp follows world.json); a topological **minimap** top-right with **M** for the full world map; the placement aim stops at entities so the kettle lands on a rail; **Patrol** stance for rail weapons; mountains keep off the home area and a corridor to the enemy base so waves always have a way in. T154.
- **Round-3 fixes** (`39c5fe3`): sword hits land (swing ray on the raider layer, wider reach cone, raiders targetable after a lost drill); **aggro** — a raider hurt by you or a machine chases that attacker for 8 s and hits back (player health 100 with HUD `HP`, regen, respawn at the core); Settings → Graphics → **Terrain view distance** 64–320 (default 128); raiders path around water. T153.
- **Core of Power wired to the drill** (`9fa59f1`): the drill defends the placed blue core (240 integrity, hits land on the station, destruction = loss); new games start with one in the pack; the red enemy core is placed at the enemy base when you reach it; compass shows the enemy base bearing. T152.
- **P4G Core of Power + light sources** (merged `2fce93f`, subagent): entities with full attribute sheets (value, role, footprint, mount, cost, integrity, light) — Core of Power, enemy core, torch, wall lantern, post lantern, campfire, blue/red light blocks; icons from the owner's renders; lights never go out (torch/campfire flicker). T148–T150, `TEST_P4_ASSETS.cmd`. See [P4G](P4G_CORE_AND_LIGHTS.md).
- **Far attacks** (`P4E`, commits `1b25e8c`…): waves march from the enemy base 150–200 cells away over a surface route, ghost-walking unloaded ground, and hand over to the local planner near the core; pause menu **Start Attack from the Enemy Base (far)** and **Start Drill (NEAR)**. T151.
- **The big world** (`1b25e8c`): 320×48×384, random seed per new game, mountains, lakes (walk-through water), richer ore and ore on the ground, enemy base clearing. See [P4E](P4E_WORLD_AND_ENEMY_BASE.md).
- **Pathing** (`508f066`): raiders never shove each other (layer 4), re-plan when stuck (1.6 s watchdog), random wave spread.

**EXPECT:** New game → a fresh world each time, lakes and distant mountains, coal/iron visible on the ground, Core of Power in slot 1. Place it on a slab; pause → **Start Attack from the Enemy Base (far)**: the HUD counts the wave marching in ("lead 140 m out"); build while they come. Raiders no longer huddle or freeze; a stuck raider re-routes within two seconds. Torches/lanterns/campfires light the night and never burn out.

**TEST (wave 1 integration, editor, 2026-09-21):** Static PASS (validator 58 items / 51 recipes; 60/60 unit tests). Headless gates PASS on the merged head: `--coaster-rails-automation=gate` (incl. T191), `--coaster-car-automation=gate` (T195), `--p3f-presentation-automation=gate` (T192), `--p4-resources-automation=gate` (T193), `--p3e-container-automation=gate` (T194), `--p4-siege-units-automation=gate` (T196–T198), `--p2-navigation-automation=phase1`, `--p3c-player-defense-automation=phase1`, `--p3d-usability-automation=phase1`, `--f3-automation=phase1`, `--coastercraft-check` (`ok=true`), `--coaster-sandbox --coaster-sandbox-facing-check` (all demos, `reversals=0`); windowed `--coaster-rails-automation=visual` and `--p3f-presentation-automation=visual` PASS. No export of this head yet (the previous export line follows). Owner playtest NOT RUN.
**TEST:** Static PASS (validator 54 items / 47 recipes; 60/60). Exported PCK `573687f8…` then `21b63264…` (EXE `4ac12872…`): all fourteen runners PASS — `TEST_COASTER_CAR` (T164–T167), `TEST_COASTER_RAILS` (T160–T163, T168, T170–T181; T173's Shift-aim checks failed once on the `573687f8…` sweep before the region's collision meshes had settled — the automation now waits 45 physics frames after the region loads, and the runner passed twice on rerun / on `21b63264…`), `TEST_P4_SIEGE_UNITS`, `TEST_P4_ASSETS`, `TEST_P4_ENEMY_UNITS`, `TEST_P4_WEAPON_PANEL`, `TEST_P4_RESOURCES`, `TEST_P3C`, `TEST_P3G`, `TEST_P3D`, `TEST_P3F`, `TEST_P3K`, `TEST_P3E`, `TEST_P3H` (sweep log in scratch `runall.log`, evidence in the newest `artifacts/manual-*` per runner). Owner playtest NOT RUN.

**LIMITATIONS/FAILURES:** Water is walk-through (no swimming). Trolls shoot through walls. The enemy base is just the red core on a clearing (no camp, no patrols); attacking it is not yet a win condition. The march ignores player structures until it is 20 cells from the core. No corkscrew, winding-snake or splitter track yet (cards 7, 9, 10 in COASTERCRAFT_TRACKS.md). Kettles treat a Rail Cross as the first track laid; diagonal curve ends (45° / 135°) cannot join plain rails yet. The sandbox facing-check sampler occasionally reports `reversals=1` (sampler, not the cart). Diagnostic fixtures use the fixed seed 41026 and an empty starting pack.

**NEXT:** Owner re-playtest (checklist in the session report) and tuning; then swimming, line of sight, enemy camp props/patrols, win by destroying the enemy core, side-mounted lights, traps (owner's next asset batch).

**GIT/REPRODUCIBILITY:** Runners now live in `tools\runners\` (`RUN_ALL.cmd` = the old scratch `runall.ps1`); launcher `START.cmd`. Worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`, pushed; `feature/p4-core-and-lights` (`e8ffeb5`) and `feature/coaster-rails` (`fe1be67`…, pushed) and `feature/coaster-car-and-hero` (`a230867`) merged; the CoasterCraft agent branches `feature/coaster-smooth-switcher` (`6c52847`), `feature/coaster-curves` (`44e719e`), `feature/coaster-climbs` (`673aa0d`), `feature/coaster-autoclear-supports` (`c619911`) merged (`be6a40b`…`98a3263`) and pushed. Engine `4.6.stable.custom_build.89cea1439`; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `e275e1ceb78804a524e59da5e43eb6cba5a52c5b84958c17135acd71cbc97647` (wave 1: `35800a4`…`cfcb785` merged and pushed; export runners TEST_COASTER_RAILS / TEST_COASTER_CAR / TEST_P4_RESOURCES PASS on it) (sweep on `573687f831563d3d9a6119d7a44cce3ece13ce6526c661113c2fc87b0433f3a1`, which differs only by the T173 settle wait). Evidence roots: the newest `artifacts/manual-*` folder per runner from the 2026-09-20 sweep (scratch `runall.log`).

---

## Previous checkpoint — 2026-09-19 (owner playtest round 1 fixes + owner-art machines + enemy units)

**STATUS:** **PLAYTEST ROUND-1 CORRECTIONS, OWNER-ART MACHINES, ORC/TROLL UNITS — CANDIDATE PASS ON THE EXPORT (PCK `fd2b7b0f…`, branch `feature/p3d-tools-world-feedback`, pushed); OWNER RE-PLAYTEST PENDING; NOT MERGED TO `main`.**

**DONE (newest first):**
- **Coaster car and hero** (branch `feature/coaster-car-and-hero`): a rideable `coaster_car` (Shift boards, 1–9 speed, Shift / Escape leaves, chase camera), the hero character (`HeroModel`, armour toggle in the pause menu) seated in the car and shown in third person with **V**; T164–T167, `TEST_COASTER_CAR.cmd`. See [Coaster car and hero](COASTER_CAR_AND_HERO.md) and [its evidence](evidence/COASTER_CAR_AND_HERO.md). Not yet merged.
- **P4F enemy units** (merged `8271f25`, subagent): orc melee with two cleavers replaces the capsule raider; brute = purple orc ×1.25; new **troll** ranged kind (28 hp, bolt 5 every 2 s from 9 cells, stops at range); Wave Drill 6+1 brute+1 troll, Siege Drill 12+3+3; weapon target filter gains `troll`; T144–T146, `TEST_P4_ENEMY_UNITS.cmd`. See [P4F](P4F_ENEMY_UNITS.md).
- **Hybrid rule** (`a87a332`): brutes turn on player defenses within 2.6 cells (T147); plain raiders rush the core.
- **Owner-art machines** (`716d410`): cannon (now 2×2), rail block and kettle restyled after the owner's renders on a shared studded platform; new `turret_catapult_mk2` (owner-art version, the pedestal one stays); card icons of cannon/kettle/rail/mk2 are the owner's renders; wood-axe card icon mirrored to face like the picks.
- **Cursor-stack loading** (`c6427cd`): a stack picked up on the cursor loads into a weapon (Ammunition slot) or a chest tile instead of "Place the held stack first"; right-click one, click all.
- **The enemy never gives up** (`6ba89ed`): raiders with no permitted route probe every 2 s (HUD "RAIDERS PROBING FOR A WAY IN") instead of freezing until a station changed; siege machines, chests, rails and kettles are breachable structures with integrity (blocked raiders smash them); defeated raiders topple and vanish after 6 s; restored raiders stand on the ground (no half-buried bodies); waves spawn only once the terrain under the spawn line is loaded; T143.
- Runner fix: `TEST_P4_SIEGE_UNITS.cmd` restore step path.

**EXPECT:** Wave Drill: orcs lead, a purple brute smashes any machine/chest it passes, a troll stops ~9 cells out and shoots bolts; machines turn and fire at their nearest raider; dead raiders fall over. Walled-in raiders keep probing (HUD says so) and break through a chest/machine plugging the gap. Workbench page 2–3: Cannon, Rail, Kettle and Turret Catapult Mk2 show the owner's renders; placed, they match them (platform with iron corners, gold studs, blue banner). Click a munition tile, then click the Ammunition slot: it loads.

**TEST:** Static PASS (182 links; 60/60). Exported PCK `fd2b7b0f…` (EXE `4ac12872…`): `TEST_P4_SIEGE_UNITS` (T130–T136, T141–T143, T147), `TEST_P4_ENEMY_UNITS` (T144–T146), `TEST_P4_WEAPON_PANEL`, `TEST_P4_RESOURCES`, `TEST_P3C`, `TEST_P3D`, `TEST_P3F`, `TEST_P3K`, `TEST_P3E`, `TEST_P3H` PASS; `TEST_P3G` PASS on its second run (T115 real-click test failed once — flaky window focus, see limitations). Editor gates for P3B (phase1/save/restore), P3, P4 weapon panel PASS after the merge.

**LIMITATIONS/FAILURES:** T115 (P3G real-click furnace test) is flaky when the desktop steals focus during the windowed run; rerun passes. Trolls shoot through walls (no line of sight) and only target the core or the structure they were sent to breach. Unit and machine proportions are box-part approximations of the owner's renders — expect notes. Raiders can still step onto 1-cell-high entities (chests, rails) when the cell above is free.

**NEXT:** Owner re-playtest (checklist in the session report); then line of sight for trolls, raiders vs walls/gates by siege kind, P3K slice 2, province/market.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`, pushed; `feature/p4-enemy-units` (`891529f`) and `feature/p4-resources` merged in. Engine `4.6.stable.custom_build.89cea1439`; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `fd2b7b0f0ed3e6e4fbab67b142c0e563afea13a46c9f947fcd8871fd918ca475`. Evidence roots `artifacts/manual-p4-siege-units-1210924237`, `manual-p4-enemy-units-12152126`, `manual-p4-weapon-panel-121749829`, `manual-p4-resources-121948784`, `manual-p3c-player-defense-122048261`, `manual-p3d-usability-122635124`, `manual-p3f-presentation-1228514827`, `manual-p3k-blueprints-123122510`, `manual-p3e-furnace-1233412213`, `manual-p3h-balance-1235411168`.

---

## Previous checkpoint — 2026-09-19 (P4F enemy units on `feature/p4-enemy-units`, since merged)

**STATUS:** **P4F — ENEMY UNITS (ORC MELEE, BRUTE, TROLL RANGED) — CANDIDATE PASS IN THE EDITOR; NOT EXPORTED, NOT PLAYTESTED, NOT MERGED.** Bounded card built in worktree `worktrees\p4-enemy-units` on top of `6ba89ed` (`feature/p3d-tools-world-feedback`).

**DONE:** `BasicRaider` rebuilt as box-part models from the owner's two reference images (orc with two cleavers and a red pauldron; brute = purple orc ×1.25; troll with crossbow, quiver, topknot) with a distance-driven walk bob, melee chop and crossbow kick; new `troll` kind (28 hp, 5 damage, speed 2.4) that stops within 9 cells of its route target and shoots a bolt every 2 s (core or breach structure); `"trolls"` wave option, drills carry 1/3 trolls; `troll` weapon target filter; balance keys `troll_*`; `P4EnemyUnitsAutomation` T144–T146; `TEST_P4_ENEMY_UNITS.cmd`. See [P4F](P4F_ENEMY_UNITS.md) and [evidence](evidence/P4F_ENEMY_UNITS.md).

**EXPECT:** Pause → Start Wave Drill: an orc leads, then a purple brute, a blue-grey troll and three orcs from 22 cells; the troll halts about nine cells short of the core (or of the barricade it would breach) and fires bolts every two seconds; orcs chop at what they reach.

**TEST:** Static PASS (179 links; 60/60). Editor: `--p4-enemy-units-automation=gate` (T144, T145) and windowed `=visual` (T146, `p4-enemy-units.png`) PASS; regression `--p4-siege-units-automation=gate/save/restore`, `--p3c-player-defense-automation=phase1`, `--p3b-core-defense-automation=phase1` PASS. Export and `TEST_P4_ENEMY_UNITS.cmd` NOT RUN. Owner playtest NOT RUN.

**LIMITATIONS/FAILURES:** No line of sight for trolls; the bolt is presentation (damage lands with the timer); trolls do not target machines or the player; model proportions await owner notes.

**NEXT:** Owner playtest of the wave drill with trolls; export; merge decision together with the P4 branch below.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\worktrees\p4-enemy-units`, branch `feature/p4-enemy-units`, not pushed. Engine `4.6.stable.custom_build.89cea1439`, Voxel Tools 1.6.0 Module.

---

## Previous checkpoint — 2026-09-19 (autonomous P4 build, awaiting owner playtest)

**STATUS:** **P4a-2..4, P4B, P4C/P4D and P4E — CANDIDATE PASS ON THE EXPORT (PCK `19bab7cb…`, branch `feature/p3d-tools-world-feedback`); OWNER PLAYTEST PENDING; NOT MERGED TO `main`.** Built while the owner was away on the standing instruction "program as much game logic as you can … don't stop working". `main` still holds the authorised baseline (`0be5f75`); merging this branch needs the owner's approval.

**DONE (this checkpoint, newest first):**
- **P4D follow-up** (`8ea2302`, `d546a0b`): pause-menu **Start Siege Drill** (12 raiders, 3 brutes, 28 cells); heavy impacts scorch grass to dirt; far spawn lines sit on the terrain surface (ground under canopies) and the drill waits for streaming terrain instead of failing; T141 wave persistence, T142 far spawn on natural ground.
- **P4E world expansion** (`1dcceb3`): 128×32×192 cells (was 64×32×128); the seeded generator keeps every existing cell.
- **P4B resource distribution + gold** (merged `d4c0d35`, subagent): data-driven ore table (`world.json → terrain.ores`), `gold_ore` block 11 (iron pick), gold ingot furnace chain, gold icons; T137–T140, `TEST_P4_RESOURCES.cmd`. See [P4B](P4B_RESOURCE_DISTRIBUTION.md).
- **P4C siege units + P4D wave drills** (`98352d9`): remodelled Ballista (slider draw/release, strings), Turret Catapult (tower socket), Cannon (cannonball, recoil, muzzle flash), Rail + Kettle (rides rails to the raider, dumps burning hot oil); per-weapon nearest-raider targeting with filters; wave drills with brutes and far spawns (pause menu **Start Wave Drill**); T130–T136, `TEST_P4_SIEGE_UNITS.cmd`. See [P4C](P4C_SIEGE_UNITS_AND_WAVES.md).
- **P4a-2 weapon and chest panels** (`547ce2c`, subagent): right-click a weapon → panel (load/unload, stance, target, supply readout, munition legend); right-click a Chest → 3×3 grid; T121–T123, `TEST_P4_WEAPON_PANEL.cmd`. See [P4a-2](P4A2_WEAPON_AND_CHEST_PANELS.md).
- **P4a-2..4 service logic** (`d63b55c`, `1804a84`): munitions table, weapon load/unload/stance/filter, Chest container, supply radius + auto-reload, impacts resolved at landing with splash, Flame Shot and `FireService` (burning, spreading, consuming wood); T119, T120.
- Tooling: `tools/format_content.py` (record-per-line content mirrors). Stale expectations repaired: T66, T73, T78, T103 (three-page book), F0 T08 (rebind to T), F1 T14 (guard lifted inside the test).

**EXPECT:** From the Workbench: Cannonball, Turret Catapult, Cannon, Rail, Kettle recipes (page 2–3); Hot Oil from logs at the Furnace. Place a Cannon on flat ground, a Turret Catapult on a Tower Platform, Rails along a wall top, a Kettle on a rail. Right-click any weapon for its panel; put munitions in a Chest within 8 blocks and empty weapons refill themselves. Pause → **Start Wave Drill**: six raiders (one purple brute) from 22 cells out; machines turn to their nearest raider; the kettle slides to whoever reaches the wall and pours burning oil. Gold ore appears from depth 12 (iron pick). The world is twice as wide and half again as long.

**TEST:** Static PASS (171 links; 60/60 unit tests). Exported PCK `19bab7cb…` (EXE `4ac12872…`): `TEST_P4_SIEGE_UNITS` (T130–T136, T141, T142), `TEST_P3C` (T72–T78, T118–T120), `TEST_P4_WEAPON_PANEL` (T121–T123), `TEST_P4_RESOURCES` (T137–T140), `TEST_P3G` PASS; on the earlier PCKs `3d1d2cb9…` / `81214a8f…` of the same day (only diagnostics and the defense service changed since): `TEST_P3F`, `TEST_P3E`, `TEST_P3D`, `TEST_P3K`, `TEST_P3H` PASS. Editor gates: F0, F1, F2, F3, P1, P2, P3, P3B (phase1/save/restore/visual), P3C (save/restore), castle kit PASS. Owner playtest NOT RUN.

**LIMITATIONS/FAILURES:** Raiders ignore rails, kettles and machines (they only breach barricades and hit the core). Brutes share the basic step capability. Cannon damage to castle stone is not modelled. The wave button's numbers are a first guess. The four unit models follow the reference images from memory of the earlier review; expect proportion notes from the owner. Held items are still 2D billboards; blueprints still have no player entry point (P3K slice 2). Old saves load into the larger world (their unedited outer area generates fresh).

**NEXT:** Owner playtests the four machines, the panels and the wave drill (checklist in the session report); then decide the merge to `main`. Then P3K slice 2, raiders vs machines/rails, cannon vs stone, province/market.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`, pushed. `feature/p4-resources` (`d6ae85f`) merged in and pushed. Engine `4.6.stable.custom_build.89cea1439`, Voxel Tools 1.6.0 Module; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `19bab7cb8fd5e3e166103fc9431e08759cbd66b9eeb00c3f517f23f4bf474109`. Evidence roots `artifacts/manual-p4-siege-units-106913565`, `manual-p3c-player-defense-1105726`, `manual-p4-weapon-panel-114120654`, `manual-p4-resources-11588860`, `manual-p3g-furnace-11678338`, and earlier the same day `manual-p3f-presentation-3237825924`, `manual-p3e-furnace-3239724878`, `manual-p3d-usability-322211520`, `manual-p3k-blueprints-3226032197`, `manual-p3h-balance-3228031152`, `manual-p3f-presentation-3237825924`, `manual-p3e-furnace-3239724878`.

---

## Previous checkpoint — 2026-09-19 (baseline merged to `main`)

**STATUS:** **BASELINE. Owner authorised merging `feature/p3d-tools-world-feedback` to `main` on 2026-09-19.** Everything from F2 through P4a-1 (see the checkpoints below) is on `main` and pushed. The core-defense drill works as a baseline and needs more work later (owner statement); it is not being polished further before the next milestones. Claude is the implementing agent; a new developer can start from `main` with the README's reading order.

**DONE (since the previous checkpoint):** catapult modelled from the owner reference with a 2×4 footprint (`5bb8ac9`); siege motion — turntable facing, throw, wind-back, loaded stone, bucket muzzle (`ca7599f`, T118); diagnostics ignore focus-loss pauses (`098ceb1`); README and handoff refreshed for new developers.

**EXPECT:** `main\START_GAME.cmd` rebuilds the export once (provenance) and opens the playable slice described in the README.

**TEST:** Exported at `ca7599f` (PCK `903ddc26…`): `TEST_P3C` (T72–T78, T118) and `TEST_P3G` (T93–T98, T107, T114, T115) PASS; earlier the same day `TEST_P3D`, `TEST_P3F`, `TEST_P3K`, `TEST_P3E`, `TEST_P3H` PASS on their respective builds. Static PASS. Owner playtested P3H.4–P3J and the catapult; core-defense drill confirmed working as a baseline.

**LIMITATIONS/FAILURES:** Held items are still 2D billboards (3D proposal in the design direction §10). Blueprints have no player entry point yet (P3K slice 2). Siege supply, weapon panel and fire are cards (P4a-2..4). Ballista has no draw/release animation. The 2-cell ammo trough is not yet part of the siege footprint.

**NEXT:** P3K slice 2 (Blueprints in the B menu) or P4a-2 weapon panel — owner's choice; then P4a-3 supply/auto-reload, P4a-4 flame shot and fire, 3D held items.

**GIT/REPRODUCIBILITY:** `main` = merge of `feature/p3d-tools-world-feedback` (`ca7599f` + doc refresh), pushed to `origin/main`. Superseded worktrees removed (branches kept). Engine `4.6.stable.custom_build.89cea1439`, Voxel Tools 1.6.0 Module; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`.

---

## Previous checkpoint — 2026-09-18 (playtest round 4)

**STATUS:** **ROUND-5 PLAYTEST CORRECTIONS — CANDIDATE PASS ON THE EXPORT (PCK `9935113a…`, commit `bcbd6c6`); OWNER RE-PLAYTEST PENDING.** Round 5: axe back to north-east, hand lower and further left, Tab-inventory message moved into the header, hold-to-repeat strikes. Owner confirmed the rest of round 4. Open design question recorded: held items are 2D billboards of the icon art while placed machines are 3D; proposal to make held items 3D box-part models is in the design direction. Axe mirrored toward the crosshair (per-item mirror, no art regeneration); held blocks smaller and above the hotbar; Shift-held drag builds upward; square hotbar tiles with a bottom margin; item captions without slot numbers; windowed frame fitted inside the usable screen (the real cause of the clipped hotbar); P3K stamp registry, save envelope and socket snapping. Work split between the integrator and a subagent (hotbar/captions), reviewed and export-verified. No merge to `main`, no release.

**DONE:** `d520f0e` axe mirror, low-held position, Shift vertical drag (T108, T91); `e654a53` P3K stamps/sockets (T117); `4458500` (subagent) square 80 px hotbar tiles anchored 14 px above the bottom, captions name-only, Tab hotbar key labels top-left (T83, T83_HOTBAR_ULTRAWIDE); `ffc2722` windowed client size clamped to the screen's usable rectangle; `18b1001` P3K slice-2 contract. See [P3J](P3J_DRAG_BUILDING.md), [P3H.4](P3H4_HINGE_HELD_TOOLS_AND_MEASURED_ICONS.md), [P3K](P3K_BLUEPRINTS.md).

**EXPECT:** Axe blade faces the crosshair. Blocks held lower-right, fully visible above the hotbar. Drag sideways, hold Shift, raise the aim → wall rises even with the aim on the ground. Nine square hotbar tiles with a gap below them at any aspect. Tiles show names and ×n only. A windowed game never extends under the taskbar. Blueprints (service-level) snap to stamped sockets and survive save/load.

**TEST:** Static PASS (49/49). Exported on PCK `f0840c9f…`: `TEST_P3D` (T79–T83, T83_HOTBAR_ULTRAWIDE, T108, T109), `TEST_P3F` (T90–T92, T103–T106), `TEST_P3G` (T93–T98, T107, T114, T115), `TEST_P3K` (T110–T113, T117), `TEST_P3C` (T72–T78) PASS. Editor rendered: F1 visual T18 suite PASS after the window-fit change. Owner playtest pending.

**LIMITATIONS/FAILURES:** The window-fit change is verified by the F1 display suite and by reasoning, not on the owner's 3440×1440 desktop; if the hotbar still clips there, re-check `settings_store._apply_display`. Blueprint entry point still absent (slice 2 next).

**NEXT:** P3K slice 2 — Blueprints column in the B menu, W/R rotation, right-click stamp, socket-snapped ghost — then parapet block and Tower Platform retirement.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`, HEAD after this commit, clean, pushed. Canonical `main` unchanged at `c085c013…`. Engine `4.6.stable.custom_build.89cea1439`; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `f0840c9f76504916904316dc6797c822349e7629a64e827adf41e7d0286dc08d`. Evidence roots `artifacts/manual-p3d-usability-1830826926`, `artifacts/manual-p3f-presentation-183313860`, `artifacts/manual-p3g-furnace-1835724312`, `artifacts/manual-p3k-blueprints-1837623266`, `artifacts/manual-p3c-player-defense-1839622221`.

---

## Previous checkpoint — 2026-09-18 (furnace round 3 + P3K slice 1)

**STATUS:** **FURNACE ROUND 3 AND P3K BLUEPRINTS SLICE 1 — CANDIDATE PASS ON THE MERGED EXPORT; OWNER PLAYTEST PENDING.** Furnace: the lit Coal stays until the last job it funds completes; the panel fits 720; select-then-add works without the recipe book (delegated to a subagent, reviewed and verified by the integrator). P3K: blueprint catalogue and stamp engine for castle pieces made of ordinary blocks, with sockets and diagnostics; no player entry point yet. No merge to `main`, no release.

**DONE:** Furnace round 3 (`ca2ac0b`, `5cba9e2`, `1f7502a`): `furnace_fuel_burning` flag, Coal released on `JOB_COMPLETED`, additive Load ×1/×5, gold-highlight selection, inferred recipe, T114/T115. P3K slice 1 (`feature/p3k-blueprints`, merged `c323630`): `tools/generate_blueprints.py` → `contracts/blueprints.json` + runtime mirror (foundation_4, tower_segment_4, cap_4/6/8, wall_4 with top/side sockets); `InteractionService` blueprint planning/commit generalised from P3J with fixpoint support ordering and per-type budgets; per-cell ghost textures; `P3KBlueprintAutomation` T110–T113; `TEST_P3K_BLUEPRINTS.cmd`; `tests/test_blueprints.py`. See [P3I evidence](evidence/P3I_FURNACE_AUTO_PROCESSING.md) and [P3K evidence](evidence/P3K_BLUEPRINTS.md).

**EXPECT:** Furnace: click ore → gold highlight; click Raw Input +1 (Shift +5); Load ×1 works with no recipe chosen; the Coal stays in its slot until the third bar completes. Drag building extends into open sky. Blueprints are service-level only (diagnostics stamp a tower); the player cannot select one yet.

**TEST:** Static PASS (140 links; 45/45). Exported on the merged build (PCK `957aebc8…`): `TEST_P3K` T110–T113, `TEST_P3D` T79–T83/T108/T109, `TEST_P3G` T93–T98/T107/T114/T115, `TEST_P3F` T90–T92/T103–T106 PASS; `TEST_P3E` and `TEST_P3H` PASS on the pre-merge furnace build `4eac9a46…`. Owner playtest pending.

**LIMITATIONS/FAILURES:** No blueprint entry point yet (slice 2). Spiral steps are jumpable full blocks, not stair entities. No parapet auto-connect block. Load ×1 is additive (never returns items) — deliberate change from the target-based slider semantics. The furnace click gestures are covered by a real-click diagnostic (T115) at 1280×720; ultrawide is owner-verified.

**NEXT:** Tony playtests furnace gestures and drag-to-sky. Then P3K slice 2: Blueprints page in the B menu with W/R rotation and right-click stamp, socket ghosting on look; then parapet block and Tower Platform retirement; then P4a siege on stamped caps.

**GIT/REPRODUCIBILITY:** Integration worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`, HEAD after this commit, clean, pushed. Side branch `feature/p3k-blueprints` (worktree `worktrees\p3k-blueprints`) merged and pushed. Canonical `main` unchanged at `c085c013…`. Engine `4.6.stable.custom_build.89cea1439`; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `957aebc87391fa49c7cbdf51432ff1cce508df8dba4fb8b432179e1ccba18e76`. Evidence roots `artifacts/manual-p3k-blueprints-1198625691`, `artifacts/manual-p3d-usability-1200524645`, `artifacts/manual-p3g-furnace-120281580`, `artifacts/manual-p3f-presentation-12048535`, `artifacts/manual-p3e-furnace-117316515`, `artifacts/manual-p3h-balance-1175416218`.

---

## Previous checkpoint — 2026-09-18 (P3I + P3J)

**STATUS:** **P3I FURNACE AUTO-PROCESSING AND P3J DRAG BUILDING — CANDIDATE PASS (ROUND 2 AFTER OWNER PLAYTEST); OWNER RE-PLAYTEST PENDING.** Round 2: the burning Coal now stays in the Fuel slot until burnt out; furnace +1/+5 click gestures and Load ×1/×5; drag building extends into open sky along the row's vertical plane; chained furnace jobs carry leftover time. Both owner-directed slices are implemented on the exported, provenance-matched build with new gate and render tests. The 2026-09-18 design decisions (one game in three tiers, loss as a pillar, blueprints stamp blocks with machines as entity exceptions, Market as core, growing province with a neglect leash) are locked in [the design direction](DESIGN_DIRECTION_2026-09-18.md). Claude is the implementing agent. No merge to `main`, no release.

**DONE:** P3I — idle Furnaces start themselves from deposited input and fuel (`WorkstationService._auto_start_idle_furnaces`), never while paused; the furnace button became **Load from Inventory**; T107. P3J — right-drag plans rows, columns and walls of the held block with support-first ordering, skips blocked cells, trims to stock, commits one world edit plus one inventory transaction with rollback, cancels on left-press; framed green/amber/red ghosts; T108/T109. Repaired the stale F2_KEYBIND_UI test. P3D visual checks now wait for the ghost. See [P3I evidence](evidence/P3I_FURNACE_AUTO_PROCESSING.md) and [P3J evidence](evidence/P3J_DRAG_BUILDING.md).

**EXPECT:** Drop ore and coal into a Furnace and walk away; ingots accumulate in Output. Hold dirt/stone/castle stone, right-press and drag along the ground for a row, upward for a column, both for a wall; release builds the green cells; left-click while holding right cancels. Held tools strike toward the crosshair and vanish down-left (P3H.4 round 3); Tab inventory tiles match the crafting screens.

**TEST:** Static PASS (131 links; 44/44). Exported: `TEST_P3G` (T93–T98, T107), `TEST_P3E` (T84–T89), `TEST_P3D` ×3 (T79–T83, T108, T109), `TEST_P3C` (T72–T78), `TEST_P3F` (T90–T92, T103–T106) PASS. Editor headless: F2 gate, F3 phase1, castle-kit phase1 PASS. Not re-run: P3H balance suite. Owner playtest of P3H.4/P3I/P3J pending.

**LIMITATIONS/FAILURES:** A plain right-click now places on release, not press. No post-build undo, no entity dragging. Furnace auto-start polls each idle furnace every tick (negligible with one recipe). Placed Furnace/Tower skins still lack a close-up render. The Tower Platform entity is slated to retire under P3K blueprints.

**NEXT:** Tony playtests. Then P3K (parapet auto-connect, blueprint model with sockets, first pieces) → P4a siege on stamped caps → P4b Market/gold → P4c army director.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`, HEAD after this commit, clean, tracking origin (pushed). Canonical `main` unchanged at `c085c013…`. Engine `4.6.stable.custom_build.89cea1439`; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `f0c296b2832f8b4c719037152a6d93c5ff717691f03126180895fd5f6cd81ab7` (round 2, commit `620b368`). Evidence roots: `artifacts/manual-p3g-furnace-2406315692`, `artifacts/manual-p3h-balance-2408214646`, `artifacts/manual-p3d-usability-2410213601`, `artifacts/manual-p3e-furnace-2412212555`; round 1: `artifacts/manual-p3g-furnace-568724137`, `artifacts/manual-p3e-furnace-570723092`, `artifacts/manual-p3d-usability-883220165` (+2), `artifacts/manual-p3c-player-defense-851526145`.

---

## Previous checkpoint — 2026-09-18 (P3H.4)

**STATUS:** **P3H.4 HINGE HELD TOOLS / MEASURED ICONS / MASONRY SKINS — CANDIDATE PASS (ROUND 3); OWNER RE-PLAYTEST PENDING.** Round 3: the strike is now a keyframed path (toward the crosshair, whip down-left out of sight, rise back). Tony's wider vision (tower cap sizes, build wheel, gold economy/Foundry/Market, daily waves and breaching, food) is recorded in [the design direction](DESIGN_DIRECTION_2026-09-18.md) with proposals marked not-commissioned. Round 2 after Tony's ultrawide playtest: hand moved one block inward; Tab inventory tiles conformed to the crafting tiles Tony approved. Tony's playtest of the P3H.3 build reported wrong tool hinge and sweep, oversized ammunition with a bolt fragment beside the Stone Shot, off-centre card icons and unskinned Furnace/Tower Platform. All positioning defects traced to fixed cell geometry; they are now driven by measured art regions and a hand hinge. Four follow-up cards (P3I Furnace auto-processing, P3J drag building, P4 siege rework, plus the P3H.4 record) are in the backlog. Claude is the implementing agent from this checkpoint; no merge, push to `main` or release is authorized or performed.

**DONE:** `tools/measure_item_atlas.py` → `game/data/item_atlas_regions.json` (26 tight regions, guarded by `tests/test_item_atlas_regions.py`). `ItemIconCatalog` resolves measured regions and centres card icons in a padded square. `HeldItemView` hinges each item's bottom-left corner at a lower-right hand: tools rest north-east with the handle at the screen base and strike counter-clockwise about the hinge toward the crosshair; low items sit above the hotbar; sprite height is normalised per class so ammunition matches item scale. Furnace visual rebuilt in Castle Stone masonry with an ember arch; Tower Platform textured. `ContentRegistry` accessors typed so headless editor diagnostics run again. T80/T91 strengthened. See [the P3H.4 evidence](evidence/P3H4_HINGE_HELD_TOOLS_AND_MEASURED_ICONS.md).

**EXPECT:** Holding Sword, Picks or Axe shows the handle at the lower-right base pointing up-right; using it swings the head left through the crosshair region and back. Stone Shot and Ballista Bolt are item-sized with nothing else visible. Every icon is centred in its card. A placed Furnace is brick masonry with a glowing arch on its front; Tower Platform is Castle Stone.

**TEST:** Static PASS (118 links; 44/44). Editor headless P3F gate/visual and P3D phase1 PASS on the working tree. Exported provenance-matched `TEST_P3F_PRESENTATION.cmd` (T90–T92, T103–T106), `TEST_P3D_USABILITY.cmd` (T79–T83) and `TEST_P3G_FURNACE_USABILITY.cmd` (T93–T98) PASS. P3C, P3E and P3H suites not re-run. Owner playtest pending.

**LIMITATIONS/FAILURES:** Furnace front arch and Tower Platform skin are not rendered close-up by automation. Hinge constants were tuned at 1280×720; ultrawide may need a nudge. Held items remain raster billboards. A nested pwsh→cmd invocation of a `TEST_*.cmd` failed to find the rebuild script once; direct and double-click launches rebuild correctly and the launcher was not changed.

**NEXT:** Tony playtests the items above. Then P3I → P3J → P4 in backlog order.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`, implementation `1ff190d`, round 2 `67f0e8a`, round 3 `2415c6e`, plus documentation commits, tracking `origin/feature/p3d-tools-world-feedback`. Canonical `main` unchanged at `c085c013…`. Engine `4.6.stable.custom_build.89cea1439`; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `54f822a3b530b6fc53bf4d3c2ee5e76a9c4452a6a86a2168175be60ab05c5909` (round 3). Evidence roots `artifacts/manual-p3f-presentation-137818283`, `artifacts/manual-p3d-usability-1380728734`, and earlier rounds `artifacts/manual-p3f-presentation-268251316`, `artifacts/manual-p3d-usability-2685121767`, `artifacts/manual-p3g-furnace-2694916539`.

---

## Previous checkpoint — 2026-09-18 (recovery)

**STATUS:** **P3H.3 WORK-IN-PROGRESS CHECKPOINTED; LAUNCHER AND RUNTIME GATE RECOVERED; OWNER PLAYTEST PENDING.** The previous Codex session ran out of tokens mid-P3H.3 and left the `game/` tree dirty, which made `START_GAME.cmd` and every `TEST_*.cmd` fail at the clean-tree provenance guard. A third-party audit by Claude committed that work as-is, restored one-click launch, pushed the branch to GitHub for the first time, and recorded findings in [the audit](AUDIT_2026-09-18.md). No merge to `main` and no release occurred.

**DONE:** Committed the P3H.3 candidate (`3a05452`): shared lower-right held base with inward-facing raised tools, dedicated true-alpha ammunition atlas, Castle Stone skins on Gate Frame and Wall Walk Slab, one Log per Axe use, wheel recipe paging, one-press Furnace Escape and red MISSING recipe cards. Added `safe.directory` for the project so Tony's account can use git in the sandbox-owned worktrees. Pushed `feature/p3d-tools-world-feedback` with upstream tracking. Wrote the audit with measured atlas-alignment data, code-quality findings and combat direction.

**EXPECT:** `START_GAME.cmd` in this worktree reports matching provenance and opens the P3H.3 candidate. Tools sit at the lower-right with the handle at the screen base and the head facing inward; Ballista Bolt and Stone Shot show their own art; placed Gate Frame and Wall Walk Slab use the Castle Stone face; the Axe removes one Log per swing; the mouse wheel pages the recipe book; Escape closes a Furnace in one press even with the search focused; recipes lacking materials show red cards.

**TEST:** Foundation validation PASS (11 blocks, 26 items, 21 recipes, 113 links). Python tests PASS 40/40. `start_game.ps1 -PrepareOnly` PASS after one rebuild with manifest `game_tree` = `HEAD:game`. Exported `TEST_P3F_PRESENTATION.cmd` PASS: T90, T91, T92, T103, T104, T105, T106 (`artifacts/manual-p3f-presentation-969719652`). P3C, P3D, P3E, P3G and P3H runtime suites were not re-run at this commit. Owner playtest not yet performed.

**LIMITATIONS/FAILURES:** The P3H.3 commit is a checkpoint of interrupted work, not an owner-accepted milestone; its evidence record has not been written. Canonical `main` documentation still describes F1 because promotion requires review. The ten superseded worktrees remain on disk. Held items remain camera-facing raster billboards anchored at their atlas-cell centre; the audit measures why icon and held positioning drift and proposes anchor metadata rather than further constant tuning. The launcher still refuses a dirty `game/` tree by design; relaxing that is an open proposal.

**NEXT:** Tony launches this worktree's `START_GAME.cmd` and playtests the P3H.3 items above at ultrawide. Then decide the audit proposals in order: launcher dirty-tree behaviour, worktree cleanup, promotion toward `main`, icon/held anchor metadata, defense-service consolidation before any wave/invasion card, `app.gd` split.

**GIT/REPRODUCIBILITY:** Worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`, HEAD `3a05452de91a3ea74c20f8572c674ff226ce6482` plus this documentation commit, clean, tracking `origin/feature/p3d-tools-world-feedback` (pushed). Canonical `main` unchanged at `c085c013d39b2b64321ea6c5a696b96fe2efd45c` local and remote. Game tree `cddb74a2bd16a26c24ffbb4a4cf22745a9e9c8ec`; engine `4.6.stable.custom_build.89cea1439`; Voxel Tools `1.6.0 Module`; EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK SHA-256 `763591684d8618d1fe4b177ddca47ce736090af484d3209ac96fb29475da4e26`. Another machine can now clone the branch and rebuild from repository truth. No merge, release or publication performed.

---

## Previous checkpoint — 2026-09-15

**STATUS:** **P3H.2 HELD-SCALE / UNIFIED-ALPHA CANDIDATE PASS — OWNER ULTRAWIDE PLAYTEST PENDING.** The matching Windows export uses one true-alpha atlas for inventory, recipes and first-person references. Tools are enlarged and share one frame; their visual use has a broad strike arc. Low-held items are twice the prior scale and higher. P3G Furnace usability, P3H balance/fuel/controls and P3H.1 alignment remain passing candidates. No merge, push or release is authorized or performed.

**DONE:** Added the non-destructive `item_atlas_p3h2.png` successor from the verified RGBA P3H.1 source and removed the RGB atlas from the active runtime path. Named tool/low-held framing and motion constants now own presentation. Sword, all Picks and Wood Axe use 0.00340 scale at a common lower-right anchor and a 1.45-radian use sweep. Blocks, materials, stations and siege references use 0.00340—exactly twice P3H.1's 0.00170—at a higher anchor; they receive a short placement nudge instead of a weapon swing. T104 adds a ready/swing/Furnace render to the existing catalog and Workbench-page diagnostic.

**EXPECT:** Inventory and recipe cards show clean cutouts without baked card backgrounds. Iron Sword, Wood Axe and all Picks are approximately the marked-up sword size and location; using a tool visibly travels toward the center before returning. Blocks and stations are substantially larger, higher and remain above the hotbar. No combat damage, recovery, reach or hit timing changed.

**TEST:** Foundation validation PASS: 11 blocks, 26 items, 21 recipes and 112 local links. Python tests PASS 40/40. Provenance-matched exported T90, strengthened T91/T92/T103 and new T104 PASS. Exported P3D T79–T83 regression PASS. The two Workbench pages, eight-item held sheet, three-panel Sword/swing/Furnace comparison, Axe/iron-marker frame and held-block/ghost frame were visually inspected and are clean. See [the P3H.2 evidence record](evidence/P3H2_HELD_SCALE_AND_ALPHA.md).

**LIMITATIONS/FAILURES:** Owner ultrawide gameplay remains required for exact scale/feel. This remains camera-facing prototype raster art, not a hand/arm rig, modeled equipment or per-tool animation set. Two generated transparency candidates were correctly rejected because they baked checker pixels into RGB images; the active asset is the verified alpha source copy. An optional dirty-tree editor headless launch crashed before it could write a log and is not counted as evidence; the authoritative clean provenance-matched release gate passed. The pinned engine emits a non-blocking certificate-store warning in restricted runs. Storage chests remain planned, not implemented.

**NEXT:** Tony should launch the prepared build, hold Sword/Picks/Axe and use each, then hold a block, Workbench, Furnace, Ballista and Catapult at ultrawide resolution. If framing is accepted, P3I remains the next planned runtime slice: small and large persistent storage chests with lossless container transfers and separately playtested capacities/footprints.

**GIT/REPRODUCIBILITY:** Canonical checkout `D:\CODEX\Craft_and_Defend\main` remains on `main` at `c085c013d39b2b64321ea6c5a696b96fe2efd45c`. Active worktree `D:\CODEX\Craft_and_Defend\worktrees\p3d-tools-world-feedback`, branch `feature/p3d-tools-world-feedback`; P3H.2 implementation commit `ecfb3cca23e2f2a4be49ba93308f1a03eace54c8`, game tree `3c4a890d621a30d098813aa1d367e7ce59c99ec5`. Engine `4.6.stable.custom_build.89cea1439`, Voxel Tools `1.6.0 Module`, Compatibility renderer. Export EXE SHA-256 `4ac128729e86108904e6d038d161322404d42d71892b1cc0dca751039aa7e4b2`; PCK `e59799f01b0cf6f23418bb0268f0b12fa25861a7939b95174d82f6ecf68e52f1`. Final evidence roots: `artifacts/manual-p3f-presentation-3273310838` and `artifacts/manual-p3d-usability-9925539`. Exact alpha-source provenance and rejected generation attempts are recorded beside the asset. No merge, push or release performed.
