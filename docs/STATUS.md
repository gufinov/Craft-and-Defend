# Current checkpoint — 2026-09-11

**STATUS:** **F3 PASS; owner accepted 2026-09-11.** Tony confirmed that Slot A and Slot B independently save, exit and Continue, and that Windows Print Screen captures successfully with Snipping Tool visible without exposing or sticking the game pause overlay. The reported `dubious ownership` launcher blocker is repaired and the exact `START_GAME.cmd` path passes under Tony's Windows account. Canonical `main` remains clean and unchanged at its accepted F1 checkpoint.

**DONE:** Implemented independent Slot A/B selection and status on the native menu; timestamped coherent checkpoints with atomic pointer backup/fallback; two-complete-checkpoint retention; recoverable Retry/Return-to-Pause save errors; refusal of malformed, newer-schema and missing-content saves without replacement; copy-only migration from legacy `slots/default` to Slot A; and full workstation/job persistence across restart. Hardened launcher and builder Git probes with an exact repository-scoped, command-only `safe.directory`; no global trust or ownership setting is changed. Kept the exact engine, Voxel Tools edition, architecture, ESDF defaults and F2 game loop.

**EXPECT:** Each slot restores only its own terrain, inventory, player and workstation/job state. A failed write or interrupted publication leaves the previous complete checkpoint loadable. Unsupported data is explained and unchanged. A furnace saved with 3.0 seconds remaining completes once after restart. Print Screen does not expose or stick the Pause menu; ordinary focus loss still pauses.

**TEST:** PASS on Windows 11 `10.0.26200`, Godot `4.6.stable.custom_build.89cea1439`, Voxel Tools `1.6.0 Module`, OpenGL Compatibility on NVIDIA GeForce RTX 5090. T23–T26 pass in the pinned editor and provenance-matched export; exported F2, F1 and F0 gates also pass in fresh processes with the matching editor closed. Tony's graphical test confirmed independent Slot A/B save-exit-Continue cycles and the real Windows Print Screen/Snipping Tool path. The builder, `-PrepareOnly`, direct launcher and exact `START_GAME.cmd` wrapper pass under Tony's account with ambient Git trust overrides removed. Foundation validator and 19/19 Python tests pass. Rendered 1280×720 menu/gameplay/pause captures pass. See [F3 evidence](evidence/F3_PERSISTENCE_HARDENING.md).

**LIMITATIONS/FAILURES:** Failure recovery remains injection-tested rather than a physical power-removal test. The worktree ownership boundary still exists by design; the one-click launcher and builder handle it without weakening global Git security, while ad-hoc manual Git commands outside Codex may still require an explicit per-command trust argument. Injected failures prove application recovery boundaries, not filesystem-hardware guarantees or final large-world performance. No engine/content substitution occurred.

**NEXT:** Begin F4 in a new dependent worktree. Add Tony's approved non-pausing in-game screenshot action as a globally persisted, rebindable F2 control, then complete T27–T30. Do not merge, push or publish without separate authority.

**GIT/REPRODUCIBILITY:** Main path `D:\CODEX\Craft_and_Defend\main`, clean `main` and `origin/main` at `c085c013d39b2b64321ea6c5a696b96fe2efd45c`. F3 worktree `D:\CODEX\Craft_and_Defend\worktrees\f3-persistence`, branch `feature/f3-persistence-hardening`; launcher-fix and final package source commit `a1172cbc3d6b5041077aab85428eb227f9ccd3da`, game tree `498ad7ea30fd5066aa659b2c7c50e174e2c5f1a1`. No merge, push or release performed.
