# Current checkpoint — 2026-09-10

**STATUS:** **F0 Windows integration PASS.** T01–T12 have recorded Windows evidence. F1 and all content expansion remain not started and unauthorized by this checkpoint.

**DONE:** Implemented the native menu and explicit Start lifecycle, finite 64×32×128 block world using `VoxelTerrain`, collision-ready `CharacterBody3D`, exact ESDF defaults, voxel-raycast dirt break/gather/place transactions, invalid placement rejection, Escape pause, forward-key capture/conflict rejection/persistence, fresh per-session `VoxelStreamSQLite`, coherent normal checkpoint publication, full process restart/Continue, wrong-engine rejection, Windows export with the matching custom release template, editor-closed execution, real WM_CLOSE save-and-exit, and visible save-failure behavior. Added double-click start/build paths. See [F0 evidence](evidence/F0_WINDOWS_INTEGRATION.md).

**EXPECT:** `START_F0.cmd` opens the native menu. Start enters a plain green finite voxel test world after collision is ready. E/D/S/F move; Space jumps; mouse looks; left click gathers dirt; right click places it; Escape opens the pause menu. Save and Quit followed by Continue restores the tested terrain edits, exact dirt count, transform, and rebound Forward key.

**TEST:** Windows 11 Pro build 26200; Ryzen 9 7900X3D; 63.2 GB RAM; RTX 5090; 1920×1080 desktop; 1280×720 app viewport; OpenGL Compatibility. Python 3.12.14 validator and all 16 unit tests pass. Godot `4.6.stable.custom_build.89cea1439` / Voxel Tools `1.6.0 Module` class probe passes. The exact exported EXE/PCK passed two-process F0 automation, real Windows close, injected missing-snapshot visibility, and rendered menu/gameplay/pause capture with the Godot editor closed.

**LIMITATIONS/FAILURES:** F0 is a deliberately plain integration slice, not the full Foundation. Power-loss durability, denied-write/full-disk recovery, checkpoint retention cleanup, A/B slots, performance targets, richer settings, inventory UI, crafting, workstations, day/night and content are later gates. Input and key-capture paths were exercised by deterministic Windows runtime events and visually inspected; a separate human exploratory play session is still recommended. No project license has been selected. The first hidden-window WM_CLOSE attempt had no window handle and was discarded; the visible real-window test passed. No external source/assets were copied.

**NEXT:** Stop at the F0 gate. Tony may perform the short graphical test in `game/README.md` and review the evidence. Begin F1 only with explicit authorization; do not start crafting, enemies, rifts, workers, multiplayer, automation, procedural expansion or siege weapons.

**GIT/REPRODUCIBILITY:** Canonical checkout `D:\CODEX\Craft_and_Defend\main` remains clean on `main` at `1a94cffc82280f485f6f877a5e64567159ddd3af`. Implementation worktree `D:\CODEX\Craft_and_Defend\worktrees\foundation`, branch `prototype/foundation`, code/export checkpoint `81f075a`; the final documentation/evidence commit is the commit containing this file. Build output and local logs/saves are ignored. No merge, push or release was performed.
