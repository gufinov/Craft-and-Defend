# Current checkpoint — 2026-09-11

**STATUS:** **F4 REVISION IMPLEMENTATION PASS on an isolated worktree; owner graphical retest pending.** Tony correctly rejected the first day/night presentation as imperceptible and clock/solar-time inspection found it misaligned. The revised T27 controls, exported restart gate and affected F1 regressions pass. F3 remains preserved at owner-accepted commit `6e8838f`; canonical `main` remains clean and unchanged at its accepted F1 checkpoint.

**DONE:** Preserved accepted F0–F3 behavior and the F2 capture/portable package. Corrected solar time, made sunrise/sunset/night visually distinct, added a visible sun, changed new-world start to `08:00 Sunrise`, corrected the HUD wording, and added a scrollable World Settings accordion with validated `HHMM`/`HH:MM`, a Day/Night cycle toggle and explicit Apply. Time and toggle state belong to each slot and persist in the coherent checkpoint.

**EXPECT:** A new world visibly opens at 08:00 sunrise. From paused Settings, World Settings applies a valid time immediately; an invalid time changes nothing. Disabling the cycle freezes time/lighting, enabling resumes it, and Save/Continue restores the per-slot choice without wall-clock catch-up. F2 capture and portable behavior remain unchanged.

**TEST:** PASS on Windows 11 `10.0.26200`, Godot `4.6.stable.custom_build.89cea1439`, Voxel Tools `1.6.0 Module`, OpenGL Compatibility on NVIDIA GeForce RTX 5090. Final packaged export T27/T28/T29/T30 phase1/phase2 PASS: phase `0.3333 → 0.4333`, invalid `2460` remained unchanged, `2200` applied, disabled cycle stayed frozen across full restart, and re-enabling advanced. GPU evidence at 1280×720 shows the complete World Settings section plus distinct 08:00/14:00/20:00/22:00 frames. Affected F1 phase1/phase2 and 16:9/high-DPI/ultrawide visual regressions pass. Validator PASS; 20/20 Python tests PASS. See [F4 evidence](evidence/F4_FOUNDATION_ACCEPTANCE.md).

**LIMITATIONS/FAILURES:** Rain/weather and a backtick command console are not implemented; adding nonfunctional controls was explicitly avoided. One intermediate export exposed `Identifier "scroll" not declared` after a local UI rename; it was corrected and the final export/regressions pass. A direct custom-engine headless parse probe crashed while opening its user log, so release export plus graphical execution remain the authoritative runtime checks. Portable isolation remains verified on this PC rather than a second physical machine. Owner graphical F4 acceptance remains pending; no engine/content substitution occurred.

**NEXT:** Tony launches the revised F4 worktree, starts a new slot at visible 08:00 sunrise, then uses Pause → Settings → World Settings to apply `1400`, `2000`, `2200`, disable/enable the cycle, and Save/Continue. Fix any owner-found F4 defect before promotion. Do not merge, push or publish without separate authority.

**GIT/REPRODUCIBILITY:** Main path `D:\CODEX\Craft_and_Defend\main`, clean `main` and `origin/main` at `c085c013d39b2b64321ea6c5a696b96fe2efd45c`. F4 worktree `D:\CODEX\Craft_and_Defend\worktrees\f4-foundation`, branch `feature/f4-foundation-acceptance`, based on clean F3 acceptance commit `6e8838fc3112589f2fcf59291b02a0a040245729`. Corrected code commit `e52e6b74f8cf2371b7cd5dc0fb695eab695350e4`; final export source `f637df970b835cd46c7ba71b0aa0973fc143d542`, game tree `0e8a2918013c1565d4d2623864116a1fe590d75c`. No merge, push or release performed.
