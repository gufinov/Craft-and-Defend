# Current checkpoint — 2026-09-11

**STATUS:** **F1 corrective candidate automated PASS; owner fullscreen retest pending.** Tony confirmed every other F1 path works, then correctly rejected T18 because fullscreen pillarboxed the fixed 16:9 canvas on his 3440×1440 monitor. The defect is corrected and proven in the exact exported Windows build; F1 remains isolated and unmerged until Tony verifies the replacement package.

**DONE:** Preserved the complete F1 feature set and corrected display scaling from Godot 4.6's default `keep` aspect to explicit `expand`. Fullscreen now uses the monitor's native size and fills ultrawide edges; the windowed-resolution selector is disabled in fullscreen with an explanation, and real windowed selections change the native client area. The automated display gate now measures native window/screen sizes and non-black edge pixels instead of only checking saved settings and same-aspect captures. See [F1 evidence](evidence/F1_INTERACTION_HARDENING.md).

**EXPECT:** `START_GAME.cmd` opens the corrected F1 candidate. Fullscreen fills the complete 3440×1440 display without black side bars and explains that it uses the monitor's native resolution. Windowed mode enables the resolution selector and changes the actual app window. Existing keybind, settings rollback, inventory, focus, ESDF movement, dirt edit/save and Continue behavior remain unchanged.

**TEST:** Windows 11 Pro build 26200; Ryzen 9 7900X3D; 63.2 GB RAM; RTX 5090; OpenGL Compatibility. The corrected exported build reported an actual 1600×900 window, then a 3440×1440 fullscreen window matching the active screen. Its 3440×1440 framebuffer contained game-background pixels at both horizontal edges and the capture was visually inspected edge-to-edge. The F1 two-process restart suite, complete F0 save/Continue regression, foundation validator and all 16 Python tests passed after the correction.

**LIMITATIONS/FAILURES:** Tony still needs to retest this corrected fullscreen build. The original automated T18 result was insufficient: it set the canvas base and test window to the same aspect ratio, so a legible ultrawide PNG did not prove native fullscreen fill. Tony's screenshots superseded that claim and triggered the stronger test. The inventory/footprint and later-gate limitations remain unchanged. No engine/content substitution occurred.

**NEXT:** Tony relaunches from `START_GAME.cmd`, selects Fullscreen and confirms the blue game canvas reaches both physical edges. He then switches to Windowed and confirms 1280×720 versus 1920×1080 changes the window size. If those pass, F1 can be promoted through the normal review path. Do not begin F2 before that decision.

**GIT/REPRODUCIBILITY:** Canonical checkout `D:\CODEX\Craft_and_Defend\main` remains on accepted F0 commit `466579e9df7570aa174836895e65082cda765b3a`. F1 worktree: `D:\CODEX\Craft_and_Defend\worktrees\f1-interaction`; branch `feature/f1-interaction-hardening`; fullscreen correction checkpoint `004cd34`. Build output and local logs/saves are ignored. F1 is not merged, pushed or released.
