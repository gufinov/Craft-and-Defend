# Current checkpoint — 2026-09-10

**STATUS:** **F1 interaction-hardening candidate automated PASS; owner review pending.** T13–T18 passed in the exact exported Windows build, and the full F0 automation, restart/Continue, real Windows close and visible close-failure paths regressed cleanly. F0 remains the owner-accepted `main` checkpoint; F1 remains isolated and unmerged.

**DONE:** Replaced the one-key F0 binding screen with a scrollable editor for all 21 implemented actions, keyboard and mouse capture, named conflict rejection, reset defaults, restart persistence and corrupt-map recovery. Added mouse sensitivity, invert-Y, master volume, window mode/resolution preview with ten-second rollback, inventory input ownership, focus-loss pause, explicit mouse recapture, boundary/edit feedback and an atomic rotated multi-cell footprint service with synthetic rejection/release tests. Added generic `START_GAME.cmd` and `BUILD_WINDOWS.cmd` paths while preserving F0 wrappers. See [F1 evidence](evidence/F1_INTERACTION_HARDENING.md).

**EXPECT:** `START_GAME.cmd` opens the F1 candidate. Keybinds lists every implemented action and allows a keyboard or mouse assignment; conflicts explain themselves and change nothing. Settings changes input/audio immediately and display only after a confirmed preview. Tab opens a blocking inventory overlay; focus loss pauses; only explicit Resume recaptures play. Existing ESDF movement, dirt edit/save and Continue behavior remain unchanged.

**TEST:** Windows 11 Pro build 26200; Ryzen 9 7900X3D; 63.2 GB RAM; RTX 5090; OpenGL Compatibility. The pinned Godot `4.6.stable.custom_build.89cea1439` / Voxel Tools `1.6.0 Module` export passed T13–T18 in two clean processes, including keyboard/mouse persistence and corrupt-map recovery. Editor-closed PNGs passed at 1280×720, simulated 150% scaling and 1720×720 ultrawide and were visually inspected as legible and unclipped. Real `CloseMainWindow()` saved and exited 0; a clean process verified the checkpoint. The F0 two-process exported regression, injected save failure, foundation validator and all 16 Python tests passed.

**LIMITATIONS/FAILURES:** Tony has not yet hands-on tested this F1 candidate. The inventory is intentionally a read-only dirt summary until F2; the footprint service is contract-tested but no multi-cell gameplay content exists. Power-loss durability, denied-write/full-disk recovery, checkpoint retention, independent slots, crafting/workstations, day/night and richer content remain later gates. No project license has been selected. The custom runtime logged a non-fatal Windows root-certificate warning in sandboxed runs. One headless startup hit log rotation before project boot; assigning a project-local log path eliminated it. No engine/content substitution occurred.

**NEXT:** Tony tests the F1 candidate from `START_GAME.cmd`, especially rebinding several keyboard/mouse actions, conflict/reset behavior, Tab inventory, focus loss, display rollback and restart persistence. If accepted, promote this branch through the normal review path. Do not begin F2 or later content before that decision.

**GIT/REPRODUCIBILITY:** Canonical checkout `D:\CODEX\Craft_and_Defend\main` remains on accepted F0 commit `466579e9df7570aa174836895e65082cda765b3a`. F1 worktree: `D:\CODEX\Craft_and_Defend\worktrees\f1-interaction`; branch `feature/f1-interaction-hardening`; runtime checkpoint `805c3bc`. Build output and local logs/saves are ignored. F1 is not merged, pushed or released.
