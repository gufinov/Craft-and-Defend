# Current checkpoint — 2026-09-11

**STATUS:** **F1 corrective candidate automated PASS; owner window-placement retest pending.** Tony confirmed the fullscreen-fill correction and every other F1 path, then found that Windowed previews ignored the active monitor's virtual-desktop origin. On his multi-monitor layout this moved the window left/up or onto another display. The second T18 defect is corrected and proven in the exact exported Windows build; F1 remains isolated and unmerged until Tony verifies the replacement package.

**DONE:** Preserved the complete F1 feature set and both display corrections. Fullscreen uses the active monitor's native size with an expanding canvas. Windowed transitions now preserve the active monitor, include its virtual-desktop origin, center the decorated frame inside its usable work area and keep the title bar reachable. Fullscreen displays the detected `3440 × 1440 (monitor native)` value instead of a disabled stored windowed size. See [F1 evidence](evidence/F1_INTERACTION_HARDENING.md).

**EXPECT:** `START_GAME.cmd` opens the corrected F1 candidate. Fullscreen fills the complete 3440×1440 display and reports that native size. Switching to Windowed at 1920×1080, 1280×720 or 1600×900 keeps the complete decorated window centered on the same active monitor, including on layouts whose monitor origin is not `(0, 0)`. Existing keybind, rollback, inventory, focus, ESDF movement, dirt edit/save and Continue behavior remain unchanged.

**TEST:** Windows 11 Pro build 26200; Ryzen 9 7900X3D; 63.2 GB RAM; RTX 5090; OpenGL Compatibility. The exported build detected the active monitor's usable rectangle at position `(1080, 240)` with size `3440×1392`. Fullscreen remained native 3440×1440 and edge-filled. Fullscreen-to-windowed previews at 1920×1080, 1280×720 and 1600×900 stayed on that same monitor; their decorated frames were completely within the usable rectangle. A synthetic negative-origin case also passed. The F1 two-process restart suite, complete F0 save/Continue regression, foundation validator and all 16 Python tests passed.

**LIMITATIONS/FAILURES:** Tony still needs to retest window placement in the replacement build. The first display correction proved native fill but its windowed test asserted only client size, not monitor identity or decorated bounds; Tony's screenshots superseded that incomplete T18 claim. One editor parse attempt reproduced the already-recorded log-rotation startup crash; the fresh-log rerun parsed normally. The inventory/footprint and later-gate limitations remain unchanged. No engine/content substitution occurred.

**NEXT:** Tony relaunches from `START_GAME.cmd`, opens Settings from the main menu, switches Fullscreen → Windowed and previews 1920×1080 and 1280×720. The title bar and all decorated edges should remain visible on the ultrawide. Repeat once from paused in-game Settings. If those pass, F1 can be promoted through the normal review path. Do not begin F2 before that decision.

**GIT/REPRODUCIBILITY:** Canonical checkout `D:\CODEX\Craft_and_Defend\main` remains on accepted F0 commit `466579e9df7570aa174836895e65082cda765b3a`. F1 worktree: `D:\CODEX\Craft_and_Defend\worktrees\f1-interaction`; branch `feature/f1-interaction-hardening`; multi-monitor correction checkpoint `1db98b5`. Build output and local logs/saves are ignored. F1 is not merged, pushed or released.
