# Current checkpoint — 2026-09-11

**STATUS:** **F4 DAY/NIGHT OWNER ACCEPTED; SIDE-FACE PLACEMENT REPAIR IMPLEMENTATION PASS on the isolated worktree; owner graphical retest pending.** Tony confirmed Sunrise, day/night, the moving sun and current World Settings all work. His acceptance playtest exposed the below-only block-support defect; the matching Windows export now permits ordinary blocks to attach to any solid neighboring face while retaining atomic invalid-placement rejection. F3 remains preserved at owner-accepted commit `6e8838f`; canonical `main` remains clean and unchanged at its accepted F1 checkpoint.

**DONE:** Preserved accepted F0–F3 behavior and the F4 clock/capture/portable package. Ordinary voxel blocks now accept support from any of their six orthogonally adjacent solid voxel faces, allowing horizontal ledges with air below and attachment underneath existing blocks. Truly floating, occupied, out-of-bounds and player-overlap placement still fails without mutation. Workbench/furnace floor support is unchanged.

**EXPECT:** A right-click aimed at any face places the selected block into the adjacent empty cell when at least one solid voxel touches that destination, even if nothing is below it. A destination with no solid neighboring face remains unsupported. Successful placement consumes exactly one item and persists through Save/Continue; every rejected attempt consumes nothing.

**TEST:** PASS on Windows 11 `10.0.26200`, Godot `4.6.stable.custom_build.89cea1439`, Voxel Tools `1.6.0 Module`, OpenGL Compatibility on NVIDIA GeForce RTX 5090. Matching exported F0 phase1 proves elevated side-face placement with air below, exact consume/refund accounting, true-floating rejection without mutation, and all prior bounds/occupancy/player-overlap cases. A fresh phase2 process restores terrain, inventory/revision and transform. Exported F4 phase1/phase2 reconfirms Sunrise/time controls, night readability, F2 capture and clock Continue. Validator PASS; 20/20 Python tests PASS. See [placement repair evidence](evidence/F4_SIDE_FACE_PLACEMENT_REPAIR.md) and [F4 evidence](evidence/F4_FOUNDATION_ACCEPTANCE.md).

**LIMITATIONS/FAILURES:** The side-placement automation exercises one horizontal direction; all six directions use the same support loop, but camera-targeting presentation awaits Tony's graphical retest. General structural collapse remains out of scope. Rain/weather and a backtick command console are not implemented. Godot's recorded Windows root-certificate warning did not stop the offline gates. No engine/content substitution occurred.

**NEXT:** Tony launches the F4 worktree, targets the side of a raised block and places another block into the adjacent empty space with air below, then saves, fully exits and Continues. Fix any owner-found defect before F4 promotion. Do not begin P1, merge, push or publish without separate authority.

**GIT/REPRODUCIBILITY:** Main path `D:\CODEX\Craft_and_Defend\main`, clean `main` and `origin/main` at `c085c013d39b2b64321ea6c5a696b96fe2efd45c`. F4 worktree `D:\CODEX\Craft_and_Defend\worktrees\f4-foundation`, branch `feature/f4-foundation-acceptance`, based on clean F3 acceptance commit `6e8838fc3112589f2fcf59291b02a0a040245729`. Placement repair/export source `c91f0f294510d4f979cd913f9267d1a75fcb3d56`, game tree `51c4aa8e4db470c7716270dc8fa0e3b8aed6f261`. No merge, push or release performed.
