# Test and acceptance plan

Runtime rows begin **NOT RUN** and change only through recorded runtime evidence. T01–T12 are owner-accepted PASS as of 2026-09-10 in [the F0 evidence record](evidence/F0_WINDOWS_INTEGRATION.md). T13–T18 are owner-accepted PASS as of 2026-09-11 in [the F1 evidence record](evidence/F1_INTERACTION_HARDENING.md). Tony's ultrawide tests exposed and verified corrections for fixed-aspect pillarboxing and window placement that ignored a nonzero monitor origin. The final exported gate tests native fill, active-monitor identity, decorated bounds and negative virtual-desktop coordinates. Passing repository checks alone does not change a runtime gate. Record later results per test ID in an evidence file copied from [the template](evidence/TEMPLATE.md), including actual/expected behavior and artifacts. Use isolated disposable saves for failure injection.

## Existing repository checks (G0)

`python tools/validate_foundation.py` checks JSON structure/cross-references, unique stable IDs, tool/station progression reachability, default control conflicts, bounds/placement fixture expectations, release manifest shape, original-report hash and local Markdown link targets. `python -m unittest discover -s tests -v` tests rejection of invalid variants. These are static contract checks only; the reachability proof ignores finite resource quantities and spatial accessibility.

## Runtime gates

| ID | Gate | Test / required result |
|---|---|---|
| T01 | F0 | Record archive hashes, editor version, module classes and renderer; wrong editor fails clearly |
| T02 | F0 | Clean launch opens menu; player enters only by Start; Quit from menu works |
| T03 | F0 | Start loads correct finite bounds; collision-ready spawn never falls through unloaded terrain |
| T04 | F0 | E/D/S/F, jump, mouse look work; keyboard/pointer usable after pause/resume |
| T05 | F0 | Break dirt, receive exactly one dirt, place it elsewhere, consume exactly one |
| T06 | F0 | A block can attach to any solid side face with air below; truly floating, outside-bounds, occupied and player-overlap placements fail without inventory/world mutation |
| T07 | F0 | Escape pauses; menu/settings stay responsive; Resume restores intended state |
| T08 | F0 | Rebind forward, save settings, fully restart; new key works and default/reset behavior is clear |
| T09 | F0 | Save/exit/restart/Continue preserves edited cells, player transform and exact inventory counts |
| T10 | F0 | Move far enough to unload an edited chunk, reload it, save/quit/restart; edits remain |
| T11 | F0 | Export with custom release template; launcher rejects/rebuilds a stale checkout package; launch outside editor; repeat T02–T09 |
| T12 | F0 | Window-close/Alt-F4 during play follows coherent save workflow; failure stays visible |
| T13 | F1 | All defaults, conflict rejection, cancel capture and reset work; UI actions never mine/place |
| T14 | F1 | Inventory/settings/focus-loss pause; mouse capture restored only on explicit resume |
| T15 | F1 | Negative coordinates and all six bound faces/corners use half-open bounds; bottom protected |
| T16 | F1 | Rapid clicks/stale request/full inventory/wrong tool cause no duplication or resource loss |
| T17 | F1 | Multi-cell synthetic footprints reject partial overlap, unloaded cells, unsupported cells and player overlap; removal releases all owned cells once |
| T18 | F1 | Display changes revert if unconfirmed; fullscreen reports/fills the active monitor; windowed previews stay within that monitor's usable decorated bounds; 16:9/ultrawide/high-DPI menus remain usable |
| T19 | F2 | Empty-inventory player reaches wood pick → stone pick → smelted iron → iron pick with no debug grants |
| T20 | F2 | Insufficient input/tool/workstation/output capacity leaves inventory unchanged; recipe output exact |
| T21 | F2 | Bench/furnace placement consumes one item; support/dismantle rules prevent orphans/duplicates |
| T22 | F2 | Furnace consumes input/fuel once, pauses correctly, reserves output and completes once |
| T23 | F3 | A/B save slots and rapid menu/new/continue cycles have no terrain/state contamination |
| T24 | F3 | Inject denied write/full disk and interruption around checkpoint publication; previous complete checkpoint remains loadable |
| T25 | F3 | Malformed/newer/missing-content save is refused clearly without replacement; supported migration copies original |
| T26 | F3 | Save/restart midway through furnace job preserves remaining time and produces exactly one output |
| T27 | F4 | New world begins at visible 08:00 sunrise; validated World Settings apply `HHMM`/`HH:MM` and cycle toggle; day/night advances during play, freezes when disabled or in overlays/pause, restores phase/day/toggle; no offline catch-up |
| T28 | F4 | Blocks/resources distinguishable; selection, craft feedback and key help visible; rebindable F2 captures the rendered viewport in the background without pausing; no placeholder functional claims |
| T29 | F4 | Measure frame times, edit latency, memory, save size/latency in fixed scenario; record hardware/build/renderer/resolution |
| T30 | F4 | Fresh portable folder launch with editor closed runs full Foundation loop; README run/save-location instructions accurate |
| T31 | F5 | Tab opens inventory only; B opens/closes 2×2 hand crafting; both freeze the body and block world mutations while the world keeps running (2026-09-22); B persists/rebinds safely |
| T32 | F5 | Right-clicked workbench opens 3×3 station modal without placing; Shift cannot bypass; non-station right-click still places; workbench recipe book contains both hand and advanced recipes |
| T33 | F5 | Furnace owns ore+fuel processing modal; recipe results remain atomic and timed jobs complete exactly once |
| T34 | F5 | Inventory and crafting presentations remain legible/keyboard-usable at 1280×720 and 3440×1440; labels do not imply unimplemented content |
| T35 | F5 | 4× MSAA, VSync and physics interpolation default on; quality preferences persist; sunlight shadow transforms remain stable within a visual minute and use bounded blended splits |
| T36 | F5 | Three-panel crafting shows inventory/grid/scrolling recipe book; inventory drag/click staging, recipe search+Enter autofill, manual recognition and atomic craft validation work |
| T37 | P1 | New worlds select `terrain_p1_1` with seed 41026; metadata-free saves select legacy flat generation; unknown generator versions are refused |
| T38 | P1 | The pinned seed reproducibly yields hills, valleys, flat safe clearing, trees and coal/iron while fixed starter resources remain reachable |
| T39 | P1 | Exploring outside the clearing shows an updating home distance/bearing cue; returning reports `HOME CLEARING` |
| T40 | P1 | Save/full-process-restart/Continue restores terrain edit, player location and exact generator version/seed; legacy routing remains unchanged |
| T41 | P1 | Terrain is visually readable at 1280×720 and owner ultrawide; fixed exploration route records frame/edit/save evidence without blocking streaming |
| T42 | P1 | Workbench exposes stable IDs/recipes for stair, wall-walk slab, parapet merlon, tower platform and open gate frame; the platform has a typed future defense mount socket |
| T43 | P1 | Entity preview is non-mutating; quarter-turn rotation transforms occupied/support cells; unsupported, partial-overlap, unloaded and player-overlap placements reject atomically |
| T44 | P1 | A selected structural item shows a green/red world preview, X rotates it, right-click commits only a valid preview, and collision geometry matches the visible piece |
| T45 | P1 | Save/full-process-restart/Continue restores every structure once with anchor/orientation; dismantling releases the complete footprint and refunds exactly one item |
| T46 | P1 | First castle kit and workbench recipe list are legible at 1280×720 and owner ultrawide; stairs, wall walks, parapets, platform and gate opening support a useful small build |
| T47 | P1 | Tab separates 18 carried slots, one equipped 1–9 hotbar row and a full-height six-position armor loadout without changing the authoritative 27-slot accounting |
| T48 | P1 | The three-section inventory and original character guide are readable and unclipped at 1280×720 and the 1720×720 logical ultrawide viewport used for 3440×1440 fullscreen validation; unavailable armor interaction is stated truthfully |
| T49 | P1 | Drag/drop and the two-click keyboard fallback move or swap exact Carried/Hotbar slots; category filters are view-only; sorting reorders only the 18 Carried slots in one inventory revision; the resulting snapshot restores exactly |
| T50 | P1 | Stone stairs render and collide as exactly two half-block steps; sustained ordinary forward movement climbs both without Jump, while step-up remains capped below a full block |
| T51 | P2 | The pinned Module exposes the inspected `VoxelAStarGrid3D` API and the local spike contract fixes one cardinal 1×2-cell agent with bounded step/drop rules |
| T52 | P2 | Both planners route a single agent around a corridor wall; the local planner also accounts for a placed stair entity and validates every returned transition with zero stuck cases |
| T53 | P2 | A two-cell trench and one-cell tunnel clearance produce no unsafe route; removing one bridge cell invalidates the route and an incremental snapshot refresh observes the exact changed cell |
| T54 | P2 | A blocked unarmed agent reports `NO_ROUTE`; a basic raider attacks allowed earth/wood but not castle stone; a siege candidate can target fortification; no fake path is returned through an intact obstruction |
| T55 | P2 | The fixed 13×5×13 fixture records full/incremental snapshot cost, median/p95 query time, visited cells, route validation and a clearly labelled memory-scale estimate for both approaches where available |
| T56 | P2 | A rendered 1280×720 diagnostic visibly distinguishes start, goal, wall, local route and 1×2 probe at the goal; it is evidence only and does not imply production enemy locomotion |
| T57 | P3 | An explicit paused-menu action creates one practice barricade and one stationary mounted ballista, then gives a readable five-second warning before exactly one raider spawns |
| T58 | P3 | One physical 1×2 raider follows the selected P2 local obstruction plan; voxel and placed-entity add/remove events refresh only exact changed cells in its bounded snapshot |
| T59 | P3 | The raider visibly damages the barricade; Shift-use through the real physics target path consumes exactly one Planks item and restores six integrity; full/no-material attempts do not mutate state |
| T60 | P3 | The stationary ballista engages after the first wall hit, consumes exactly four visible bolts for four five-damage shots and defeats exactly one 20-health raider while retaining readable wall damage |
| T61 | P3 | Defense phase, arena, wall integrity, ammunition, raider health/position and navigation revision survive service snapshot/restore without duplicating fixtures |
| T62 | P3 | Save during an active attack and fully restart; Continue restores exact wall integrity, remaining bolts, raider health and one reconstructed physical raider |
| T63 | P3 | A rendered 1280×720 frame shows the physical raider, damaged barricade, mounted ballista and readable defense HUD; owner playtest decides readability/fun |
| T64 | P3 | The live gameplay HUD shows all nine held/hotbar slots, their key, item identity and count; changing keys moves the selected **HELD** state and mirrors the Tab inventory hotbar |
| T65 | P3 | A ballista shot requires a clear direct physics ray to the raider; an inserted solid blocker causes no damage or ammunition consumption, and a clear shot renders a visible travelling bolt |
| T66 | P3B | A workbench recipe creates one two-cell-high wooden barricade; ordinary placement consumes one item and creates one stable entity with data-driven wood tags, current/max integrity and exact occupied cells |
| T67 | P3B | One clearly labelled strategic-core prototype is the default target; when any valid route reaches its approach, the raider selects that route and does not damage a nearby wooden or castle defense |
| T68 | P3B | When wooden barricades close every valid local route, the basic raider selects one exact obstruction, deals bounded damage, destroys the whole entity without refund, releases its occupied cells once and replans toward the core |
| T69 | P3B | Basic-raider capability cannot select castle-stone voxels or castle-kit entities for damage; a bounded siege-candidate capability can identify the same fortification without fabricating a route through it |
| T70 | P3B | Save during an active core attack and fully restart; Continue restores exact core health, raider health/position, active target and every surviving player-built defense integrity once |
| T71 | P3B | Rendered 1280×720 evidence clearly shows the strategic core, field-side raider, an open entrance and player-built barricade health/target feedback; owner ultrawide playtest decides readability |
| T72 | P3C | Every registered item resolves a stable icon; inventory, hotbar and crafting expose graphical identity and counts |
| T73 | P3C/P3H.3 | Workbench recipe book (35 recipes, three pages since the CoasterCraft Shop) shows at most 12 fixed cards per page with bounded buttons, wheel page-turning and deterministic search; insufficient-resource cards are red |
| T74 | P3C | Equipped sword performs one clear bounded raider hit; miss/cooldown do not mutate terrain or enemy state; lethal strike wins |
| T75 | P3C | Ballista accepts supported ground/typed tower socket, holds fire through obstruction and consumes one bolt only on a clear shot |
| T76 | P3C | Catapult rejects below-minimum/beyond-maximum/blocked arcs and consumes one shot only for a valid clear ballistic arc |
| T77 | P3C | Carried sword plus placed siege identity, ammunition and cooldown survive atomic save and separate-process Continue |
| T78 | P3C | Rendered 1280×720 Workbench shows 12 populated icon cards, paging and three distinct crafting panels |
| T79 | P3D | Shift+Click on an immediate recipe crafts exactly five batches in one revision; insufficient input changes nothing; timed jobs remain single |
| T80 | P3D/P3H.4 | Active sword, picks, axe and placeables share one lower-right hand column; tools hinge at the screen base and placeables sit above the hotbar; hotbar identity remains authoritative |
| T81 | P3D/P3H.3 | Wood Axe recipe is reachable; one use removes/gathers only the targeted Log while the upper trunk remains |
| T82 | P3D | Placeable blocks expose the same non-mutating validation in their green/red world ghost; the starter marker points to actual guaranteed iron ore |
| T83 | P3D | Rendered 1280×720 evidence visibly shows the held axe/iron marker and the low held block/world placement ghost; owner ultrawide test decides scale/readability |
| T84 | P3E | Whole compatible inventory stacks transfer into distinct persistent Furnace Raw Input and Fuel slots without duplication |
| T85 | P3E | Right-click takes the larger half; right-click deposit and right-drag distribute one per compatible slot with exact counts |
| T86 | P3E | Furnace consumes one input and one counted fuel operation, retains exactly one Output across clean restore and moves it only on explicit collection |
| T87 | P3E/P3H.3 | Furnace modal has three real slots (help label names the select-then-add gesture; the persistence note lives in its tooltip), closes with one Escape while recipe search owns focus, and retains revised station/tool identity |
| T88 | P3E | Manual hand/workbench patterns are recognized without recipe-book selection or search |
| T89 | P3E | Rendered 1280×720 evidence visibly shows the three-slot Furnace and revised world/held identity |
| T90 | P3F | Workbench recipes use an exact stable progression order from hand fundamentals through tools, castle construction, iron equipment and siege recipes |
| T91 | P3F/P3H.4 | All 26 registered items resolve filter-clipped true-alpha references from measured art regions that do not overlap; Ballista Bolt and Stone Shot are isolated from each other; the held view hinges at the lower-right hand with the Sword normalised to `TOOL_HEIGHT`, ammunition to `LOW_HEIGHT` and a swing arc of at least 1.2 rad; all ten non-air voxel cubes keep complete face textures |
| T92 | P3F | Rendered contact sheet shows representative raised/low held items plus textured placed Castle Stone without opaque inventory-card backgrounds |
| T93 | P3G | Shift+Click moves the maximum compatible amount from inventory to Furnace and back without loss or duplication |
| T94 | P3G | Auto-load target independently fills scarce/available input and fuel, lowers transactionally and conserves exact totals |
| T95 | P3G/P3I | Per-item progress reaches a measurable midpoint, deposits one retained output, resets when the next loaded batch begins, and the Coal that funded it stays in Fuel marked burning with one operation left |
| T96 | P3G | Furnace progress advances while its modal is open (the session advances it: the world runs under menus since 2026-09-22) |
| T97 | P3G | Placed Catapult renders a distinct four-wheel chassis, axle, frame, throwing arm, basket and projectile without changing footprint/combat identity |
| T98 | P3G/P3I | Rendered evidence shows the auto-load/progress panel, the highlighted selected ore tile with its selection message, and the revised placed Catapult; the crafting modal (furnace and workbench) ends inside the 1280×720 canvas (clear button and message bottoms ≤ 720) |
| T99 | P3H | Named catalogue values drive the active Furnace, harvesting and defense services |
| T100 | P3H | One Coal funds exactly three outputs; residual work survives restore and no fourth output is free |
| T101 | P3H | W rotates clockwise, R counterclockwise and Left Shift remains Interact |
| T102 | P3H | Rendered evidence shows the counted fuel ratio/stored work and separate W/R Keybind rows |
| T103 | P3H.1 | Both rendered Workbench pages keep every complete item silhouette inside its own recipe card without neighboring fragments |
| T104 | P3H.2 | Exported three-panel evidence shows the enlarged ready Sword, broad active strike travel and doubled/higher low-held Furnace frame; the active 1536×1024 atlas is RGBA with alpha-zero background |
| T105 | P3H.3 | Placed Gate Frame and Wall Walk Slab mesh parts resolve the Castle Stone texture instead of a flat material |
| T106 | P3H.3 | Exported evidence visibly shows the Castle Stone skin on the placed Gate Frame and Wall Walk Slab |
| T107 | P3I | A Furnace holding input and fuel starts without a manual press on the next unpaused tick, never while paused, processes every input one at a time into retained Output, returns to idle when the input is spent, keeps the Coal in Fuel while the last job it funds is still running (0 operations left, burning) and removes it only when that job completes, round-trips the lit Coal through snapshot/restore (records without `furnace_fuel_burning` restore sensibly), and counted +N transfers move min(N, carried) |
| T108 | P3J | A right-drag plans a row, column or wall of the held block with support-first ordering, extends into open sky along the row's vertical plane, skips blocked cells, trims to the carried count, commits as one world edit plus one inventory transaction, and cancels with nothing built |
| T109 | P3J | Rendered evidence shows a multi-cell drag-build ghost stretched from the anchor toward the aimed cell |
| T110 | P3K | The runtime blueprint catalogue exposes every generated piece, every block resolves to a placeable item, and a quarter turn rotates a wall from x onto z |
| T111 | P3K | Foundation, tower segment and cap stamp on top of each other through top sockets, each as one world edit plus one inventory transaction across castle stone, stone and planks |
| T112 | P3K | A blueprint short on one block type trims only that type's cells, skips an occupied cell, cancels with nothing built, and otherwise stamps every affordable cell |
| T113 | P3K | Rendered evidence shows a stamped foundation, segment and cap tower of ordinary blocks and a multi-block blueprint ghost |
| T117 | P3K | Stamped pieces are remembered with their sockets, a piece aimed within one cell of a socket snaps to it, an aim far away does not, and the stamp list round-trips through the session snapshot while an unknown piece is refused |
| T118 | P4a-1 | A catapult turned away from the raider swings its turntable to face it at the turn rate before firing, the arm whips forward on the shot and winds back to rest over the reload, and the bucket stone reappears only when reloaded |
| T114 | P3I | Furnace select-then-add without the recipe book: clicking an ore or Coal tile highlights it and the message names its slot; Raw Input / Fuel handler presses add +1 and the Shift gesture adds +5; a wrong-slot press is refused with a message; the recipe is inferred from the staged ore so the Load ×1 button and slider are enabled and Load adds one input plus the Coal owed to the staged input (never returns items) |
| T115 | P3I | Visual: real viewport mouse presses on the ore tile then on Raw Input add one ore and a Shift+click adds up to five more through `CraftingItemSlot` input handling (nothing swallows the plain click) |
| T121 | P4a-2 | Right-clicking a placed Catapult opens the weapon panel in the crafting shell (title, empty Ammunition slot, two-row munition legend); select Flame Shot then click the slot loads 1, the Shift gesture loads up to the capacity, a full weapon / mixed munition type / non-ammunition are refused with readable text, Unload returns every shot and disables when empty, Hold / Fire at will and the raider target filter reach `siege_status`, the supply readout reports "no chest" and then names a chest with distance and count once Stone Shot is stored within the supply radius, drag and double-click load as many as fit, Escape closes |
| T122 | P4a-2 | Right-clicking a placed Chest opens a 3 × 3 tile grid with a "N/9 slots used" header; select-then-click stores 1 and Shift stores 5 into one stack, a plain click on a chest tile takes 1, dragging an inventory stack stores all of it, Shift on the chest tile takes the whole stack back, an empty tile explains itself, and totals are conserved |
| T130 | P4C | Cannon, turret catapult, rail, kettle, cannonball and hot oil are registered with icons; hot oil smelts from logs at the Furnace; the kettle is a `dump` weapon with a rail speed |
| T131 | P4C | A cannon places on 2×3 ground, launches from its barrel muzzle (`SiegeMuzzle`) and one cannonball deals 14 |
| T132 | P4C | A turret catapult mounts on a tower platform's `light_siege` socket, shares the catapult arm animation nodes, lobs stone shot for 9 and the impact tears the grass under the raider to dirt |
| T133 | P4C | Six rails chain along a wall top; a kettle is refused off-rail (`INVALID_MOUNT`) and accepted on a rail (`rail_mount`); it reports `TARGET_TOO_FAR` before riding, rides the chain to the rail above the raider, dumps hot oil that lights the ground and burns the raider |
| T134 | P4C | The remodelled ballista shows a drawn slider with a bolt while loaded and strings spanning tip to nock; the bolt hides once unloaded |
| T135 | P4D | A wave drill spawns four raiders (one brute) twenty cells out; splash damages every raider in radius; the nearest raider is targeted; the drill is won only once the whole wave is down; the snapshot records the wave size |
| T141 | P4D | A running wave (three raiders, one brute at 25, the lead at 11) checkpoints and a clean-process Continue rebuilds every raider with its kind, health, wave size and spawn distance |
| T142 | P4D | A 28-cell wave line over natural terrain (outside the flat clearing) spawns every raider on the surface of its column, the service waits for terrain that is still streaming (`WAITING_FOR_TERRAIN`, retried every 0.5 s) and each raider routes toward the core |
| T143 | P4D | A walled-in raider sets out to break the wall itself (voxel breach: castle stone falls after 45 hits at a third damage), switches to a chest plugging a newly opened gap as the cheaper breach, destroys it and routes on to the core; siege machines, chests, rails and kettles are breachable structures with integrity |
| T147 | P4D | Hybrid rule: a brute within 2.6 cells of a player-built breachable structure (chest, machine, rail, kettle, barricade) turns on it and hits it while plain raiders keep their core route |
| T144 | P4F | A wave of three (orc, brute, troll) builds the orc with two cleavers (`OrcSwordL/R`) and a pauldron, the brute as the same orc with `Model.scale` 1.25, the troll with `TrollCrossbow`, `TrollQuiver` and `TrollTopknot` and no swords; ≥ 40 mesh parts each; one direct collision shape per body; entries carry brute 40/10, troll 28/5 ranged (9 cells, 2 s) and the lead orc 20/6 |
| T145 | P4F | A troll 14 cells from the core keeps routing without shooting; at 6 cells it stops, engages the core (`attacking_core`), fires a visible `TrollBolt` and costs the core 5 per shot every 2 s while the drill stays ROUTING |
| T146 | P4F | Visual: an orc, a brute (scaled purple orc) and a troll with a crossbow render side by side about four metres in front of the camera in one 1280×720 view (`p4-enemy-units.png`) |
| T148 | P4G | Every core and light entity (`core_of_power`, `enemy_core`, `torch`, `wall_lantern`, `post_lantern`, `campfire`, `light_block_blue`, `light_block_red`) carries `attributes` (value/role/light/mount/space/notes) with a light, a ghost visual, navigation and defense; the seven craftable ones are Workbench recipes ordered 197–203 after the chest; the enemy core is a hidden item with no recipe and never appears in the book; all eight card icons resolve (`ItemIconCatalog.missing_item_ids` empty) |
| T149 | P4G | All eight entities place on levelled ground with exactly one `EntityLight` OmniLight3D of the attribute colour and range; the Core of Power owns its 3×3 base and centre column (12 cells) and nothing else; `defense_status` reports 240 / 400 / 4; a torch over air is `UNSUPPORTED` while a torch on a light block places; the torch light flickers within 0.5 s and the wall lantern's energy holds |
| T150 | P4G | Visual: the blue Core of Power, the red enemy core, the campfire, the post lantern, the wall lantern, the torch and both light blocks render with their eight lights at 20:30 in one 1280×720 view (`p4-assets.png`) |
| T151 | P4E | The enemy base lies 150–200 cells from home; a far wave spawns there with long surface routes (SurfaceRouter), marches ghost-walking over unloaded ground, and hands over to the local planner inside the defended area |
| T152 | P4G | The drill centres on the placed Core of Power (240 integrity), routes raiders to a free cell beside its 3×3 footprint, mirrors hits onto the station and fails when the core is destroyed |
| T153 | P4H | A sword swing lands on the raider in front of the player (swing ray on layer 1\|4, then the reach cone), provoking it: it chases and hits the player (health drops, HUD shows HP); attention lapses back to the core; a machine that hurt it is chased and hit; the player respawns at full health beside the core |
| T154 | P4C | Aiming down at a rail places the kettle on the cell above it (the placement aim stops at entity-owned cells, not only solid voxels); Patrol is a rail-weapon-only stance that rides the connected chain end to end while idle |
| T155 | P4G/P4C | A wall lantern hangs on a wall face (mount `wall`, bracket toward the wall) and refuses bare ground while a torch stands or hangs; a kettle rides through a rail corner as one track but never turns onto a T branch; a stuck raider backs up and steps sideways before re-planning |
| T160 | Coaster | `rail_slope` (slope 1, not linear), `rail_loop` (loop tool, no support) and `mine_cart` (rail speed 3, `rail_mount`) are registered with icons and CoasterCraft Shop recipes at orders 204–206 (4 slopes, 2 loops, 1 cart) |
| T161 | Coaster | Two flat rails, a slope rising +x and three rails one level up chain as six cells (the slope joins both ends); an upper rail beside a lower one without a slope stays alone; the kettle router steps up through the slope; the slope model has posts, deck, rails and ties; a mine cart mounts on the first rail, rides to the top rail and comes back down |
| T162 | Coaster | With Rail Loop held a press ghosts a complete size-4 loop element (six switcher pieces, two slopes, the circle pieces); 6 by key adds pieces, C makes 7, X makes 6, 12 clamps to 9, every ring piece rides the true circle; release lays every piece for one item as one chain from the entry through both slopes and the circle's top to the exit two lanes over; every ring piece draws the loop track and leans toward the loop's centre; a cart from the approach reaches the top and then the exit; a ghost over a stone column shows red and lays nothing; the laid track survives a save round-trip; L switches to the TRUE loop: a diameter-8 helix touching the ground only at its entry and exit one lane right, Shift-drag to 14 grows it, a cart rides the approach over the top and out on the exit lane |
| T163 | Coaster | Visual: a size-6 loop element renders as a diameter-6 true loop with a mine cart on it, beside a slope run climbing a step with its own cart and a plain-rail corner, in one 1280×720 view (`coaster-rails.png`) |
| T164 | Coaster car | `coaster_car` is a stackable (2) building item placing a rail-mounted cart entity (rail speed 3, role `rail`) with an owner-art icon and a CoasterCraft Shop recipe at order 207 (4 planks + 2 iron ingot + 1 castle stone → 1) |
| T165 | Coaster car | A coaster car placed on a loop's lead-in stays parked; Shift aimed at it (interaction raycast) boards: player parked, ride camera current, seated hero on the `Seat` node, HUD `RIDING · speed 3/9 · 1-9 speed · Shift leave`; speed 2 then 6 move the car 2.0 / 6.0 cells per second; the `hotbar_5` action sets speed 5 without changing the hotbar slot; `interact` leaves the player 1 m beside the parked car on the rail cell's floor; a mid-ride snapshot saves the player beside the car |
| T166 | Coaster car | The hero model has hair, face, tunic, lion crest, scarf, belt, bracers, boots and a sword; armoured it swaps to breastplate, pauldrons with shoulder diamonds, tabard, greaves and vambraces; walking swings legs and arms; seated hides the legs and puts the arms on the bar; V moves the camera 3.5 m behind and above with the hero visible and the aim ray still from the eye; the pause-menu Hero: Armour toggle persists in `settings.cfg` and re-dresses the hero |
| T167 | Coaster car | Visual: the hero sits in the coaster car climbing into a loop, seen from the three-quarter chase camera, in one 1280×720 view (`coaster-car.png`) |
| T168 | Coaster | Rail Switch (smooth, 2026-09-20): a right-press with `rail_switch` held starts the `smooth_bend` drag at the aimed entry heading the way the player faces; the default ghost is the 4-long S-bend to one lane right (`rail_loop` curve pieces, exit 4 on and 1 over, all green); release lays every piece for one Rail Switch each (`BEND_PLACED`); with four rails before and four after on the new lane one chain runs end to end; a rail beside the entry on the exit lane stays alone; the entry and exit ride at rail height and draw the curve track; a mine cart crosses to the far rail and back |
| T176 | CoasterCraft | `rail_climb` (climb tool, no support, Workbench 212 → 8, icon) is registered; with Climb held a right-press starts the climb drag at the aim; the ghost is a complete climb of `rail_loop` curve pieces from the entry to a landing 8 ahead and 4 up; Shift-aiming at a 6-high column 12 ahead sets length 12 / rise 6 (landing on its top) and aiming into a pit sets length 10 / rise −3 (a descent, all pieces ok); 6 by key, X and C set the rise; a 20-item pack caps the length; release lays every piece for one item each; plain rails join the entry (behind) and the landing (ahead, 4 up) in one chain, both ends ride at rail height, the pieces draw the loop track; a mine cart rides up to the rail past the landing and back home with its up vector never dipping below 0.5; a ghost through a stone column shows red and lays nothing; the laid climb survives a save round-trip |
| T177 | CoasterCraft | The mountain: a climb of length 13 / rise 6 from the plate lands on top of a 6-high stepped stone bank, three plain rails cross the top, a descent of length 10 / rise −6 lands back on the plate, and with a rail at each end the whole run is one chain; a mine cart rides up, across, down to the far rail and back home with its up vector never dipping below 0.5; a climb of rise 2 into the bank ghosts red cells and lays nothing |
| T178 | CoasterCraft | Visual: a climb of length 12 / rise 6 renders with rails at both ends and a mine cart part way up, seen from the side, in one 1280×720 view (`coaster-climb.png`) |
| T170 | CoasterCraft | `rail_switch` (Rail Switch = the smooth lane switcher, coaster_tool bend, order 208, 8 per craft, stack 64, smooth-S icon; no `rail_bend` item / entity) ghosts an S-bend of `rail_loop` curve pieces from the entry to one lane right four cells on (default 4 x 1); Shift-aim 10 ahead and 3 left sizes it (length 10, lanes −3, exit there); C / X make 11 / 10; the pack caps 40 to what 24 items pay for; a stone block on the path shows red and release lays nothing; release lays every piece for one item each; rails behind and ahead join into one chain while a rail beside the entry on the exit lane stays out; entry and exit ride at rail height and the middle between the lanes; the entry draws the loop track; a mine cart rides through to the far rail and back; save round-trip |
| T171 | CoasterCraft | `rail_cross` (Crossing, coaster_tool cross, order 210, 4 per craft, stack 32, icon) ghosts two S-bends of one length whose lanes swap (A: lane 0 → 2, B: lane 2 → 0, eight cells) and lays them for one item per piece; the shared middle cell is one record with two curves and two joint pairs joining both tracks (kettles ride it as A); with rails at all four ends everything is one chain; a cart entering on A passes the shared cell and leaves on A's exit, never touching B's entry or exit, and comes home on A; a cart entering on B leaves on B's exit; the shared cell draws two track node sets over one collision box; save round-trip |
| T172 | CoasterCraft | Visual: a six-long Rail Switch (smooth) and an eight-long two-lane Crossing render with lead-in / exit rails and a mine cart riding each, the shared cells drawing both tracks, in one 1280×720 view (`coaster-smooth.png`) |
| T173 | CoasterCraft | `rail_curve` (curve tool, no support, icon, recipe 211 → 8) is registered; with Curve held a right-press ghosts a flat 90° arc of `rail_loop` pieces bending right with radius 4 (entry heading +x exits 4 on and 4 right, heading +z); Shift-aim straight right snaps 135° at half the distance, ahead-left a mirrored 90° LEFT bend, ahead 45°, behind 180°; 5 by key, C 6, X 5, 30 caps at what 40 curves pay for; release lays every piece for one item each; three rails behind and three beyond the exit chain through it with flush ends; the pieces draw the curve track and lean toward the centre by the bank; a cart rides to the far rail leaning into the bend and comes home; a stone block on the arc shows red and lays nothing; the laid curve survives a save round-trip |
| T174 | CoasterCraft | A 180° curve of radius 3 entered heading +x exits 6 cells to the right (2R) heading −x and joins three rails at each end; a cart rides to the far rail and home; a 45° curve of radius 6 ends on the diagonal (−x, −z) cell, which a plain rail cannot join; a Curve pressed on that cell takes the diagonal heading and its 45° bends to −z, and the two 45s with rails behind and beyond chain as one track a cart rides to the far end |
| T175 | CoasterCraft | Visual: a radius-4 90° curve with a mine cart on it, a radius-3 U-turn and an S of two radius-5 45s render with rails at their ends in one 1280×720 view (`coaster-curves.png`) |
| T179 | Coaster | Track auto-clear off: a diameter-8 loop ghost over a stone, a water and a castle-stone cell shows exactly those three red and release lays nothing; the pause-menu toggle turns it on, writes `[track] auto_clear` to settings.cfg (a fresh store reads it back) and the session mirrors it; on: the stone cell alone is amber, water and castle stone stay red and the loop is still refused; with only the stone in the way release mines it (+1 stone, reports cleared 1) and lays every piece; a rail line through a dirt bump shows the bump amber, mines it (+1 dirt) and lays four rails |
| T180 | Coaster | Every lower piece of a laid diameter-8 true loop (1-3 cells up, not over another piece of the loop) carries a `Support` node whose post reaches the plate; the top row has none; a piece within two cells above another piece of the same loop has none |
| T181 | Coaster | Visual: a straight of `rail_loop` pieces laid five cells up (`TrackCurve.make_line`) grows a post per piece down to the plate and the true loop's lower pieces carry posts; both render in `coaster-supports.png` |
| T182 | Coaster | With the build orientation East, a Climb right-press from east of the entry looking west starts a climb heading west at the aimed cell; Shift-dragging 8 cells east turns it east (length 8, every ghost cell east of the entry); a Loop pressed the same way heads west and a Shift-drag 6 cells south turns it south at diameter 6; a Bend heading east Shift-dragged 6 cells behind its entry flips west with length 6 |
| T183 | Coaster | The middle piece of a climb (length 12, rise 6) laid on a levelled plate carries a `Support` bent with two legs (`Post`, `Post2`) reaching the plate, at least one `Tie`, at least two `Diagonal` braces and at least one `Stringer` whose centre lies between this bent and the next piece's bent |
| T184 | Coaster | Pressing U removes every piece of the climb just laid (records and visuals) and hands back one Rail Climb per piece, popping one undo entry; a single rail placed then undone is gone with its rail back; a rail line that auto-cleared a dirt bump, undone, loses its rails, the bump is back and the mined dirt leaves the pack |
| T185 | Coaster | On a levelled plate a plain-rail L corner draws a round arc (no `RailArms` plate, at least 5 rail sections), no `rail` / `rail_slope` in a rail-slope-rail run carries the old stone posts (0.22×0.56×0.22 / 0.22×1.36×0.22) or oak deck, and across the corner, the slope run, a rail-curve-rail run (radius 4) and a rail-loop-rail true loop (diameter 6) every joined pair's rail ends meet within 0.02 (`rail_end_gap`) |
| T186 | Coaster | On a levelled plate a 4×1 Smooth Switch between plain rails leans (`TrackCurve.up_at`) within 1° of upright at t 0, 0.02, 0.5 and 1, peaks between 3° and the full bank (28.3° for bank 0.35) and never steps more than 2° per 0.01 t; a radius-4 Curve leans the full bank at its middle and none at its ends; at every joint of both runs the two pieces' end rings coincide (point within 0.02, across within 2°, `rail_across` meta); every piece's rails and spine are one swept `ArrayMesh` of at most three surfaces with no rail boxes. Visual: `coaster-sweep-crossing.png` (the crossing's centre with a plain-rail corner in front) and `coaster-sweep-uturn.png` (the radius-3 U-turn) close up |
| T187 | Coaster | Three plain rails heading +x on a levelled plate report exactly two `CoasterRails.open_ends` (west of the first, east of the last, `along` out of the run); a Rail Curve right-press aimed one cell beside and one short of the run's free cell snaps (`drag_state().snap.instance_id` = the last rail, anchor = the free cell, rotation 1, a `SnapHint` under the drag preview); aimed three cells away the snap and hint clear; aimed back it re-snaps and `commit_drag_place` lays the curve (`CURVE_PLACED`) chained to the run, and a cart from the first rail rides through the curve past its exit |
| T188 | Coaster | Auto-shape (owner playtest 2026-09-21): plain rails laid through the interaction service shape themselves. (a) A rail line (−60..−57, 40) and single rails (−57, 36..39): the corner stays plain until the straight beyond it exists; that rail turns the three corner cells (A, C, B) into `rail_loop` pieces of one radius-1.5 arc (`auto_shaped` = elbow) starting on A's far face and ending on B's far face at height 0.55, the straights beyond stay plain, the chain runs (−60,40)..(−57,36) as 8 cells, the pack's rails drop by the one laid, every joint's rail ends meet within 0.02, a mine cart rides round the corner and home, the kettle router (`_rail_step`) steps A2 → A → C → B with its rail point on the corner's arc; `undo_last` restores the three plain rails (`replaced` 3), removes the triggering rail and refunds it. (b) Three rails in lane 44 then three in lane 45 one cell on become one six-piece `s_bend` (`auto_shaped` = lane_shift) from (−60,44)'s west face to (−55,45)'s east face that a cart crosses and returns. (c) A "+" of two rail lines stays nine plain rails. Visual: `T188_AUTO_SHAPE_RENDERED` renders an elbow and a lane shift with a cart on each in `coaster-auto-shape.png` |
| T189 | Coaster | A climb (12 / 6) crossing a plain-rail line under its middle: the three pieces over the line and beside it carry no Support (a hole in the truss); the pieces either side of the hole keep their bents |
| T190 | Coaster | Deselecting the held slot leaves no active item (slot −1); Y drops one rail ahead of the player (the pack loses one, one drop exists, the session snapshot lists it); standing on the drop picks it up; Shift+Y drops the whole stack and standing on it takes it all back |
| T191 | Coaster | CoasterCraft mode ([contract](COASTERCRAFT_MODE.md)): main menu CoasterCraft → New opens a session on the mode's own coordinator (`<data root>/coastercraft`, fixed seed) with a bare 60 × 100 stone plate (stone at y 0, air above across a 5-cell sample grid, no stations), the player on it, creative and the stock filled in hotbar order, the drill line and enemy base hidden, no enemy core; its pause menu has Resume / Save / Save and Restart / Save and Exit / Save and Quit and no drills; Save keeps the session (revision 1, rail kept, streaming back); Save and Restart opens a new bare plate (revision 2); Save and Exit to Menu (revision 3, the game's slot untouched) then Continue restores the rail placed on the second plate. Same assertions standalone: `--coastercraft-check` |
| T192 | Industry | CoasterCraft Shop (gate): the shop's book lists exactly `rail`, `rail_slope`, `rail_loop`, `mine_cart`, `coaster_car`, `rail_switch`, `rail_cross`, `rail_curve`, `rail_climb` in that order; the Workbench book lists none of them, still lists the kettle and ends with `coastercraft_shop` (order 213, 4 iron + 3 planks + 2 sticks → 1); the entity is a 2 × 1 `coastercraft_shop` station with an icon; a placed shop (`ShopCart` visual) opens the crafting modal titled COASTERCRAFT SHOP on one page, crafts 4 rails and the Workbench refuses the rail recipe (`WRONG_WORKSTATION`). Visual (`T192_COASTERCRAFT_SHOP_RENDERED`): `p3f-coastercraft-shop-book.png` and `p3f-coastercraft-shop.png` |
| T193 | Industry | On a levelled plate a `miner` beside an `ore_bin` drills the three iron ore voxels within radius 3 into the bin, one per `MINER_SECONDS` (`advance(MINER_SECONDS + 0.1)` three times → 1, 2, 3 Iron Ore; the voxels become stone; an ore 5 cells away stays); a fourth tick changes nothing and the status is "no ore"; the right-click line reads "Miner: 3 ore mined, no ore." plus its hint; the bin shows its `OreHeap`; a bin-less miner beside ore reports "no bin" and leaves the ore; a JSON save round-trip through `WorkstationService.restore` keeps the bin's 3 ore, 9 slots and the miner's count. Content: recipes 214 / 215 at the Workbench, measured icons |
| T194 | Industry | A Warehouse (27 slots) stocked with 3 Iron Ore, 3 Coal, 2 Gold Ore and a Foundry touching it (target Any): three `FoundryService.advance(job + 0.1)` calls (iron 5 s) yield 3 Iron Ingots in the Warehouse and no Gold Ingot (iron first), the mouth `Glow` lit meanwhile and the crate stack shown; the status then reads `no fuel`; a Foundry with no storage reports `no storage`; target Gold Ingot with 2 Coal added yields 2 Gold Ingots (gold 8 s, made 5); a `WorkstationService` snapshot restore keeps the target, the counter and the Warehouse contents; the FOUNDRY modal shows the three slot cells, the inventory column, three target buttons (Gold Ingot pressed), "made 5" and closes on Escape; the Warehouse opens the Chest panel titled WAREHOUSE
| T195 | Industry | Hauling (`docs/INDUSTRY.md`): a mine cart on a 12-cell straight loads 10 iron ore from an `ore_bin` beside cell 3 (HUD "Cart loaded 10 iron ore", the `Cargo` heap shows) and unloads them into a `warehouse` beside cell 9 (HUD "Cart unloaded 10 iron ore", heap hidden); from a bin holding 20 it takes `CART_CARGO` = 16 and 4 stay; a mid-haul station snapshot round-trip keeps the 16; the warehouse ends with 26; a coaster car riding the same straight hauls nothing |
| T200 | Industry | Storage network (`docs/INDUSTRY.md`): a Warehouse with 70 Iron Ore + 70 Coal beside a Foundry: one `FoundryService.advance` → Ore / Fuel slots 64 / 64, the Warehouse keeps 6 / 6; one iron job (`job_seconds` 5 + 0.1) → one Iron Ingot pushed into the Warehouse, slots 63 / 63; the state carries status / elapsed / working; 2 s on, `job_status` is active (> 30 %) and the FOUNDRY panel (three furnace-style cells, inventory column, no auto-load slider) shows the progress bar with `value` > 0 and a time label; the slots topped back up (Warehouse 5 / 5); `try_collect_furnace_stack(foundry, "input")` hands 64 ore to the pack and the next tick refills 5 from the Warehouse; a `WorkstationService` restore keeps the slots |
| T201 | Industry | A chest beside a warehouse beside another warehouse: `StorageNetwork.network_for` from a cell touching the first lists all three, the first first, and not a chest three cells off; `put` of 10 Iron Ore into a full first warehouse (27 × 64) overflows into the others (remaining 0); `take` drains 1738 across them to 0; `room` counts all three |
| T202 | Industry | An empty catapult beside a chest holding 5 Stone Shot and an empty ballista beside a chest holding 3 Ballista Bolts: one `SiegeDefenseService.advance(SIEGE_AUTO_RELOAD_SECONDS + 0.1)` loads 5 Stone Shot and 3 bolts from them (chests empty, two `storage_reloaded` "reloaded from storage" lines); a cannon beside a chest of Stone Shot stays at 0 and the chest keeps its 4 |
| T196 | Pathing | On its own levelled plate (core of power at (30, 0, 71), spawn (31, 0, 59)) a one-block castle-stone ledge spans the whole approach: the raider's route is `OK`, the step-up assist fires at least once (`BasicRaider.step_ups`), its feet stand on the ledge top (y 1) and it reaches the core (`attacking_core`) within 30 s |
| T197 | Pathing | On a second plate a raider stands in a 3×3 castle-stone pocket (2 high) whose only gap a second raider stands in, frozen (parked without progress); both route `OK`; the progress watchdog re-plans the frozen one at about 6 s (`active` again) and both bodies have left the pocket box within 20 s |
| T198 | Pathing | On a third plate a raider boxed in by bedrock (unbreachable) starts with `NO_PERMITTED_ROUTE`; the drill widens its capture at about 6 s (`_capture_wide`), marks the raider stalled (`last_route_reason` = `STALLED`) between 19 and 22 s, `stall_warnings` rises by exactly one, and because the wave is otherwise over the raider retires (body dead, 0 living) and the drill ends `WON` |
| T199 | Industry | Live under menus (`--p3e-container-automation=gate`): with the world live, a furnace job (`remaining_seconds` set to 3) started on its own furnace finishes (an Iron Ingot in the output) and the day clock advances while 4 s of real frames pass with the inventory open, again with the Workbench modal open and again with the hand-build modal open (each opened through the app's own path: `menu_open`, tree not paused, player body inactive, the modal still open after the 4 s, closing returns to PLAYING with the body active); `_pause_game()` stops the clock for 1 s and `_resume_game()` releases it; a core-defense raider (`start_prototype`, warning skipped) closes on the core by more than 0.5 cells over 2 s with the inventory open |
| T210 | Expo | Lost-Core Continue ([contract](DEVELOPMENT_EXPO.md), `--f3-automation=phase1`): in slot B a Core of Power is placed on a levelled patch, a core-defense drill is started on it, a chest is placed and the ground under it dug away, then the core is smashed (`DESTROYED`, drill `failed`) and the game saved. The main menu reports the slot as `Game Over — Start New` (Continue disabled, the slot line says GAME OVER, Start New still offered), `GameSession.game_over_report` agrees, Development Start still opens and the checkpoint is still readable. Continue through the app's own `_open_session(true)` reaches PLAYING within 3600 frames (never the endless loading screen) with the drill restored as lost, no re-created core and the unsupported chest back in place |
| T211 | Expo | Development mode isolation (`--development-check`, asserted in-process as `T211_DEVELOPMENT_MODE_ISOLATION`): a complete normal Slot A checkpoint carrying a marker is written before the mode is opened; after Development Start → New and a Reset Expo, Slot A's marker and revision read back unchanged, the CoasterCraft namespace still has (or has not) exactly the save it had, and `DEVELOPMENT DATA_ROOT` is `<data root>/development`, outside the game's slots root. The same run checks the submenu gating, the session flags and fixture version, no scheduled wave or enemy core after three seconds of play, the hidden drill line and enemy base, the daytime clock with the cycle running, no creative top-up, the builder and reset-group seams, the drill-free pause menu, the Reset Expo confirmation, Save and Exit and Continue |
| T136 | P4C | Visual: the ballista, catapult, turret catapult on its tower, cannon and kettle on a rail-topped wall render as distinct multi-part machines in one 1280×720 view (`p4-siege-units.png`) |
| T123 | P4a-2 | Visual: rendered 1280×720 evidence of the weapon panel (loaded Flame Shot, stance, filter, supply, legend) and the Chest panel; real viewport clicks on a munition tile then the Ammunition slot (and on the chest grid) route through `CraftingItemSlot`; message, supply, legend and chest-grid bottoms are ≤ 720 |
| T137 | P4b-1 | Sampled columns of the seeded generator show coal from depth 3 (above 6), iron only from depth 6, gold only from depth 12, gold rarer than iron rarer than coal, identical for the same seed and different for another |
| T138 | P4b-1 | Every non-gold cell matches the pre-table P1 formula (iron roll [0,18) from depth 6, coal [18,55) from depth 3) and gold only replaces stone at depth ≥ 12, so `terrain_p1_1` saves keep their layout |
| T139 | P4b-1 | A generated Gold Ore cell refuses the Stone Pick (`WRONG_TOOL`), breaks under the Iron Pick into one Gold Ore item, and block 11, items, texture and measured gold icons are registered |
| T140 | P4b-1 | Two Gold Ore and one Coal auto-start the 8 s `gold_ingot` Furnace recipe and yield two collectable Gold Ingots after two durations |
| T212 | Expo signs | Sign placement and editor (`--p3d-usability-automation=phase1`, `docs/SIGNS.md`): a sign places on the ground (mount `ground`, post) and on the side of a block (mount `wall`, no post), a second sign on the same anchor is refused `OCCUPIED`; the two-cell **wide board** (`sign_board`) places on the ground and on a wall too, reserves both of its cells, is refused `OCCUPIED` when only its second cell is taken, and carries its content through the same save round trip; `configure_sign` stores each of the four display modes with its text and items and rejects an unknown mode or item id; eight item ids plus the mode and text survive `snapshot()` → JSON → `restore()` exactly (a record without the `sign` block migrates to an empty text sign); right-click through the app's own `_show_workstation` opens the SIGN panel with the record's mode, text, eight slots and a built category strip + icon grid, picking an item into slot 1 and saving rewrites the record, and Close returns to PLAYING |
| T212 | Expo signs | Visual (`--p3d-usability-automation=visual`): rendered 1280×720 evidence `p3d-sign-item-grid.png` of a placed ground sign configured Header + Item Grid — the "SUPPLY DEPOT" heading and eight item icons with readable names in 4 rows × 2 columns, read at player distance |
| T212 | Expo signs | Visual (`--p3d-usability-automation=visual`, record `T212_SIGN_WIDE_PRESENTATION`): rendered 1280×720 evidence `p3d-sign-wide-board.png` of the two-cell wide board configured Header + Item Grid — the heading "DEVELOPMENT EXPO SUPPLY DEPOT" laid out on whole words (asserted against the rendered label, not only the record: no line is a fragment of a word) over the same eight-item 4 × 2 grid of icons and names, read from about four metres |
| T213 | Development Expo | Layout (`--development-expo-automation=gate`, record `T213_EXPO_LAYOUT`): `contracts/development_expo.json` parses, every referenced item/entity id exists in the content registry, no two districts, expansion corridors or exhibit parcels overlap (a `nested` parcel such as the mine tunnel is exempt and must lie inside its host), every district has an expansion corridor at least one avenue wide and flush against it, every reserved parcel is empty and signed, the chunk-aligned world bounds computed from the layout plus the expansion margin (`(-160, -16, -96)` size `(384, 48, 320)`) hold every parcel plus that margin, and two loads of the same manifest solve to the same fingerprint |
| T214 | Development Expo | Western campus (`--development-expo-automation=gate`, records `T214_EXPO_PLAZA_AND_DAY_ONE` and `T214_EXPO_MOUNTAIN`): after a Development New the plaza is solid and clear (feet and head) across a sampled grid around the spawn, the development Core of Power stands on it, all eleven Day One exhibits are built in chain order with the Workbench and Furnace standing; the Mining Mountain's authored ore core holds coal, iron and gold voxels, a straight 56-cell walk down the tunnel is unobstructed, floored and within 12 cells of a light the whole way, the 64-cell rail line inside chains cell by cell and the automated-mining Miner and Ore Bin stand. `--development-expo-automation=visual` renders `development-expo-plaza.png` and `development-expo-tunnel.png` |
| T215 | Development Expo | Expo signs placed (`--development-expo-automation=gate`, record `T215_EXPO_SIGNS_PLACED`): after a Development New every district and exhibit sign the manifest asks for is a real placed `sign` station (card B) — each request carries the instance id of a standing `sign` whose `sign_data` holds exactly the block the manifest asked for (display mode, title in `text_a`, body lines in `text_b`, the named item id), nothing is left in `pending_signs()`, and the plaza orientation sign and the Day One district sign are read back field by field against `contracts/development_expo.json` itself |
| T215 | Development Expo | Visual (`--development-expo-automation=visual`, record `T215V_SIGN_VIEW`): rendered evidence `development-expo-sign.png` of the plaza's orientation sign framed from in front of its board — a real standing oak sign with the manifest's text on it, beside `development-expo-plaza.png` and `development-expo-tunnel.png` |
| T216 | Development Expo | Industry chain (`--development-expo-automation=gate`, record `T216_EXPO_INDUSTRY`): after a Development New the Industry district holds the whole authored chain — a Miner on an authored iron-and-coal face, an Ore Bin beside it and beside the track, one rail line chaining cell by cell from the Mining Mountain's first cell `(-120, 0, -34)` to the yard's last `(-14, 0, -34)` with a Mine Cart on it, a Warehouse at the dock and a Foundry touching it — and every container plus all three foundry slots are empty as built (the gate pauses the simulation while it reads the fixture). Driving the services then runs the chain end to end: 24 Miner ticks put ore and coal in the bin, the cart service hauls it into the Warehouse, and the Foundry smelts an Iron Ingot back into storage. The four storage-network booths (Warehouse + Chest, daisy-chained Warehouses, Foundry touching storage, Furnace touching storage) each report exactly the pooled network their sign claims, read through `StorageNetwork.network_of` |
| T216 | Development Expo | Visual (`--development-expo-automation=visual`, record `T216V_INDUSTRY_VIEW`): rendered evidence `development-expo-industry.png` — the authored ore face with its sign, the Miner and Ore Bin beside the track, and the rail running east past the yard |
| T217 | Development Expo | Lighting gallery (`--development-expo-automation=gate`, record `T217_EXPO_LIGHTING`): after a Development New the Lighting district's roofed pavilion holds all six light sources — Torch, Wall Lantern (hung on the gallery wall), Post Lantern, Campfire, Blue and Red Light Block — each on its own signed parcel with a fulfilled sign request, each a real placed entity whose `EntityLight` `OmniLight3D` carries the colour and range from its content attributes with a positive energy, and each standing under the gallery's roof slab so the difference reads at the fixture's 09:00 clock |
| T217 | Development Expo | Visual (`--development-expo-automation=visual`, record `T217V_LIGHTING_VIEW`): rendered evidence `development-expo-lighting.png` — the shaded walk with the six sources side by side and daylight at the open end |
| T218 | Development Expo | Construction Yard (`--development-expo-automation=gate`, record `T218_EXPO_CONSTRUCTION`): every castle piece — Castle Stone, Stone Stair, Wall-walk Slab, Parapet Merlon, Tower Platform, Gate Frame, Wood Barricade — stands on its own signed booth, and the same pieces stand assembled beside them: a drag-built run of castle stone with a wall-walk deck and merlons on top, the P3K blueprint stack FOUNDATION 4 / TOWER SEGMENT 4 / CAP 4 stamped into ordinary voxels and recorded in `InteractionService.stamps_snapshot()`, and a small castle with a curtain wall, a Gate Frame, a stair and a Tower Platform on its tower; the future-castle-technology parcel is signed and empty. `--development-expo-automation=visual` renders `development-expo-construction.png` |
| T219 | Development Expo | Defense Range (`--development-expo-automation=gate`, record `T219_EXPO_DEFENSE_RANGE`): each of the six booths — Ballista, Catapult, Turret Catapult, Turret Catapult Mk2, Cannon, Kettle on rail — stands its weapon on the mount its content sheet allows (tower platform, rail or ground), with its own munition in a container the existing `StorageNetwork` reports beside it, a castle-stone target down its own lane and a sign naming the weapon and its ammunition; draining the Cannon back to the clip its sheet opens with is topped up again to capacity by the unmodified siege storage-network reload, and the munition leaves the chest. `--development-expo-automation=visual` renders `development-expo-range.png` |
| T220 | Development Expo | Battlefield (`--development-expo-automation=gate`, record `T220_EXPO_BATTLEFIELD`): the Battlefield control pedestal's **START ATTACK** sends a mixed wave of the enemy kinds that exist through the existing `CoreDefenseService` drill at the Battlefield's own Core of Power (not the plaza's); the wave's distance to that Core shrinks by at least 6 cells as it routes in and a player siege weapon engages it (a munition in flight or a clip that dropped); **RESET BATTLEFIELD** then removes every attacker, returns the drill to idle, and restores both cores, the batteries, their ammunition and the magazine through the `battlefield` reset group, while damage done to the plaza Core and to a Defense Range weapon in other districts is left exactly as it was; a second START ATTACK straight afterwards is accepted. `--development-expo-automation=visual` renders `development-expo-battlefield.png` mid-attack |
| T221 | Development Expo | CoasterCraft component gallery (`--development-expo-automation=gate`, record `T221_EXPO_COASTER_GALLERY`): after a Development New every gallery booth stands inside its own parcel with its placed sign — the arrival plaza's post lanterns, a plain Rail run, a Rail Slope between two levels, the true Rail Loop (a `helix` curve), the Rail Switch (a shifted `s_bend`), the Rail Cross (a cell carrying two curves), the Rail Curve (an `arc`), the Rail Climb (a rising `s_bend`), a Mine Cart, a Coaster Car and a placed CoasterCraft Shop — each track family read from the station records' entity and curve data, so a specimen only passes when the real lay tool laid it; the Shop opens the COASTERCRAFT SHOP crafting modal through the app's own `_show_workstation` |
| T222 | Development Expo | Grand Demonstration Coaster (`--development-expo-automation=gate`, record `T222_EXPO_GRAND_COASTER`): the showpiece parcel holds at least one piece of every required family (straight, climb, curve, true loop, lane switcher with its one-lane shift, crossing) read from the records' curves; every track piece in the parcel is reachable from the station rail through `CoasterRails.chain` and the chain holds nothing else, so the circuit is one connected chain through the station; the parked Coaster Car boards through `GameSession.board_coaster_car` (the real `CoasterRide` path), runs at speed 9 until its trail covers every cell of the circuit and it is back on the station rail inside the frame budget, rides a piece of every family on the way, and the rider's up vector (`CartRig.global_basis.y`) never points downward on a cell outside the loop |
| T221 / T222 | Development Expo | Visual (`--development-expo-automation=visual`, records `T221V_COASTER_GALLERY_VIEW` and `T222V_GRAND_COASTER_VIEW`): rendered evidence `development-expo-coaster-gallery.png` (the signed gallery booths side by side) and `development-expo-coaster.png` (the Grand Demonstration Coaster from a vantage that shows the lift hill, the crest and drop and the true loop over the station) |
| T223 | Development Expo | Supply Depot (`--development-expo-automation=gate`, record `T223_SUPPLY_DEPOT`): after a Development New every non-hidden visible item of the content registry is stocked exactly once in the generated depot, eight units of it, in a chest carrying at most eight distinct item types and still holding one free slot of its nine; each chest's sign is a Header + Item Grid whose header is the chest's category label and whose ids are exactly the ids in that chest, in the chest's own order; the walk from the spawn down the plaza avenue and along the depot's front aisle is unobstructed, floored and clear at head height; and a registry carrying an item with no Expo category reports it as `unassigned` (the classifier is driven directly) instead of bucketing it
| T223 | Development Expo | Visual (`--development-expo-automation=visual`, record `T223V_SUPPLY_VIEW`): rendered evidence `development-expo-supply.png` of one Supply Depot chest framed from the aisle in front of it, close enough to read its board's 4 x 2 item grid against the chest below
| T224 | P3D | Flight (`--p3d-usability-automation=phase1`, [Keybinds](KEYBINDS.md)): one Right Shift tap does not start flight; two taps within the double-tap window do; a flying body holds its height with no input, travels along the camera's aim when the movement keys are pressed, rises on Space; a second double tap lands it and gravity pulls it down again |
| T225 | Development Expo | Sign anchors and boards (`--development-expo-automation=gate`, record `T225_SIGN_ANCHORS`): an exhibit's manifest `sign_anchor` places its board where it is read instead of at the parcel's origin corner — the Battlefield's arena sign stands within four cells of the `near:battlefield_fortification` gate, not at the corner of the 60×54 field — the plaza orientation board and the Supply Depot chest boards are the two-cell `sign_board`, a wide board's second cell answers to the same station, and `pending_signs()` is still empty (T215 stays green alongside it) |
| T226 | P4C | Gate (`--p4-siege-units-automation=gate`, record `T226_GATE`, [Defence sets](DEFENSE_SETS.md)): a gate leaf hangs in a gate frame's opening and starts shut; shut it reads to the planner as solid player stone tagged `fortification`, so a raider with no other way picks it as an obstruction at a third of its damage instead of routing through; right-click opens it and the route runs straight through the gate cell; it takes damage and is repaired atomically with castle stone; the open/shut state survives a save round trip; a destroyed leaf leaves the frame standing with the way open |
| T227 | P3K | Wall kit (`--p3k-blueprint-automation=gate`, record `T227_WALL_KIT`, [Defence sets](DEFENSE_SETS.md)): one Wall Kit stamp raises a finished defensive section — sixteen castle stone in two courses, an eight-cell wall-walk, four merlons and a pair of stone stairs up at each end — paid for with the real items in one inventory transaction; an empty pack builds none of its thirty-two cells and a pillar standing in the section blocks its cells; one U takes the section back out, blocks and pieces together, hands every item back and forgets the stamp |
| T228 | P4C | Rail turret (`--p4-siege-units-automation=gate`, record `T228_RAIL_TURRET`, [Defence sets](DEFENSE_SETS.md)): a turret catapult on a rail carriage mounts only on a rail; parked on Hold it stays on its home rail; on Patrol it rides the chain to both ends; it restocks stone shot from a chest beside the rail it is riding rather than only beside its anchor; a raider in range draws it along the line and it throws at it; a patrolling carriage survives a save round trip with its stance |
| T229 | P4C | Gate toggle (`--p4-siege-units-automation=gate`, record `T229_GATE_TOGGLE`, [Defence sets](DEFENSE_SETS.md)): right-clicking a gate through the real interact path (`InteractionService.secondary_press_from_view`, the aim `PlayerController` uses) from a normal standing position toggles it BOTH ways, twice — four presses at one aim give open, shut, open, shut and every press resolves to the gate itself; a shut gate leaves a raider no route and an open one routes straight through the gate cell in the same frame; the state survives a save round trip and the restored gate still shuts on the next right-click. The regression for the owner's "I could open the gate, but not close it": the open leaf's blockers are its only collision, so the aim ray flew past it and no station was ever found to toggle back |
| T230 | P4C | Gate family (`--p4-siege-units-automation=gate`, record `T230_GATE_SIZES`, [Defence sets](DEFENSE_SETS.md)): the Gate (1×2), the Double Gate (2×3) and the Great Gate (4×4) each hang only in the frame built for them and in no other; a frame whose opening is not wholly clear is refused (`OPENING_BLOCKED`); shut, every cell of every leaf reports solid breachable `fortification` and integrity rises with the size; open, every one of those cells is air; the Great Gate's four-wide opening clears a catapult's two-wide footprint with a cell to spare each side and headroom over it, and a real catapult parks inside that span; castle stone lays flush against both jambs |
| T231 | P4E | Gate Expo (`--development-expo-automation=gate`, record `T231_GATE_EXPO`, [Defence sets](DEFENSE_SETS.md)): the Construction Yard stands a signed Double Gate booth and a signed Great Gate booth, each with its own frame and its leaf hung shut, and a catapult parked beyond the Great Gate whose two-cell width sits inside the four-cell opening with room to spare; every gate inside the Battlefield reset group — now that a gate is a family of sizes rather than one entity id — is hung shut again by RESET BATTLEFIELD |
| T232 | P3D | Stacked board (`--p3d-usability-automation=phase1`, record `T232_SIGN_STACKED`, [Signs](SIGNS.md)): the `header_body` mode stores and restores its header, subheader and body across a real save; the rendered labels come back ordered header > subheader > body with none under its readable floor (0.10 / 0.070 / 0.055 m of cap height); a ground board's centre sits 1.65 m above the floor it stands on, on a post whose foot reaches that floor; and a wall board centres at 1.55 m with no post at all. Rendered evidence `T232_SIGN_STACKED_PRESENTATION` (`--p3d-usability-automation=visual`, `p3d-sign-stacked-board.png`): the three-cell district board read from five metres at standing eye height |
| T233 | Development Expo | District boards (`--development-expo-automation=gate`, record `T233_EXPO_BOARDS`, [Signs](SIGNS.md)): every district's entrance board carries the stacked Header + Subheader + Body block on the three-cell `sign_board_large` (a district with no room for three cells falls back to the two-cell `sign_board`, never to a narrow sign), each with a header and at least a subheader, and no manifest sign request is left unfulfilled (T215 and T225 stay green alongside it). Rendered evidence `T233V_DISTRICT_BOARD_VIEW` and `T233V_PLAZA_BOARD_VIEW` (`--development-expo-automation=visual`, `development-expo-district-board.png`, `development-expo-plaza-board.png`): a district board read standing five metres in front of it, eyes at 1.6 m |
| T234 | Development Expo | Expo Directory (`--development-expo-automation=gate`, record `T234_DIRECTORY_SEARCH`, [Development Expo](DEVELOPMENT_EXPO.md) section 9): the raw key **K** opens the Directory panel in a Development world and is refused in the ordinary game with one line; a search for the owner's own words ("wall kit") answers the `cy_wall_kit` exhibit with the description its sign carries and its icon, and the panel draws exactly the rows the filter answers; the district filter returns that district's rows only and still holds the wall kit; the category filter returns that category's rows only; and **What's new** returns exactly the exhibits carrying the manifest's newest `added_in` stamp. `--development-expo-automation=visual` renders `development-expo-directory.png` |
| T235 | Development Expo | Directory teleport (`--development-expo-automation=gate`, record `T235_DIRECTORY_TELEPORT`): selecting an exhibit stands the player within six cells of its parcel on ground that reports `LOADED` and solid, with an empty cell at the feet, facing the parcel's centre - proved for the Construction Yard, Lighting and the far CoasterCraft park, whose chunks have to stream in before the player is put down (the panel says so rather than dropping the player into unloaded ground) |
| T236 | Development Expo | Expo test notes (`--development-expo-automation=gate`, record `T236_EXPO_NOTES`): a remark left with a status is stored against its subject id, a second remark and a status-only change are appended as history (three entries, the newest remark kept), the panel's row shows the newest status and remark, the notes go into the development save's own `expo_notes` namespace and come back unchanged from a real checkpoint on disk, the same session asked for an ordinary game's snapshot writes no `expo_notes` key at all (and the development data root is not the game's), and the export writes `artifacts/expo_notes.md` with the subject / status / newest remark / date table and every historical remark underneath - the same file the headless `--expo-notes-export` switch writes with no world opened |
| T237 | P4C | Spike Trap (`--p4-siege-units-automation=gate`, record `T237_SPIKE_TRAP`, [Traps](TRAPS.md)): a raider standing on an armed Spike Trap takes its 8 damage and the trap disarms for its 6 s reset; a second raider standing on it inside that window is unharmed; the trap re-arms itself on its own clock and springs on the next body; the armed state, the reset clock and the fired count ride in the session save snapshot and restore from it |
| T238 | P4C | Traps are undetected (`--p4-siege-units-automation=gate`, record `T238_TRAPS_UNTARGETED`, [Traps](TRAPS.md)): a raider whose only way through a wall is over a Spike Trap routes straight over it, never names the trap (or the wall) as a target, takes the trap's damage in passing and reaches the core with the trap still at full integrity |
| T239 | P4C | The weakest obstacle (`--p4-siege-units-automation=gate`, record `T239_BLOCKED_RAIDER`, [Traps](TRAPS.md)): a raider sealed in castle stone with no route picks the weakest thing it can reach — the Spike Trap at 24 over the Wood Barricade at 30 and the 90-integrity stone — and really damages it; with the barricade beaten down to 5 the same raider picks the barricade instead. `T239_TRAP_RANGE` (`--development-expo-automation=gate`) is the Expo Trap Range fixture: a lane walled with 33 castle-stone cells a side, nine Spike Traps armed as placed and walkable to the planner, the Core at the far end, the control pedestal beside the mouth, both parcels signed, the future-traps parcel empty and the pedestal's own scenario resolving to this Core rather than the Battlefield's; `T239V_TRAP_RANGE_VIEW` (`--development-expo-automation=visual`) is the same lane mid-attack |

## Performance evidence, not invented guarantees

Use the 128×32×192 world (expanded from the 64×32×128 fixture on 2026-09-19) and a scripted or documented route with at least 100 edits and a save/reload. Record median/p95 frame time, longest edit hitch, memory and save duration. Suggested initial usability targets are 60 FPS with ordinary movement and visible edit response under 100 ms on the designated test PC; these are **provisional targets**, not a tested minimum specification. Record failures and determine cause before tuning or enlarging the world.

## Runtime test layers

Exported-game runners: `tools\runners\TEST_*.cmd` (one per suite; each prepares the export with `tools\start_game.ps1 -PrepareOnly`, runs the suite's gate and visual modes and prints `<NAME> TEST: PASS` / `FAIL`; `<PREFIX>_DIAGNOSTIC_NO_OPEN=1` keeps evidence images closed, `<PREFIX>_DIAGNOSTIC_NO_PAUSE=1` skips the pause; the Development Expo runner is `tools\runners\TEST_DEVELOPMENT_EXPO.cmd`, prefix `EXPO`) and `tools\runners\RUN_ALL.cmd`, which runs them all and prints a PASS/FAIL table (logs in `artifacts\run_all-<stamp>`). They moved out of the repo root on 2026-09-22; the one-click `VIEW_NAVIGATION_SPIKE.cmd` was retired with them (the P2 diagnostic still runs headless: `--p2-navigation-automation=phase1`).

Automate pure inventory/crafting/occupancy rules in GDScript tests; integration-test actual VoxelTerrain edits and save/load in the pinned engine. Add manual Windows tests for pointer, UI, export and failure recovery. Reuse or add a small test runner only when needed; no testing framework dependency has been selected. Port static placement fixtures into runtime tests, but do not mistake the Python oracle for a test of Godot physics or collision meshes.
